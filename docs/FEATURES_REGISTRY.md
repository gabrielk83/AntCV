# AntCV — Feature Registry

A living registry of **features** (distinct from the bug tracker at
`docs/qa/ACTIVE_BUGS.md`). Each feature is **CLOSED** (shipped + live), **OPEN**
(planned or in progress), or **PARKED** (descoped / blocked).

For product positioning / problem→solution framing (general statement, per-persona,
and the v~1.50.300 engineering work as problems solved), see
[docs/marketing/PROBLEM_STATEMENTS.md](marketing/PROBLEM_STATEMENTS.md).

Last updated: **2026-08-18 (39)** — INCREMENT: **FT-CL-GREETING — cover letter always opens with a hiring-manager greeting CLOSED** (owner-shipped 2026-08-17 via Fable 5, commit `e93d2940`; PWA `1.51.4266-cl-greeting`; registered + VERIFIED by CI nightly 2026-08-18, Opus 4.8; ACTIVE_BUGS top "CL-GREETING-001"). The cover letter now ALWAYS opens with a greeting to the hiring manager, by name when known. `gold-rules.json` `prompt_block` + fallback-regen rule + the PWA `meta.greeting` contract (`app.js`/`app.src.js` mirror; `antcv-bullet-targets.js` GOLD_FALLBACK_BLOCK regenerated from the site); `gen-runner.py` already carried the hm-name override. Suite 1574/1574; the fresh-generation content check (an actual generated CL opening with the greeting) is owed to a live-model / signed-in run. PREVIOUS INCREMENT: **2026-07-29 (38)** — INCREMENT: **FT-HDR-TYPE-CONTROLS — panel-owned size AND letter spacing for the five identity lines CLOSED** (desktop session 2026-07-29, Opus 5; ACTIVE_BUGS top "HDR-TYPE-CONTROLS-001"; PWA `1.51.3862`, docx-worker `1.14.173` deployed, run 30450517787). Name / Specialisation / Application / Contact / Slogan each get a font size AND a signed letter-spacing delta (0.05pt steps = exactly one DOCX `w:spacing` unit), on the CV and the CL, in preview and export. Specialisation and Application split into separate rows; Slogan gained a control at all. Also closed APPLINE-PARITY-001 (the CL application line was 11px in preview vs 10.5pt in export). PREVIOUS INCREMENT: **2026-07-22 (37)** — INCREMENT: **FT-IBSEN-HEADER-COLORS — Ibsen header + per-element colour package CLOSED** (desktop session 2026-07-22, Opus 4.8; ACTIVE_BUGS top "IBSEN HEADER + PER-ELEMENT COLOUR PACKAGE"; live `1.51.2201`→`2981` + docx-worker deploy). From the Ibsen 1017 reference. SHIPPED + verified: (a) **CL-NO-PHOTO-001** — no candidate photo on the cover letter (preview + docx); (b) **CL-SPEC-CENTER-001** — CL specialisation line centred; (c) **HEADER-ELEM-COLORS-001 + HEADER-COLOR-CONTROLS-001** — per-element BRAND colours (name white / spec accent / contact white / slogan sloganColor / application gray) with a plain solid swatch + ↺ reset in every candidate/slogan/app-line control (the swatch IS the native `<input type=color>` — a hidden-input `.click()` was Chrome-blocked); (d) **BRAND-RESET-001** — ↺ reset on the rich-block lead text+underline colour pickers; (e) **APPLINE-RULE-001** — rule under the application line (toggle/colour/thickness); (f) **EXPORT-HEADER-COLORS-001** — DOCX 1:1 parity (client fetch-guard injects per-element colours + app_line_color + app_line_rule into `/generate`; worker gives the app-line its own colour + renders the rule), render-verified 5/5 in the generated DOCX (`workers/docx-worker/test/diag-header-elem-colors-parity.mjs`); (g) **QUICK-CONTACT-SCOPE-001** — the contact-collapse bug (a Settings→Personal collapser's global observer mis-hid the section-editor row list; scope-guarded). Memory `header-elem-colors-system`, plan `docs/plan/IBSEN_HEADER_BRAND_CONTROLS.md`. Do not re-pick.

CI/test-hardening note (2026-07-29, Opus 4.8; not a numbered code feature — test-only, no version consumed) — **META-DRIFT-GUARD coverage lock made blob-immune.** `pwa/test/unit/meta-drift-guard-both-blocks.test.mjs` (the lock behind FT-family drift-guard work / OPEN_REGISTER rows 29·31·39a) went RED on `main` since `f584bd7` because CL-OPENING-SEED-985 inserted a ~700-char `clProseGuard` seed blob between each drift guard's `else{` and its `company: e.jd_company||""` adoption, pushing the guard token past the test's fixed 900-char proximity window — a false positive; all four adoption sites (both bundles) stayed correctly guarded. The test now asserts the guard token STRUCTURALLY (between each site and its nearest preceding `active_application`) instead of by char-distance — blob-immune, still fails if a guard is removed. No `app.js`/`app.src.js` change. Detail: ACTIVE_BUGS `META-DRIFT-GUARD-BOTH-BLOCKS-TEST-CI-RED-001` (`main` `9594413`, CI run 30461113328 green).

Previous (36) — INCREMENT: **FT-ROWFIT-FEEDBACK — ROWFIT feedback family CLOSED** (desktop session 2026-07-22, Opus 4.8; ACTIVE_BUGS top "ROADMAP — ROWFIT … ALL DONE"; live PWA `1.51.2460` → `1.51.2901`). Per-row **Fit-it (⇥) / Enhance (✨)** now work on rich_block rows AND groups, with storm-proof feedback. Legs, all SHIPPED + live: (a) **ITEM-ROWFIT-RICHBLOCK-001** — rich_block rows route through the proven `bullets_item` path in `il`/`ll` (was "not supported on row level"); (b) **LEAD-UNDERLINE marker rows + QCL/steering CV↔CL color+underline match** (docx side); (c) **ROWFIT-HOURGLASS-LATCH-001/002** (`1.51.2460`/`2681`) — the ⏳ never committed under the render storm / fast-failing relay; fixed with a MODULE-level latch in `antcv-rich-block-editor.js` (`__rowBusy`) that survives editor remounts (a `useEffect` owns the clearing timer); (d) **ROLES-STORM-CONVERGE-002** (`1.51.2680`) — `swallowMergedComponents` dropped already-hidden constituent roles that `repairExperienceCompleteness` re-adds → infinite delete/restore; one-line `c.on===false` skip; (e) **TOOLS-RESIDUE-CONVERGE-001** (`1.51.2740`) — `antcv-tools-hidden-residue.js` whole-blob guard defeated by unrelated churn; re-keyed on the TOOLS substructure + OSC damper; (f) **ROWFIT-PINK-LATCH-001** (`1.51.2800`) — section pink via module-level `window.__antcvSectionBusy` OR'd into the preview `Ce` pink flag; (g) **ROWFIT-GROUP-BUTTONS** (`1.51.2881`) — group sub-heading rows got ✨/⇥ acting on every child (editor dispatches `group:i`; `il`/`ll` expand to child `item:j` loops; Fit-it skips already-fitting rows via `al()`); (h) **ROWFIT-PINK-ROWLEVEL + ROLEGROUP** (`1.51.2901`) — per-ROW pink via `window.__antcvRowBusy["sectionId:item:i"]` OR'd into the per-row `<p>` style (groups free via the child-latch loop; roles already pink via per-role `Ce` `kr["sectionId:roleId"]`). All mirrored src↔minified `app.js`; each feedback leg verified in a real browser harness (incl. a remount); full pwa suite **1432/1432** green throughout; cache-bust quintets consistent per version. **No open ROWFIT legs remain** — do not re-pick.

Data/content note (2026-07-22, Opus 4.8; not a numbered code feature) — **Gabriel kernel: structured `eligibility` added.** The live Gabriel cloud kernel now carries structured eligibility fields (clean criminal record; no Russia / Russian-allied family ties; MISWG/NATO/EU + Israel-MISWG security context; references-on-request; Danish + global project availability) alongside the pre-existing MISWG/DDIS `securityClearance`, plus the background/accessibility "not limited his career" sentence and the rugby `INTEREST-RUGBY-LITERAL-001` override pin; `migrationVersion` → `2026-07-17-modernized-v12`. Appended additively onto the LIVE kernel (not via file import — the file lineage had drifted behind cloud). Procedure + the drift lesson: ACTIVE_BUGS `KERNEL-DATA-DRIFT-001`. Canonical superset: OneDrive `Gabriel_personalInfo_modernized_2026-07-17_v12.json` (real data, not committed); repo snapshot [docs/personas/gabriel/kernel_snapshot_2026-06-16.md](personas/gabriel/kernel_snapshot_2026-06-16.md) Eligibility mirrored (no contact PII).

Previous (35) — INCREMENT: DELETE-CONFIRM-INAPP-001 (`1.51.2146`, dd4730c) — the Application-History delete gate is now a two-click in-app confirm instead of a native confirm() (which embedded / installed-PWA browsers silently auto-cancel, so "delete did nothing"); registered below as FT-DELETE-CONFIRM-INAPP, suite 1389/1389, live-verified.

Previous (34) — INCREMENT: HTML-TO-PDF-001 (PWA `1.51.2072`, docx-worker `1.14.162`) — the JD-analysis report exports as an ATS-legible PDF through the CloudConvert docx-worker (new `POST /generate-analysis-pdf` + `convertHtmlToPdf`, Chrome engine) instead of the browser print dialog; registered below as FT-ANALYSIS-CLOUDCONVERT, worker CI 17/17 + PWA 1365/1365, live-verified e2e (0 `window.print` calls). See also this session's two export bug fixes (not features): EXPORT-PDF-PANEL-WORKER-001 `1.51.1556` (panel-open PDF routes to the worker) and EXPORT-PREVIEW-ZOOM-DECOUPLE-001 `1.51.2085` (export preview decoupled from editor zoom + no half-page clip) — both in ACTIVE_BUGS + OPEN_REGISTER + SESSION_LOG_2026-07-22_EXPORT.md.

Prior increment: **(33)** — APP-SWITCH-CONTENT-LANG-DETECT-001 (`1.51.2012`, 25a1d65) — the app-switch/boot language selector now shares babel-relang's vetted content-script detector, so a single residue CJK char no longer flips an English CV to Chinese ("every CV starts in Chinese"); registered below as FT-CONTENT-LANG-DETECT, suite 1370/1370, live-verified.

Previous (32) — INCREMENT (desktop session 2026-07-20/21, Opus 4.8; ACTIVE_BUGS top entries `SAVED-APPLICATION LIFECYCLE CLUSTER` + `LINKEDIN-CARD-EXTRACT-001`): the saved-application (History / JD-list) path gained real recoverability and de-duplication, and the LinkedIn manual-add pre-fill works again. Live at PWA `1.51.1708` / `1.51.1800` / `1.51.1882` plus access-relay `27def9ec`, cv-proxy `105bfd9c`, demo-proxy `c37d0871`. Registered below as FT-APP-RECOVER-JDONLY, FT-APP-DEDUP-NORMALIZED, FT-CONTENT-LANG-SELECTOR and FT-JD-LINKEDIN-CARD. Remaining legs = OPEN_REGISTER rows 92-94.

Previous (31) — INCREMENT (desktop session 2026-07-21, Opus 4.8; ACTIVE_BUGS top entry `ROLES-STORM-CONVERGE-001`): the ROLES leg of the at-rest `sections` write storm is fixed and live at PWA `1.51.1902` (408cc5c). Registered below as FT-STORM-CONVERGE-GUARDS. The durable lesson — a write-storm guard keyed on the WHOLE serialised blob is defeated by any parallel writer, and an N-hits-in-T-ms counter re-arms across the 2.5s poll — is why both guards are now keyed on the CONTESTED SUBSTRUCTURE and made sticky. Reproduced with a Node shim + FAKE CLOCK (`pwa/antcv-sections-normalize-415.storm.test.mjs`): RED 14/20 passes wrote pre-fix, GREEN <=2 post-fix; suite 1334/1334. NOT done: the competing writer was never identified live (pane tab `hidden` + the loaded app is converged; reaching the storming app mutates the owner account).

Previous (30) — INCREMENT (register-reconcile, antcv-nightly 2026-07-21 2nd dispatch, Opus 4.8; ACTIVE_BUGS top entry `REGISTER-RECONCILE — SIDEBAR-RICHBLOCK-NOJUSTIFY-001 (2026-07-21 2nd dispatch)`; OPEN_REGISTER 2026-07-21 STATUS block): since the earlier (29) reconcile (base `1.51.1644`) the day session shipped through PWA `1.51.1683`; one leg — **SIDEBAR-RICHBLOCK-NOJUSTIFY-001** (`1.51.1664`, commit b087dfb) — was in NO register (a rule-7 gap), now registered. It is the render-SOURCE root of the same certs/interests/tools left↔justify flap the (29) ALIGN-STORM/STORM-OSCILLATION-GUARD family treated at the symptom layer: `__rowAlign` defaulted a FLAT (non-grouped) `rich_block`'s content rows to `justify`; in the narrow sidebar column justify over-stretches into ugly inter-word gaps and `dejustifyNarrowSidebar` flips it back to `left` every pass — a left↔justify flap that reproduces on the template CV independent of pagination (owner screenshots: CERTIFICATES & COURSES, INTERESTS, TOOLS & METHODS). Fix at source (`app.src.js:6547`): sidebar content rows default to `left` (`(__hasGrp || S)`, S = the sidebar flag) so there is nothing for the de-justify pass to flip; main-column flat rich_blocks keep justify; grouped rich_blocks unchanged; explicit per-row CJLR still wins. Mirrored surgically into minified `app.js` (`(hg||N)?"left":"justify"`) — verified this run: both markers present ×1, suite's app.js↔app.src.js mirror tests green; cache-bust quintet complete at 1664 (prev `1.51.1644`→STALE). Verify-first (day-session lane, not re-implemented): standing probes all green on the current `1.51.1683` base — suite **1323/1323**, boot-smoke OK, Personal-panel DIAG PASS (0 mut/8s), button-audit **191 / 0-page-error / 0-DEAD / 0-throw / 112-active**; `app.js` head `(()=>{window` (minified-sacred intact). The other (29)-flagged day-session gaps (MANUAL-SAVE-CATEGORY-001 1576, AUTO-COMMIT-FRESHEST-001 1580, Application-History file-number 1584) have SINCE been reconciled into all three registers — confirmed this run. No code shipped by this nightly. Prior (29): INCREMENT (register-reconcile, antcv-nightly 2026-07-21, Opus 4.8; ACTIVE_BUGS top entry `REGISTER-RECONCILE — STORM-OSCILLATION-GUARD + ALIGN-STORM (2026-07-21)`; OPEN_REGISTER 2026-07-21 STATUS block): the P0 **"CV preview page-2 oscillation + freeze"** fix (owner live-diagnosed 2026-07-19/20) shipped across three day-session legs and was documented NOWHERE in `docs/` (only commit messages, no dedicated unit test) — a rule-7 gap, now registered; live-served + the 1323 suite still green. **ALIGN-STORM-001** (`1.51.1604`): the freeze + certs/interests alignment jumpiness was a text-align write storm (~200 writes/s; 5054 mutations + 68 long-tasks/25s) from three CJLR alignment sidecars fighting the same cells — `core-comp-234` self-feedback (observed `style` while writing `textAlign`), `antcv-item-align.js` untethered body observer (throttled ≥300ms), `antcv-section-align.js` writing `core_comp` to a different value than 234 (deferred to owner). Halved the storm; root was upstream. **STORM-OSCILLATION-GUARD-001** (`1.51.1624`): the root — `antcv-sections-normalize-415` ping-pongs with a competing writer (`roleCanonTitles` shortens a title → `_samePosition` misses → `repairExperienceCompleteness` restores a hidden role → `dropCanonHiddenDups` strips it → dispatch `antcv:sections-updated` → full re-render → align storm). Fix: refuse to WRITE a serialised `sections` already written within ~4s (STORM-IDEMPOTENT companion keeps true no-ops silent). **add-side** (`1.51.1644`): guard fired but the competitor's slightly-different `sections` each cycle dodged the recent-writes dedup → self-limit `repairExperienceCompleteness` (suppress after the identical missing-role signature `id|title|company` 3×/6s; restored roles are HIDDEN `on:false`, safe to stop re-adding). Verify-first (active day-session lane, not re-implemented): deployed+served confirmed (live seed = TARGET `1.51.1644-compl-selflimit`, served sidecar carries the `oscillation held` guard, cache-bust quintet consistent, STALE invariant held); standing probes green (suite 1323/1323, boot-smoke OK, Personal-panel DIAG PASS 0-mut, button-audit 190/0-error/0-DEAD/0-throw). NEW finding registered (low-pri): STORM-GUARD-TEST-COVERAGE-001 — the two `Date.now()`-window freeze guards ship with no dedicated unit test (suite count unchanged 1323→1323); a refactor could silently drop them and the freeze would return uncaught by CI. Also flagged as day-session rule-7 gaps (not registered in detail — their lane): MANUAL-SAVE-CATEGORY-001 (1.51.1576), AUTO-COMMIT-FRESHEST-001 (1.51.1580), Application-History file-number (1.51.1584). No code shipped by this nightly. Prior (28): INCREMENT (FT-JOBTRACKER, job-tracker nightly; ACTIVE_BUGS top entry `JOBTRACKER-PROPOSED-TAB-PATH-QUOTE-001`; OPEN_REGISTER 2026-07-19 job-tracker STATUS ADVANCE): re-diagnosed a 07-15 finding — the "Proposed Inbox" tab had been silently skipping on every nightly since PROPOSED-INBOX-TAB-001 shipped, previously blamed on a path-quoting bug in `job-tracker-sync.py`. The real cause: the exception print sliced the message to 90 chars (hiding a real `FileNotFoundError`), and the scheduled-task SKILL.md only named the containing Drive folder for `JOB_XLSX`, not the exact filename `build_workbook.py` hardcodes (`Gabriel_Job_Search_Workbook_2026.xlsx`) — so every nightly guessed wrong and the tab never rendered. Fixed the truncated print (full exception now shown) + corrected the SKILL.md filenames; re-ran `pull --render` and confirmed live: `added 'Proposed Inbox' tab (6 leads)`. Also generated + persisted **Teledyne / Manufacturing Manager** as application_id=2696 (quick tier, `--force` regen — its prior artifact pointer, app 805, had been deleted), verified byte-exact CV 2pg / CL 1pg, 0 banned dashes; doc rev 97→98; Excel refreshed. No PWA/worker code touched. Prior (27): INCREMENT (register-reconcile, antcv-nightly 2026-07-19, Opus 4.8; ACTIVE_BUGS top entry `REGISTER-RECONCILE + VERIFY (2026-07-19)`; OPEN_REGISTER 2026-07-19 STATUS ADVANCE): two shipped-but-unregistered released day-session PWA fixes (documented nowhere in `docs/` — a rule-7 gap) now registered, both live-served + in the green 1323 suite — **EXPORT-PDF-PANEL-WORKER-001** (`1.51.1556`, bea9cf6): with a side panel open the app's PDF-export button is unmounted, so "Save as PDF" fell back to the browser printer instead of the CloudConvert docx-worker; `antcv-pdf-preview-gate.js` now calls `window.exportPdfViaWorker` directly (same `__antcvUseServerPdf` policy, + DEMO watermark on the from-storage DOCX fallback) when the app button is absent; test `export-pdf-panel-worker.test.mjs` (7). **MERGE-COMPONENT-SWALLOW-001** (`1.51.1557`, 8633d8e): a regenerated CV kept BOTH a merged role "X & Y" and its bare component "X" (`dedupeRoles` is exact-title-only); `swallowMergedComponents` in `antcv-sections-normalize-415.js` drops the bare component of an explicit `X & Y`/`X / Y`/`X and Y` merge (same company, overlapping years, space-aware so "R&D" survives); test `merge-component-swallow.test.mjs` (3). Otherwise verify-only: standing probes green on the rebased `1.51.1558` base (suite 1323/1323, boot-smoke OK, Personal-panel DIAG PASS 0-mut, button-audit 197/0-page-error/101-active — 5 "unclickable" = the known stale-locator race on self-mutating toggles per OPEN_REGISTER row 23, not a regression), PWA live = TARGET `1.51.1558-unsol-subtitle-slogan-gate` (no version regression / no stale-SW mask), worker layer no-drift verified in-repo (worker `/health` live-attest still env-blocked: shell DNS-gated to `*.workers.dev` + Browser pane denies the un-approved origin non-interactively). Finding actioned same session (owner "fix findings from tonight"): relay HYGIENE-CATEGORY-DOWNGRADE-001 (965ca92) was already LIVE (day-session deploy run 29677003317, 06:47Z) but its VERSION was unbumped so `/health` couldn't attest it (repeat of 07-16 LEAD-UNDERLINE-VERSION-BUMP-001) — **CATEGORY-DOWNGRADE-VERSION-BUMP-001** (`9453a11`) bumped `RELAY_VERSION auth-33-cse-brave → auth-34-category-downgrade` + redeployed access-relay (run 29678819618, ✓); no behaviour change, `/health` now truthful (live eyeball owed — worker-attest env-gate unchanged). Prior (26): INCREMENT (FT-JOBTRACKER, job-tracker nightly; ACTIVE_BUGS top entry `JOBTRACKER-TERMA-JD-RECOVERY-001`; OPEN_REGISTER 2026-07-16 STATUS ADVANCE + rows 95/96): recovered the real **Terma / Advanced Systems Engineer (req 1167)** JD from the SuccessFactors server-HTML (the add-time scrape had captured title-only — 172c of 277c; the body, incl. MISWG/MBSE/clearance, IS in-page), wrote the clean 6764c JD to the doc + a grounded clearance/MISWG differentiator to `signals[uk]`, and generated + persisted **app 2642** (EN, high/opus, `engineering_hardware`) — byte-exact verified CV 2pg / CL 1pg, **0 banned dashes** (direct PDF glyph scan), 15 CV + 8 CL sections, subtitle+slogan, jd_text saved, **no fabricated PhD** despite the JD preferring one, DK-dates scrubbed, Pan Idræt → `foreningsarbejde` interest. Cleared the stale `nvidia_siph` ⏰ flag; doc rev 90→92; Excel refreshed; active pointer restored to 1008; owner PushNotified (+ deliverable note: Terma asks for no photo → toggle off on open). NEW finding registered: JOBTRACKER-SUCCESSFACTORS-SCRAPE-TITLEONLY-001 (proxy JD-fetch should extract the SuccessFactors description block; deploy-gated). Prior (25): INCREMENT (docx-worker deploy attestation; nightly verify+report; ACTIVE_BUGS top entry `LEAD-UNDERLINE-VERSION-BUMP-001`): the LEAD-UNDERLINE-001 export leg (renderRichBlock `make` lead TextRun emitting `w:u`, commit 151f129) went live 2026-07-16 01:08 (docx-worker build ced11789) but its `VERSION` constant was never bumped, so `/health` kept reporting `1.14.160-edu-row-cjlr-export` and could not attest the deploy — nearly mis-read as "undeployed" during tonight's live-verify. Bumped `VERSION → 1.14.161-leadin-underline` + redeployed (run 29465290683 success); `/health` now truthful (no behavioural change; code already live). Rest of the run was verify+report — all standing probes green (PWA suite 1313/1313, boot-smoke OK, Personal-panel 0-mut, button-audit 195/0-throw/0-error), all A1/A2/content guard sidecars + workers live-served at TARGET 1.51.1524. Prior (24): INCREMENT (FT-RELAY-COST-QUALITY-TUNE weekly run: first real head-flip shipped — coherence `anthropic→openai`, both proxies deployed; analysis→gemini held owner-gated; ACTIVE_BUGS RELAY-COHERENCE-HEAD-OPENAI-001; report COST_QUALITY_WEEKLY_2026-07-15.md). Prior (23): INCREMENT (FT-JOBTRACKER, job-tracker nightly + FOUNDATION-LANG-001; ACTIVE_BUGS top entry): tonight's nightly generated + persisted the 2 real eligible rows — **Napatech** Technical Product Manager (app 1020, DA, high/opus, product_management; web-researched SmartNIC/DPU + Copenhagen-R&D facts folded into signals) and **Aimpoint AB** Engineering Manager (app 1022, EN, high/opus, engineering_hardware) — both byte-exact verified 2pg CV / 1pg CL, 0 banned dashes, correct per-app subtitle+slogan, greeting correctly generic (Aimpoint's JD names only a recruiter, not greeted, per the greet-only-hiring-manager rule); the two "Career Opportunities" Terma rows correctly skipped as whitespace-padded stubs (155/172 real chars). **Build increment FOUNDATION-LANG-001** (`scripts/job-tracker/gen-runner.py`, runner-side, no deploy): the CL foundation labels (`Foundation`/`Hands-on`/`Professionally`) + fallback intro now localise to the JD language via `_FOUNDATION_FURNITURE` + `_flabel()` (were hardcoded English, leaking into DA/SV letters); unit-verified da/en/sv, back-patched the affected live app 1020, re-verified clean. Prior (22): INCREMENT (FT-SLOGAN-CONTROLS, SLOGAN-UNSOL-GENERIC-001 + SLOGAN-CAP-DANGLE-VERB-001; ACTIVE_BUGS top entry + `docs/qa/SLOGAN-UNSOL-GENERIC-DIAGNOSIS_2026-07-15.md`; `1.51.1504-slogan-unsol-generic`): an UNSOLICITED application now keeps the GENERIC standing default slogan (the specialisation triad), never a role-tailored one — owner report of "AS A PRODUCT AND PROJECT EXPERT, I BRIDGE" (a role-tailored line AND truncated mid-clause to a dangling verb on an open application). Three parts, all `antcv:disable-slogan-unsol-generic`-killable: (1) generation (`gen-runner.py` `_is_unsolicited` gates `_meta["slogan"]`; in-app gen blanks `meta.cl_slogan` for unsolicited); (2) render/export yield — shared `window.__antcvSloganUnsolActive` + `__antcvSloganOverrideIsGen` skip `io.cl_slogan` + an auto-copied gen override at every read (`__antcvResolveSlogan`, React render, srcdoc PDF, docx-client DOCX) → fall through to `io.subtitle`, while a genuinely USER-EDITED override is still honored; the load-path copies stop seeding a tailored slogan and `antcv-cl-slogan-fresh.js` drops a gen-equal override so already-generated unsolicited apps yield without a regen; (3) SLOGAN-CAP-DANGLE-VERB-001 — `window.__antcvSloganCap` gains the Python cap's clause-cut + a `__antcvSloganDeDangle`/`_scrub_dangle_verb` trailing pronoun+verb scrub in BOTH caps so a hard chop never strands "…I bridge". Headless verify 9/9 (extracted from the shipping bundle, incl. user-edit-honored + kill-switch), PWA 1313/1313 green, `slogan-meta-stamp` lock updated. Prior (21): INCREMENT (FT-SLOGAN-CONTROLS + FT-PREVIEW-INLINE-EDIT + FT-BRAND-DECIDES + header/photo polish — a long owner-live-driven session + a 6-way parallel spawn round; ACTIVE_BUGS top two entries). **Slogan controls (1.51.1144→1.51.1424):** Enhance (LLM rewrite, 4-8 words, undoable) + Fit-it (client word-cap; the double-cap no-op fixed) + a single CJLR cycling button (L→C→R→J, renders accept justify); **SLOGAN-CJLR-RERENDER-001** (1.51.1204) fixed the real "align doesn't change preview" bug — the sections-updated handler's STORM-IDEMPOTENT-002 guard suppressed re-renders for localStorage-only changes, now bypassed on a slogan/standalone/signoff/signature event reason (REUSABLE GOTCHA); **SLOGAN-LANG-GATE-001** (1.51.1404) rejects a wrong-language override → specialisation fallback, branded==non-branded parity; SLOGAN-WORD-CAP-DANGLE-001 (1.51.1484) drops a trailing dangling stopword. **Preview inline edit:** pubs/patents rich inline edit (1.51.944), contact per-item edit + envelope + em-space + CV/CL font parity + worker converge, EDIT-COMMIT-LAG-001 (results/text no longer revert on blur), specialisation spellcheck, **SPELL-APPLY-STANDALONE-001** (1.51.1164) commits spell corrections to the standalone slogan/spec/application editables (were no-ops), + blip-nudge near page folds. **Header/photo:** PHOTO-NUDGE-UP-001 (1.51.1244) raised the seam-centred medallion + pulled sidebar text up (flow-neutral, no bottom white gap), tuned live to −99.5/30; **header seam line #0b** — conditional header↔sidebar separator in photo-contour colour when headerBg===sidebarBg, preview + OOXML, **DEPLOYED** (docx-worker 1.14.160). **FT-BRAND-DECIDES:** BRAND-DECIDES-RESEARCH-001 (7bf683f) — brand site-crawl collects spirit/values/tone with colours, slogan text+placement fuse to it; Anita headless demo both arms (`docs/qa/ANITA_BRAND_DECIDES_DEMO.md`). **Worker slogan colour** follows the brand (contrast-guarded), **DEPLOYED** same worker run 29376733229. **Also this round:** META-STICK-001 + LOAD-EDITOR-UNSOLICITED-001 shipped (1.51.1344 kill-switched + 1.51.1425 complete), roles-as-rich_block E-cutover default-on (1.51.1264), density grow-veto→shrink lever (`docs/qa/DENSITY_SWEEP_REPORT_2026-07-15.md`), GROUP-EMPTY-HIDE + table-header-center mirror-lock tests un-staled (PWA gate 1280/1280 green). Discipline: single docx-worker deploy after full gate green (proxy 103 / pwa 1280 / worker 17, 0 fails), discarded a stale staged CHANGELOG downgrade a concurrent session left in the shared index. Prior (20): INCREMENT (FT-PHOTO-FLIP, AUTO orientation RESOLVED via gated vision fallback; ACTIVE_BUGS top entry + `docs/qa/PHOTO-FLIP-001-STATUS.md`): 1.51.942 SHIPPED a cost-gated vision fallback — new `POST /api/photo-orientation` in BOTH `workers/proxy` + `workers/demo-proxy` (`photo-orientation.js`, route mirrors jd-analysis), called from the client (MODULE D) ONLY when the free local BlazeFace+heuristic returns center/unknown (once per photo, 12s abort, failure keeps local; `DET_VER=m4`). **Mistral finding (confirmed via raw API):** this account's Mistral key answers even the bare `pixtral-12b` with `served:"ministral-14b-latest"` (text, blind) — no Pixtral access (owner action: enable a Pixtral plan). Handler tries Mistral `pixtral-12b` first but GUARDS it (auto-activates iff a real pixtral serves), real work today via Claude (claude-sonnet-5) → OpenAI (gpt-4o) with per-provider image shapes. Both workers deployed + verified live (left-profile→left, flip→right). Prior (19): INCREMENT (FT-PHOTO-FLIP, photo horizontal flip Off/On/Auto; owner live-test session; ACTIVE_BUGS top entry + `docs/qa/PHOTO-FLIP-001-STATUS.md`): new Flip control in the Settings→Layout collapsible PROFILE PHOTO control (`pwa/antcv-photo-ui-427.js` MODULE D + `antcv-docx-client.js` export mirror). **DONE + owner-confirmed:** Off/On mirror the live preview, the exported DOCX/PDF (client canvas-mirrors the PNG before send), and the upload-menu carousel avatar; mode+facing in standalone keys (`antcv:photoFlip`/`photoFacing`) surviving Reset-all/cloud-restore; detector-version tag re-runs detection on cached photos. 1.51.842 fixed the control vanishing (anchor to the position-button-row parent, never the <40-char-capped label; case-SENSITIVE "PROFILE PHOTO" so it never grabs the title-case "Profile Photo" upload block). **OPEN:** AUTO orientation unreliable on near-frontal photos — bundled BlazeFace (1.51.784, lazy CDN) reads HEAD YAW only; owner's head-frontal/angled-torso shot needs a body cue the face model can't see (heuristic insufficient too). Workaround = manual On/Off. **PLANNED (not built):** cost-gated Mistral-vision fallback (`demo-proxy POST /api/photo-orientation`, called from client only when local detector returns center/unknown; relay already allows mistral, demo-proxy already has Mistral image_url vision plumbing). Prior (18): INCREMENT (FT-JOBTRACKER gen family, job-tracker nightly; OPEN_REGISTER 2026-07-14 block + rows 94/95, ACTIVE_BUGS top entry): JOBTRACKER-ELIGIBILITY-CONTENT-LEN-001 — `scripts/job-tracker/gen-runner.py` `eligible_rows` now gates on `jd_content_len()` (whitespace-collapsed length) instead of raw `len(jd)>200`, so scraped "Career Opportunities:" listing rows (bare title + a Requisition-ID line padded with hundreds of CRLFs — 260/277 raw but 155/172 real chars) no longer clear the JD gate and force fabrication; verified the real 3Shape JD (6941c) still passes and the two hollow rows are excluded (`list` clean, `py_compile` OK, no worker/app.js surface). Same run generated + persisted the one real eligible row (3Shape Senior Project Manager, app 914, high/opus, program_management), verified full-fidelity + 0 banned dashes via `export_pdfs.py`, and folded web-researched TRIOS-6-hyperspectral-optics facts into signals. Prior (17): INCREMENT (FT-RELAY-COST-QUALITY-TUNE, production-live routing release; OPEN_REGISTER row 38; ACTIVE_BUGS top entry): closes the last of RELAY-TUNE-COVERAGE-GAP-001. (1) RELAY-DETECTION-GAP-001 — the keystone: the cost-quality health signals were BLIND to FORMAT-broken output (SSE-leak / empty-despite-tokens / control-garbage / off-language), so a silently-broken-but-cheap provider kept getting routed; added a pure detectMalformedOutput() + malformed_output signal + llm_calls column + aggregation rate + a scoreHealth penalty (server, backward-identical at rate 0) + a client mirror sidecar tagging every llm_call (1.51.580). This makes every cost-flip SAFE — a provider that starts emitting garbage now self-demotes. (2) analysis/kernel low-traffic pin: proposeRoles judges an unpinned role vs its blended baseline + a low activation floor (30) so it bypasses until a genuinely cheaper provider proves out (no routing change today; auto-pins gemini once it grows). (3) long_context + consensus_poll cost handled by the existing RELAY-COST-TIEBREAK-001 auto-seed (openai's health gap clears the 0.10 demotion threshold, provider stays reachable — no voter lost, fallback intact), now made SAFE by the detector; a manual long_context openai-drop was implemented then reverted as redundant with that design. Tests: relay 105, tune 14, malformed 7; PWA 1257. Prior (16): INCREMENT (FT-RELAY-COST-QUALITY-TUNE, LLM cost routing; OPEN_REGISTER row 38): the router now cost-tie-breaks equal-quality providers END TO END — (a) COMPRESS-COST-OPENAI-DROP-001 (1.51.538, f8350a1) dropped openai from the client `Z.compress` dispatcher (~$60/wk saved, 0 quality loss on a mechanical pass); (b) proxy `ROLE_KEYS` extended so `roleHeadOrder` reaches jd-analysis/kernel (identity-safe); (c) RELAY-COST-TIEBREAK-001 (1.51.578 + relay fix 7af73aa, access-relay deployed) — `scoreHealth(metrics, costCtx)` folds a bounded, status-safe cost penalty into `health_score` for cost-sensitive tasks (adequacy judged on SUCCESS≥0.85 so it fires on the inherently-high-latency compress/long_context/consensus 'warning' band — v1's `status==='ok'` gate was inert), and the client health-seed now demotes adequate cost-losers (health gap ≥0.10 below the task's cheapest-equal-quality) → openai auto-sinks on long_context+consensus_* (~$15/wk) without per-task edits, refreshed every ~5-min cron. Tests: cost-tiebreak 8/8, model-roles 12/12, PWA 1257/1257. Prior same day (15): CLOSED (cluster demand model, OPEN_REGISTER row 9 — **now in production**): the whole CLUSTER-QUAL-001 §3/§7.6 pipeline is live. Brought the fuse to production (access-relay redeployed with the union writer; client union auto-deployed on the #349 merge, live-verified `antcv.pages.dev/app.js?v=1.51.579-cluster-fuse`), wired the write step into the `antcv-demand-seed-weekly` SKILL.md (`node scripts/cluster-demand-research-push.mjs --url https://antcv-access-relay.karp-gabriel-a.workers.dev`, relay URL also in `~/.claude/settings.local.json`), and RAN THE FIRST D1 POPULATION: `total_inserted:181` (pm_process 21 = 20 fresh + 1 retained). Verified in D1 (`ant_memory`): rank-scaled weights step 0.400→0.020, "Obsolescence management" retained at floor 0.010 (**nothing lost**), rollup rank-5 = the new "AI/GenAI + EU AI Act" item in deterministic order. OPEN_REGISTER row 9 CLOSED (both the TO-DO-summary and verify-first-queue entries). Prior same day (14): INCREMENT (cluster demand model, OPEN_REGISTER row 9): DEPLOYED the source='research' D1 writer (access-relay deployed; CLUSTER_RESEARCH_TOKEN set; /health 200 + endpoint 401-gated live-verified), then shipped the FUSE (owner "fuse the deterministic and research list so nothing is lost", 1.51.558-cluster-fuse): (a) WRITER-side union — `insertResearchQualifications` retains a research qual dropped from a new weekly top-20 at a floor weight (`RESEARCH_WEIGHT/40`, below rank-20) so it stays in `application_qualification` and can resurface, instead of a destructive delete+insert; (b) CLIENT-side union — `__clusterRule` (app.js + app.src.js) now UNIONs the live D1 top-20 with the static deterministic SEED (dedup by normalized text, live leads, SEED-only appended, slice 20→24, all temp vars IIFE-scoped to avoid the minified shadow hazard) instead of live-OR-seed, so a qual dropped from one list still surfaces from the other. Tests: writer 14/14 (union-retention, stable-across-pushes, resurface), cluster-rule-live-preference 8/8 (incl. an executable union lock extracted from the shipped app.src.js), pwa suite 1260/1260, access-relay 81/81. Cache-bust quintet → 1.51.558-cluster-fuse. Prior same day (13): FT-RELAY-COST-QUALITY-TUNE hardened (owner ask "fix RELAY-TUNE-COVERAGE-GAP-001"; shift 558-577): the weekly tune loop is now genuinely functional, not blind. 1.51.538 fixed the compress lever client-side + extended the router ROLE_KEYS, but left the scorer half-wired (normProvider defined-but-unused; proposeRoles never iterated the new analysis/kernel roles). Completed script-only (no worker/MODEL_ROLES change): normProvider wired into scoreRows, proposeRoles scores the union of pinned + TUNABLE_ROLES (unpinned proxy cascades now proposable), coherence→apply_correction, and summarizeClientDispatch() surfaces the client-dispatched levers (consensus_poll/long_context still openai-led) MODEL_ROLES can't reorder — routed to the client ee() router as owner decisions with the format-broken detection-gap caveat. Verified vs the real 7-day D1 aggregate; tests 8→12. Evidence: ACTIVE_BUGS top entry + OPEN_REGISTER row 38; FT-RELAY-COST-QUALITY-TUNE row. Prior same day (12): INCREMENT (cluster demand model, OPEN_REGISTER row 9 — owner ask "build the nightly D1 source='research' writer now"): built the production `source='research'` WRITER, closing the last-open leg of the CLUSTER-QUAL-001 §3/§7.6 pipeline. `POST /api/cluster-demand-research` (access-relay, dedicated least-privilege token `CLUSTER_RESEARCH_TOKEN`) + `insertResearchQualifications` write research top-20s under the `__global_market__` sentinel with `application_id` NULL (never inflates jd_count) and a RANK-SCALED weight `RESEARCH_WEIGHT*(21-rank)/20` — deterministic order (a flat weight would tie every qual and lose the researched order the gen prompt reads back), every value ≤0.4 < a real required-JD qual (1.0) so live user-JD signal still overtakes research. Idempotent per cluster (delete research rows, keep jd rows); inserts all clusters then recomputes each (correct cross-cluster shared_clusters). `scripts/cluster-demand-research-push.mjs` is the routine's one-command write step (replaces the 2026-07-10 manual D1 write); no wrangler cron needed (the scheduled Claude session does research + calls the script). Tests: `cluster-demand-research-writer.test.mjs` (12, vm-extraction + fake-D1) + `cluster-demand-research-push.test.mjs` (6); full access-relay suite 79/79. Deploy owner-gated (set token + deploy access-relay per google-cse-setup §8). Prior same day (11): MODEL-TABLE-FRESHNESS-001 (owner ask "fix tables + make the cost-quality tune routine execute the needed modifications"): the two proxy cost tables had gone stale on AntCV's current gen pins — `claude-opus-4-8` (flagship since 1.51.332) was silently 3× over-priced via the legacy `claude-opus-4` substring, `gpt-5.5` (thorough tier) ~24× under-priced via `gpt-5`, both mis-metering the demo spending cap. Fixed in all 4 worker files (explicit RATES entries in both `demo-enforcement.js` + `opus-4-8` into both `multi-llm.js` cascades) with a new mirrored `model-table-freshness.test.mjs` (5/5; proxy suite 94/94, demo-proxy 25/25); worker deploy owed. RELAY-COST-QUALITY-TUNE-001 gained procedure step 1a MODEL-TABLE FRESHNESS AUDIT so the weekly tune re-verifies+fixes the price table before scoring (a stale rate silently corrupts every routing decision). Evidence: ACTIVE_BUGS top entry + OPEN_REGISTER row 89; FT-RELAY-COST-QUALITY-TUNE row updated. Prior same day (10): INCREMENT (cluster demand model, OPEN_REGISTER row 9): first LIVE-triggered weekly demand-seed refresh (scheduled task antcv-demand-seed-weekly, Fri 22:00; prior the routine had only one manual 2026-07-10 run). 1.51.478-demand-seed-refresh: re-researched all 9 clusters from current 2025-2026 postings/skills reports (WebSearch fallback — Google CSE still 403/no token, Brave-first relay fix still undeployed), applied 5 bounded evidence-backed rank shifts to the client SEED (`pwa/antcv-cluster-demand.js`): pm_process gained an "AI/GenAI tool fluency + EU AI Act" item (61% of PM postings now require AI; pm_process previously had NO AI item), and executive (AI strategy r4→r2), engineering_software (secure coding/NIS2 r11→r8), people_soft (AI literacy r4→r3), data_analytics (AI/agentic r6→r5); consulting/finance/photonics_eng/research_phd confirmed unchanged. Cache-bust quintet + suite 1257/1257. docs/analysis/cluster_top20_research_2026-07-13.json. Prior same day (9): NEW ROW: FT-GOLD-DENSITY-SYSTEM (the density/gold marathon closeout, commits 633fbd6 + a7cbdfe): gold-rules.json as the ONE control site for every generation (prompt block + caps + patterns, drift-guarded), the measure->target->rewrite density loop in gen-runner persist, PERSIST-QUALITY rules (results change-metric, certs, pubs canonical order, labels, prose health), NARROW-CELL-CASCADE monitor + tableRatio ladder, and the per-file gold_audit — FINAL AUDIT GREEN 20/20 tracker apps. Evidence: ACTIVE_BUGS top entry + OPEN_REGISTER rows 85 (closed)/86. Prior same day (8): FT-SCHEDULED-ROUTINES hardened: standing END-OF-RUN REGISTER REPORTING clause added as rule (5) in `docs/qa/SCHEDULED_ROUTINES.md` (owner ask) — every agent-driven recurring routine (antcv-nightly, antcv-job-tracker-nightly, antcv-position-discovery, the cloud Routine, weekly cost-quality tune / demand-seed) must write its run outcome into OPEN_REGISTER (advance rows + add rows for new bugs/tasks) + ACTIVE_BUGS (every fix) + FEATURES_REGISTRY (any feature increment) before ending, and push those edits with the work; the three scheduled-task prompts and CLOUD_ROUTINE_PROMPT.md were updated in the same pass; non-agent routines (GitHub security audit, relay health cron, model-freshness cron) can't write registers — the next nightly's standing sweep transcribes their findings. Prior same day (7): FT-ROLE-CANON-LANG hardened by the golden-matrix CONTAINMENT AUDIT + owner rulings (1.51.396 commit 07df2e5 / 1.51.397 commit 4c71e18): canon id fixed to gen-runner's real "earlier-career"; core-comp ROW SPEC corrected (3-4 TABLE rows, cells <=2 rendered column lines — matrix caps 4/min 3, 290 guard floor 3, the app prompt was right); "EU citizen" canonical swept (legacy capital still matched); role_bullets_max wired as a GENERATION cap in the gold prompt block; gen-runner _EC_TITLE +es/zh; pre-existing gen-runner.py IndentationError on main fixed. OPEN_REGISTER rows 82 (open) + 84 (closed with rulings), ACTIVE_BUGS top entries. Prior same day (6): bugfix companion to the tracker-Open seeding (no new feature row): JD-REMOVE-STICKY-001 (1.51.395, commits f231723/c90b63e) — the JD chip's "✕ Remove" now STICKS across refresh via a device-local tombstone, while a tracker Open/Reopen and "Read from Cloud" still re-stage the JD (the OPEN-JD-VISIBLE-001 seeding the Open flow depends on is suppressed only for the specific removed jd; cloud jd_text untouched, the tracker keeps the row's JD). Evidence: OPEN_REGISTER 2026-07-13 CLOSED block + row 83, ACTIVE_BUGS top entry, jd-remove-tombstone.test.mjs (suite 1257/1257). Prior same day (5): CLOSED: FT-ROLE-CANON-LANG (golden gating matrix role control: gold-rules.json roles section + en/da/es/zh canonical titles, normalize-415 deterministic enforcement, suite mirror-gate; 1.51.394). Prior same day (4): FT-PHOTO-LIBRARY strip layout follow-up (1.51.393-photo-strip, commit 7d5d3c2): "Library:" label removed; the strip renders its own "↺ Reset" directly UNDER "＋ Add photos…" in the same teal-outline style (drives the hidden native Reset + empties the library). Prior same day (3): CLOSED: FT-PHOTO-LIBRARY (profile-photo library 1.51.382, retro-registered, + the owner's button-fuse rework 1.51.390/391: ONE "＋ Add photos…" control with compress-on-add, "Change photo" hidden, "☆ Save current" removed, Reset also empties the library; the strip's extra buttons had triggered the CJLR photo-panel leak PW-CJLR-PHOTO-LEAK-002, fixed in the same push — OPEN_REGISTER 2026-07-13 CLOSED block + row 81). Prior same day: CLOSED: FT-JOBTRACKER-SIGNAL-MATERIALS (Signals 📎 attach materials, island 1.51.364; plus the 1.51.365 Open-flow fix trio — device-stamped pointer / brand-fit checkbox arming / delegated tracker button — logged in ACTIVE_BUGS + OPEN_REGISTER rows 78-80). Prior same day: CLOSED: FT-JOB-DISCOVERY (bi-weekly Sun+Tue position discovery task + safe-persist helper, propose-only), FT-JOBTRACKER-FIT-TOP5 (fit-ranked Top-5 re-evaluated on every add + Pin/Park/Reject + auto-tier + hybrid auto-fill, island 1.51.388), FT-JOBTRACKER-PROPOSED-INBOX (Excel "Proposed Inbox" tab on sync --render). Prior: **2026-06-23** — CLOSED: FT-GEN-CYCLE-OPT (generation-cycle optimization 1.50.819–829: per-mode provider fan-out width, recruiter auto-backfill for non-quick gen, parallel column compress, cost-quality routing scorer + tuning). Prior: **2026-06-18** — OPEN: FT-PERSTYLE-KERNELS (per-writing-style unsolicited kernels, Phase A worker + Phase B app shipped; Phase C UI/auto-load TODO). Prior: **2026-06-11** — CLOSED: SHARE-TARGET-JD-URL-001 (1.50.375), PHOTO-SIDEBAR-BRIDGE-001 (preview 1.50.368–372 + worker 1.14.51–53), ADV-INDENT-CONTROLS-001 export-parity (worker 1.14.47/49), EXPORT-PAGE2-001 print path (1.50.374) + export-preview page selector. BRIDGE-SIDEBAR-PALETTE-001 retired by owner direction (bridge uses standard sidebar palette since 1.50.371).

Status legend: ✅ CLOSED (shipped) · 🟡 OPEN (active/planned) · ⚪ PARKED.

---

## CLOSED — shipped & live

| ID | Feature | Where | Notes |
|----|---------|-------|-------|
| FT-HDR-TYPE-CONTROLS | Every identity line on both documents is fully typographable from the panel — Name, Specialisation, Application, Contact and Slogan each carry a font SIZE and a signed LETTER-SPACING delta (expansion +, compression −), in 0.05pt steps, applied identically in preview and export | `pwa/app.js` (prelude `__antcvTrackPt`/`__antcvTrkCss` helpers on `window`, `__antcvTrkStep`, the Font sizes panel, the preview band, the CL slogan + application line, the `name_block`/`spec_block`/`contact_line` figure-layout leg, the HTML export, the in-app DOCX stylesheet) + `antcv-copenhagen-v2-001.js` (panel deltas emitted after the measured fit) + `antcv-docx-client.js` (`buildFontSizes` whitelist) + `antcv-pdf-preview-gate.js` + `workers/docx-worker/src/index.js` | HDR-TYPE-CONTROLS-001 PWA `1.51.3862-hdr-type-ctrl`, docx-worker `1.14.173-hdr-type-ctrl` (deployed, run 30450517787). New `fontSizes` keys `applicationSize`/`sloganSize` + `nameTrack`/`specTrack`/`applicationTrack`/`contactTrack`/`sloganTrack`. 0.05pt = one twentieth of a point = the `w:spacing` unit, so a panel step is lossless on the Word leg; DELTA semantics keep every existing document byte-identical at 0. Four silent blockers removed (docx-client key whitelist, copenhagen `!important` fit, worker-pinned slogan/app-line, pdf-gate reading a dead mirror). Tests: `pwa/test/unit/hdr-type-controls.test.mjs` (13) + `diag-hdr-type-controls.mjs` (12, incl. a zero-delta inertness proof); suites 1504/290/50-of-50. |
| FT-ANALYSIS-CLOUDCONVERT | The JD-analysis report exports as an ATS-legible PDF through CloudConvert (the same server pipeline as the CV/CL export) instead of the browser print dialog | `workers/docx-worker/src/index.js` + `src/cloudconvert.js` `convertHtmlToPdf` + route `POST /generate-analysis-pdf`; `pwa/antcv-analysis-report-pdf-360.js` `exportPdf`/`exportViaWorker` (worker-first, `printViaIframe` fallback) | HTML-TO-PDF-001 PWA `1.51.2072` 60ba37b, docx-worker `1.14.162-analysis-html-pdf` (deploy run 29899051626). CloudConvert Chrome engine (print_background, A4 zero-margin); BYOK `X-CloudConvert-Key` + CORS allow-header added. Test `html-to-pdf.test.mjs`; live `/health` + direct POST + e2e on deployed PWA all verified (0 `window.print` calls). |
| FT-AUTO-ANALYSE-ON-JD-LOAD | JD analysis runs automatically WHEN A JD IS LOADED (not only after the Analysis panel is opened) — the panel fills itself without pressing Analyse JD | `antcv-analysis-panel-jd-block-356.js` headless runner (same `/api/recheck-fit` + `/api/jd-analysis` + rationale-merge as the in-panel Run button, minus UI); fired by scheduler + self-limiting 3s poll + `antcv:jd-loaded`; JD from `antcv:lastJdText` | AUTO-ANALYSE-ON-JD-LOAD-001 `1.51.1768` (2026-07-19). Shares the `AUTORUN_KEY` fingerprint → at most ONE LLM run per JD; never double-spends with the in-panel autorun or a later generation. Also populates `ra.category` early. Additive readable sidecar (no app.js). |
| FT-SALARY-ENVELOPE-CHECK | Job salary checked against the Dream Envelope even when pay is NOT stated (market ESTIMATE fallback), flagged as a conflict below the band | `src/islands/JobTracker/JobTracker.tsx` `enrichAdded` (+ built `pwa/antcv-react-islands.js`): `SALARY:` prompt estimates when unstated (suffixed `(est)`); `CONFLICTS:` compares stated-OR-estimated vs the ~55-75k DKK/month envelope; row label `Salary (est)` vs `(stated)` | SALARY-ENVELOPE-CHECK-001 `1.51.1794` (2026-07-19). Floor left at 55k (bottom of the band already in the calibration prompt; owner call to raise). Islands are Vite-built — source `.tsx` + bundle both edited so a later `npm run build` reproduces it. |
| FT-APP-RECOVER-JDONLY | A saved application is never a dead end or a silent loss — History Switch OPENS a JD-only app (identity + JD loaded, template ready to Generate) instead of refusing it, and an abandoned/failed regen no longer destroys the CV: the wipe snapshots sections before nulling and an explicit reopen self-heals the row | `pwa/app.src.js` (`__hasReal`, both switch handlers) + `workers/access-relay/src/index.js` (`wipe-generated` snapshot, `handleApiApplicationById` restore) + D1 `cv_sections_bak`/`cl_sections_bak` | SWITCH-OPEN-JDONLY-001 `1.51.1708` 35b6120; WIPE-NONDESTRUCTIVE-RESTORE-001 relay 0af16b9 (deployed `27def9ec`). Restore is scoped to the DELIBERATE single-app GET — never the `/api/prefs` active seed, else a mid-regen sync re-seeds the contamination the wipe exists to prevent. Verified e2e on live D1. |
| FT-APP-DEDUP-NORMALIZED | Re-generating the same job UPDATES its application row instead of spawning a duplicate — employer+role matching survives dash/suffix/case variants of the role string | `workers/access-relay/src/index.js` (`dedupeRoleKey` + normalized candidate match in the create handler) | DEDUP-ROLE-NORMALIZE-001 `1.51.1800` 1ca51c9. Replaces an exact `jd_role = ?` match that missed e.g. 3Shape `"… – R&D…"` vs `"…- R&D… - 3Shape"`. One-time cleanup merged 11 legacy duplicates (freshest-with-content kept, active app protected, empty-role placeholders excluded); 0 dup groups remain. |
| FT-CONTENT-LANG-SELECTOR | The top-bar language selector always matches the language the CV is actually written in, not the language of the job ad | `pwa/app.src.js` (both app-load handlers: content-script detection, `jd_language` fallback) | APP-SWITCH-CONTENT-LANG-001 `1.51.1800` 1ca51c9. Entry-point half of the Chinese-stuck family; the CONTENT-heal half is BABEL-EN-ASYMMETRY-001 / BABEL-LATIN-BLIND-001 (`1.51.1818`/`1.51.1838`) — complementary, runs only on load so it cannot oscillate with the heal. Prevention leg (an authoritative stored content-language) = OPEN_REGISTER row 94. |
| FT-JD-LINKEDIN-CARD | Adding a LinkedIn job by URL pre-fills Company and Role in the tracker table again — and picks the REAL employer over the posting agency | `workers/{proxy,demo-proxy}/src/fetch-jd-url.js` (`extractLinkedInCard`) + `src/islands/JobTracker/{api.ts,JobTracker.tsx}` | LINKEDIN-CARD-EXTRACT-001 `1.51.1882` 492ca1e (cv-proxy `105bfd9c`, demo-proxy `c37d0871`). The guest jobPosting fragment has NO `<title>`; role/company come from the top-card, preferring a body-confirmed trailing `"- <Employer>"` over the agency `org-name-link`. Owner-confirmed live ("DTU Wind fetched"). Islands are VITE-built — bump `antcv-react-islands.js?v` or the change will not land. |
| FT-DELETE-CONFIRM-INAPP | Deleting a saved application uses a two-click in-app confirm (🗑 -> "Delete?" -> click again), not a native confirm() dialog — so it works in embedded / installed-PWA browsers | app.src.js delete sites (topbar dropdown + Settings history) + minified app.js mirror; arms via the shared [Hl,Ul] state, 4s auto-disarm | 2026-07-22, 1.51.2146 (dd4730c). DELETE-CONFIRM-INAPP-001. Native confirm() is auto-cancelled in embedded/PWA views (returns false in ~30ms), so the old gate silently no-op'd. |
| FT-PARALLEL-TRANSLATION-KEEP | Viewing an app in another language never overwrites its canonical CV — translations kept in parallel (langRenders), canonical preserved | `antcv-parallel-translation-keep.js` (request-only PUT /api/applications/:id guard; strips cv_sections/cl_sections on a non-primary wide-script flip; primary learned fail-open from the cloud canonical + gen-complete); reuses window.__antcvContentScript; test `pwa/antcv-parallel-translation-keep.test.mjs` | 2026-07-22, 1.51.2106 (20d45f9). PARALLEL-TRANSLATION-KEEP-001. Zero app.js edits — modelled on the proven cloud-put-shrink-guard-289 request-only fetch pattern. |
| FT-CONTENT-LANG-DETECT | App-switch / boot language selector follows the content's REAL script (one shared detector with the babel-fish healer), so residue can't flip an English CV to Chinese | `window.__antcvContentScript` in `antcv-babel-relang.js` (textOf values + isInLanguage prose-ratio); both app.src.js `__cl` sites + minified app.js mirror call it with a residue-proof inline fallback; test `pwa/antcv-babel-relang.contentscript.test.mjs` | 2026-07-22, 1.51.2012 (25a1d65). APP-SWITCH-CONTENT-LANG-DETECT-001. Replaces the single-CJK-char test from APP-SWITCH-CONTENT-LANG-001 (1.51.1794) that flipped every residue-bearing app to zh and fed a translate→persist contamination loop. Selector and healer now share ONE detector, so they can't disagree. |
| FT-STORM-CONVERGE-GUARDS | At-rest `sections` write-storm guards that actually converge — substructure-keyed and sticky, so a parallel writer cannot defeat them | `pwa/antcv-sections-normalize-415.js` (`__complDone` sticky completeness decision + `__recentExpWrites` experience-substructure oscillation key); regression harness `pwa/antcv-sections-normalize-415.storm.test.mjs` (Node shim, fake 2.5s clock, competing remover + parallel churn) | 2026-07-21, 1.51.1902 (408cc5c). ROLES-STORM-CONVERGE-001. Supersedes the two STORM-OSCILLATION-GUARD-001 guards, which could not converge: whole-blob hash (defeated by the zh/English translation-residue writer) + a 6s/3-hit counter (re-armed across the 2.5s poll and reset on its own post-restore state). RED 14/20 -> GREEN <=2. BABEL-RICHBLOCK-RESIDUE-CONVERGE-001 (1.51.1972, 8b2a52a) then GENERALISED the write-side guard from experience-only to any contested section across cv+cl, and re-enabled the rich_block Latin-residue drop (disabled since 2026-07-11 for losing a write war) behind the same sticky pattern — measured: sticky guard neutered = 10/20 passes write, both layers = <=2. |
| FT-PARALLEL-GEN-POINTER | Per-DEVICE cloud active-application pointer — parallel generations across tabs/browsers/devices never clobber each other's draft | new D1 `active_application_device` table + relay `readActivePointer`/`writeActivePointer` (writes BOTH per-device + legacy global) + client `AntcvJdScope.devQ()` sends `?device_id=` on the prefs/active GETs | 2026-07-10, 1.51.259 (corrective bump 1.51.260 after a stale-base merge regressed the quintet to 245/246). `scripts/tests/relay-per-device-pointer.test.mjs` 7/7. RULE: never re-add a raw `INSERT INTO active_application … ON CONFLICT(user_hash)` — use the helpers. The 1.51.365 island device-stamping builds on this. Memory jd-scope-isolation (Stage 4). |
| FT-TAB-DOC-ISOLATION | Per-TAB document snapshot — a parallel SAME-device tab can't clobber this tab's `sections` in shared localStorage | `antcv-tab-doc-isolation.js` (sessionStorage `antcv:tabDocSnap`; boot guard restores this tab's doc) | Shipped 1.51.253 but was DEAD (served, no `<script>` in index.html) until wired 2026-07-11, 1.51.300 (commit 3bb486e) — the gap the new sidecars-live check caught on its first run. Kill `antcv:disable-tab-doc-iso`. |
| FT-WIZARD-NEW-USER | Onboarding wizard now opens for a signed-in NEW user | app.src.js Gate 2 (cloud-restore) fresh-user branch opens the wizard when there is no flag + no REAL personalInfo (name/background) | 2026-07-11, 1.51.322 (commit 3d6997c). Was never opening: Gate 1 skips when authenticated (token/session = "returning"); Gate 2 could only CLOSE. Content heuristic aligned with the WIZARD-AUTOLOAD-001 `__returning` fix; live-verified by gate simulation. |
| FT-SHIFT-LEDGER | Parallel-session coordination — reserve a version-number RANGE + work in a git worktree so sessions never collide on version numbers or shared-tree WIP | `docs/qa/NIGHT_SHIFT.md` (JSONL claims committed to main) + `scripts/shift.mjs` (claim/status/next-version/beat/release/reap) | 2026-07-11. Range from the true git high-water mark (robust to a regressed TARGET). SHIFT PROTOCOL wired into both MULTIRUN work orders + CLOUD_ROUTINE_PROMPT. |
| FT-LIVE-VERIFY | Post-deploy verification of the DEPLOYED PWA | manual in-app Browser pane (`docs/qa/LIVE_VERIFY_BROWSER_PANE.md`) + automated `node scripts/browser-qa.mjs --only version-live` / `--only sidecars-live` (checks in `scripts/qa-checks.mjs`) | 2026-07-11. version-live catches a live version BELOW the deployed build (the 245/246 class); sidecars-live probes feature globals on the live bundle (caught the dead tab-doc-isolation sidecar). |
| FT-SCHEDULED-ROUTINES | Every recurring routine (position-discovery, the nightlies, weekly demand-seed, security audit, relay-health, model-freshness) framed under NIGHT SHIFT — sync + claim a version range before pushing | `docs/qa/SCHEDULED_ROUTINES.md` + CLAUDE.md § Sync discipline | 2026-07-13. Same-day shift-hardening: `shift.mjs` reads the ledger from origin (WIP-proof — a dirty tree can't cause a stale read / double-claim); pre-push soft-reminds a versioned push with no active claim; NIGHT SHIFT pointer added to 5 standing prompts; worktree-safe IDFILE. Same-day (owner ask): standing rule (5) END-OF-RUN REGISTER REPORTING — every agent-driven routine writes its outcome to OPEN_REGISTER + ACTIVE_BUGS + FEATURES_REGISTRY before ending (wired into the 3 scheduled-task prompts + CLOUD_ROUTINE_PROMPT.md); non-agent CI/cron findings transcribed by the next nightly sweep. |
| FT-RELAY-COST-QUALITY-TUNE | Weekly review + MODIFY of the LLM cost-quality router so it improves over time (per-role provider head chosen from the week's real telemetry, not just scored) | routine RELAY-COST-QUALITY-TUNE-001 in `docs/qa/SCHEDULED_ROUTINES.md`; lever = `MODEL_ROLES` / `roleHeadOrder()` (`workers/proxy/src/multi-llm.js`); data = `llm_provider_health` + `llm_quality_signals` | 2026-07-13. Procedure + guardrails defined (bounded per-role flip, adequacy floor, keep the fallback cascade, dated before→after + rollback). Script `scripts/relay-cost-quality-tune.mjs` reads `MODEL_ROLES` + the `/api/llm-health` snapshot and EMITS the proposed diff (pure core unit-tested 8/8: cheaper-wins, hysteresis, floor/min-sample/known-provider/no-data guardrails); apply (`--apply`) + deploy stay agent/owner-gated. **First LIVE weekly run 2026-07-13** (trigger `antcv-relay-cost-quality-tune`, Wed 22:00): executed against D1 `ant_memory` 7-day aggregate (3,698 calls) — NO FLIP (all 3 heads held; script proposed no change, suite 8/8). Surfaced RELAY-TUNE-COVERAGE-GAP-001 (OPEN_REGISTER row 38): the scorer's `ROLE_TASKS` matches 0 real-traffic rows, so the loop currently can't move any head; biggest cost lever (`compress`, ~58% of weekly spend) is untunable via `MODEL_ROLES`. Closing that gap is owner-gated. Same-day (owner ask "make the tune routine execute the needed modifications"): procedure gained **step 1a MODEL-TABLE FRESHNESS AUDIT** — the tune scores on `cost_per_call`, so before scoring it must verify AntCV's current model pins (opus-4-8 / gpt-5.4-mini / gpt-5.5 / sonnet-5) are priced+present in both proxies' `demo-enforcement.js` RATES + `multi-llm.js` cascade and FIX+extend `model-table-freshness.test.mjs` if a pin drifted. Seeded by MODEL-TABLE-FRESHNESS-001 (ACTIVE_BUGS 2026-07-13, OPEN_REGISTER row 89): the tables had gone stale on opus-4-8 (3× over-priced via the legacy `claude-opus-4` substring) + gpt-5.5 (~24× under-priced via `gpt-5`); fixed same day in all 4 worker files with a mirrored regression test, deploy owed. **Weekly run 2026-07-15 — FIRST REAL HEAD-FLIP SHIPPED (the coverage gap is now closed enough that the loop modifies the function):** scored the last-7-day D1 `llm_provider_health` aggregate (via Cloudflare MCP d1_database_query; sandbox shell can't reach the relay admin path). Step-1a freshness audit green (5/5 ×2, no drift). Flipped `coherence` head `anthropic→openai` — `apply_correction` ran 100% on openai (n=1759 @100% ok, health 1.0), anthropic head produced ZERO → pin matches the working provider, anthropic stays fallback-tail; shipped to both wrangler.toml + deployed proxy + demo-proxy, `/health` verified. `analysis→gemini` HELD (task-conflation: analyze_fit is openai@1.0 with zero gemini truth; parse_jd gemini is 30%-retry/0.69-health) — surfaced owner-gated (top up gemini analyze_fit truth OR split analysis per-task). gen/flagship untouched (owner-gated). Suites: freshness 5/5 ×2, tune 14/14. Report COST_QUALITY_WEEKLY_2026-07-15.md; ACTIVE_BUGS RELAY-COHERENCE-HEAD-OPENAI-001; OPEN_REGISTER row 38 advanced. **Weekly run 2026-07-22 — NO FLIP + a routine self-fix.** Step-1a freshness audit green (5/5 ×2, all pins fresh, no table change). Read `llm_calls` directly (raw per-call) rather than the `llm_provider_health` window aggregate, which OVERLAPS and multi-counts (prior weeks' "26k calls / $8.6k/wk" were phantom; true = **434 calls / ~$6.79 spend over 7d** — a quiet week). Every tunable role thin/absent (writer/supervisor/coherence/kernel = 0 addressable rows; analysis/parse_jd = 14 mistral calls < 20 min-calls) → machine proposal NO CHANGE, guardrails held. Surfaced: the residual compress cost outlier shifted openai→**claude** ($2.39/wk, 56 calls still on claude despite the RELAY-COST-TIEBREAK-001 demotion — owner watch). Self-fix shipped RELAY-TUNE-NAN-FALLBACK-001 (OPEN_REGISTER row 39): `scoreRows` `Number(r.health_score) != null` never fell back to `success_rate` (NaN != null is true) → NaN-poisoned quality on the offline D1 snapshot path the routine itself uses; guarded the raw field + added a test, suite 14→15. No deploy, no version consumed. Report COST_QUALITY_WEEKLY_2026-07-22.md. **Weekly run 2026-07-29 — NO FLIP; phantom-window trap caught live.** Step-1a freshness audit green (5/5 ×2, all 4 pins fresh, no table change); latent gap registered (gemini-3-flash-preview / gemini-3.1-pro-preview in the app pickers are unpriced in RATES, fall through to FALLBACK_RATE [3,15] if dispatched — not active, real gemini traffic uses gemini-2.5-flash). Summing `llm_provider_health` made the scorer propose a FLIP `analysis→openai` (parse_jd read as n=1253, clearing the floor); raw `llm_calls` shows parse_jd n=12 < 20 → analysis ineligible → NO CHANGE (honest week: ~761 calls / ~$9.8 spend). Concrete proof the scorer must read `llm_calls`, not sum the overlapping health windows. compress claude leg $5.41/wk (~55% of spend) persists — compress fans out to all 4 providers ~equally (ensemble shape), owner call whether the claude leg is needed; parse_jd mistral avg latency 235s flagged. Suites: freshness 5/5 ×2, tune 15/15. Docs-only, no deploy, no version consumed. Report COST_QUALITY_WEEKLY_2026-07-29.md; OPEN_REGISTER row 38 advanced. **Weekly run 2026-08-06 — NO FLIP (very quiet week).** Step-1a freshness audit green (5/5 ×2, all 4 pins fresh + still pinned in app.src.js, no table change); gemini-3-preview unpriced gap UNCHANGED (still latent, not dispatched). Telemetry via `/api/llm-health?window=10080` (single 7-day window, no cross-window sum → phantom-count trap avoided) authorized by the owner PWA JWT — the endpoint is any-signed-in-user, not admin-only, so no admin token needed; raw `llm_calls` not independently cross-checked (no D1 MCP tool in this session). Genuinely quiet week: **~22 calls / ~$1.15 spend over 7d**; every addressable role far below min-calls=20 (analysis best=openai n=5; coherence=apply_correction openai n=2; writer/supervisor/kernel=0 rows) → machine proposal NO CHANGE, guardrails held, `MODEL_ROLES` unchanged. No per-call cost outlier worth an owner call (compress ensemble barely ran; two parse_jd rows priced high per-call — claude $0.739 n=1, mistral $0.143 n=2 — but n<floor, surfaced only). Suites: freshness 5/5 ×2, tune 15/15. Docs-only, no deploy, no version consumed. Report COST_QUALITY_WEEKLY_2026-08-06.md; OPEN_REGISTER row 38 advanced. INCREMENT (weekly run 2026-08-20, Opus 5, worktree-isolated — preflight WORKSPACE DIRTY): telemetry read from **raw `llm_calls` via the D1 MCP** (`ant_memory`), not `/api/llm-health` — no window-overlap multi-count possible. Real volume returned: **499 calls / 7d, 100% success, 0 retries, 0 quality flags**, but **467 are `compress`** (client-dispatched, not `MODEL_ROLES`-addressable) and every addressable role sits at n=4–5 < min-calls 20 → machine proposal **NO CHANGE**, guardrails held, `MODEL_ROLES` unchanged, no deploy, no version consumed. Costs were **recomputed from raw tokens** rather than trusted: logged $6.6573 vs **true $2.9542 (−55.7%)** — claude 3.04x (LLM-COST-CLAUDE-RATE-001, fixed 08-19, this traffic predates it), mistral 1.50x (**LLM-COST-MISTRAL-RATE-001, new, still live**), gemini 0.75x (**LLM-COST-GEMINI-RECONCILE-001 mechanism found**: a stale D1 `llm_provider_costs` row overrides the PWA map). **Loop improved:** shipped TUNE-BLOCKER-LABEL-001 — the no-flip rationale had reported the adequacy floor for four weeks when the real blocker was the sample floor; suite 15/15 → **17/17**. **Loop gap found:** COST-SOURCE-AUDIT-GAP-001 — step 1a audits the demo-cap `RATES` table, which is not the table that produces `llm_calls.estimated_cost_usd`. Suites: freshness 5/5 ×2, tune 17/17. Report COST_QUALITY_WEEKLY_2026-08-20.md; OPEN_REGISTER row 38 advanced. |
| FT-JOB-DISCOVERY | Bi-weekly (Sun+Tue 22:00) job-DISCOVERY: finds NEW openings vs the Dream Envelope, dedups, adds PROPOSED rows for review — propose-only, never auto-generates/applies | scheduled task `antcv-position-discovery` + `scripts/job-tracker/discover-positions.py` | 2026-07-13. `queue[uk]=False` on every proposed row (INELIGIBLE for the gen nightly until the owner flips ⏰); a `discovered` ledger blocks re-proposal. Spec/memory: position-discovery-task. |
| FT-JOBTRACKER-FIT-TOP5 | Fit-ranked Top-5 (deterministic fitScore, re-evaluated on every add) + Pin/Park/Reject + auto-tier manual adds + hybrid auto-fill (deterministic baseline + async LLM refine: tier/location/salary/deadline/hiring-manager/next-step/conflicts/brand) | `src/islands/JobTracker/rank.ts` + `JobTracker.tsx` (1.51.388) | 2026-07-13, commit 82d6eaa. rank.ts unit-tested 9/9; live-verified non-destructively. Spec `docs/qa/JOBTRACKER_AUTOFILL_TOP5_SPEC.md`. Reject feeds the discovery ledger. |
| FT-JOBTRACKER-PROPOSED-INBOX | "Proposed Inbox" Excel worksheet (tier-sorted discovered leads, posting links, first-seen) added on `sync pull --render` | `scripts/job-tracker/job-tracker-sync.py` | 2026-07-13, commit 3c67eb3. Read-only view (import-xlsx reads only Weekly Tracker). Renders on a Drive-connected host. |
| FT-JOBTRACKER-SIGNAL-MATERIALS | Signals column 📎 — attach signal MATERIALS (PDF / text / image-scan) per row, extracted via the app's PDF.js → LLM → OCR chain, stored as text-only `doc.sigfiles` chips (removable, 6k cap/file). Typed signals + materials compose into ONE effective block fed to: Open's ADDITIONAL SIGNALS in supporting_context (→ upload-panel additional info + in-app JD analysis), Top-5 (fit %, 📌 card line, ✨ refine, tracker Ask-AI), and the nightly gen-runner (`_signals_for`, prompt cap 2500→8000) | `src/islands/JobTracker/JobTracker.tsx`+`api.ts` (island 1.51.364) + `scripts/job-tracker/gen-runner.py` | 2026-07-12, commit a681e82. Blank lines in attached text collapsed so app.js's ADDITIONAL-SIGNALS block regex never truncates (invariant in memory signal-materials). Suite 1222/1222; live-verified. Companion fixes 1.51.365 (commit 7348d72): device-stamped Open pointer, brand-fit checkbox arming, delegated 📋 button — OPEN_REGISTER 2026-07-12 CLOSED block + rows 78-80. |
| FT-JOBTRACKER-CUT-INTEGRITY | Cut integrity for hard-capped generated lines — a cap landing inside a parenthetical no longer ships an unbalanced bracket around an amputated number (`"(a 10x."`); the cutter drops any parenthetical it could not complete and re-walks the dangling connectors that exposes. Plus transport resilience in the headless runner: a socket timeout on `/job/step` becomes a retried synthetic 599 instead of a traceback that kills the run and strands a resumable gen-job | `scripts/job-tracker/gen-runner.py` `_clean_cut` / `_req` / `drive`; test `scripts/job-tracker/test_clean_cut.py` | 2026-08-18, job-tracker nightly. Found from a REAL shipped artifact (CIP app 3488) and live-verified by re-generating it clean as app 3489 (0 unbalanced/0 amputated across cv+cl). Test is RED on the pre-fix tree. Python-only, no PWA asset, no cache-bust. Follow-up CAP-AMPUTATED-ENUMERATION-002 is OPEN in OPEN_REGISTER. |
| FT-JOBTRACKER-QUEUED-FILTER | "⏰ Queued" checkbox in the Job-Tracker List legend/filter — narrows the list to rows queued for tonight's generation (`doc.queue[uk]`), the same state the ⏰ per-row toggle sets and the nightly runs consume. Sits alongside the tier-band / ★ Top-5 / ✅ JD-stored filters; sessionStorage-persisted (`antcv:jobtracker:legendFilter`). Filter + ⏰ toggle share one `rowQueued(doc,uk)` predicate (explicit `queue[uk]` wins, else default-ON until an artifact exists) so they always agree | `src/islands/JobTracker/JobTracker.tsx` (built into `pwa/antcv-react-islands.js`) | 2026-07-22, PWA `1.51.3081-queued-filter` (JD-MENU-QUEUED-TAB-001). Read-only UI add, no doc-schema change → nightly writers unaffected; awareness note in `docs/qa/SCHEDULED_ROUTINES.md`. Guard test `pwa/antcv-jobtracker-queued-filter.test.mjs`; suite 1449 green. |
| FT-JOBTRACKER-PROVIDER-FALLBACK | Headless gen-runner survives a single provider outage: a billing/quota signature (`credit balance is too low`, `insufficient_quota`, …) retires THAT provider for the run and re-drives the row on the next model in the tier's chain (`MODEL_CHAIN_HIGH` anthropic→openai→gemini→mistral, `MODEL_CHAIN_QUICK` openai→gemini→mistral; provider-deduped, the tier's configured model always leads so `ANTCV_MODEL_HIGH/QUICK` overrides still win). The batch aborts only when EVERY provider in the chain is exhausted, and says which. Plus WHO-LANG-001: the WHO I AM block's displayed labels now follow the letter's language (`_WHO_FURNITURE`/`_wlabel`, mirroring `_flabel`) instead of always rendering the English canonical keys | `scripts/job-tracker/gen-runner.py`; test `scripts/job-tracker/test_provider_fallback.py` | 2026-08-20, job-tracker nightly. Found live: the run aborted its whole queue on an Anthropic credit-exhaustion 400 while openai/gemini/mistral were all healthy on the same proxy (each chain entry probed live that night). Test drives the REAL `cmd_run` loop network-free — one dead provider → both rows generate on the fallback and row 2 starts on the survivor; all four dead → abort after one pass. Python-only, no PWA asset, no cache-bust. Proved end-to-end the same night: KOMBIT row generated on `gpt-5` and persisted as app 3490. |
| FT-ROLE-CANON-LANG | Golden gating matrix ROLE CONTROL: stable canonical role id = language-agnostic identity (one visible role per id; twins/gen-ids collapse onto it) + canonical role titles in en/da/es/zh applied deterministically per ribbon (LLM title drift can't fork a position across languages) | `pwa/gold-rules.json roles` (control site) + `antcv-sections-normalize-415.js` roleCanonTitles (1.51.394) + `pwa/test/unit/gold-role-canon.test.mjs` (mirror/pin/typography gate) | 2026-07-13. Owner: "I want also danish spanish and chenese canon". he/am/ar keep translate output by design. HARDENED same day: 1.51.396 (containment audit — canon id "earlier-career" matches gen-runner; 290 guard floor aligned; _EC_TITLE +es/zh) + 1.51.397 (owner rulings — core-comp 3-4 TABLE rows / <=2 cell lines, "EU citizen" canonical, role-bullets generation cap in the gold prompt block; row 84 closed). Follow-up register row 82 (gold_audit export leg + owner eyeball of es/zh wordings). |
| FT-GOLD-DENSITY-SYSTEM | Gold-standard generation control: `pwa/gold-rules.json` = the single machine-readable control site (density thresholds, caps, results/certs/pubs/label rules, banned words, GOLD CONTENT RULES prompt block) consumed by EVERY generation (antcv-bullet-targets fetch + byte-equal fallback, drift-guarded by gold-rules-drift.test.mjs) AND by the persist pipeline: measure_density (line metrics + geometric table-cell scan), density_fit (measure->target->rewrite loop, multi-LLM-family gated, tableRatio ladder), quality_pass (PERSIST-QUALITY rules incl. RESULTS-OUTCOME-METRIC v2 id-exact kernel swaps, PUB-CANONICAL-ORDER, NARROW-CELL-CASCADE label levers, CL prose health + connector-trim fallback), export_pdfs + gold_audit (per-file locker audit) | scripts/job-tracker/* + pwa/gold-rules.json + pwa/antcv-bullet-targets.js + docs/qa/GOLD_STANDARD_LOCKER.md | ✅ 2026-07-13 (1.51.375-389 + a7cbdfe). Locker doc wins over code. FINAL AUDIT GREEN 20/20 apps (see ACTIVE_BUGS closeout entry); open residue = OPEN_REGISTER row 86. |
| FT-PHOTO-LIBRARY | Profile-photo LIBRARY (max 4 saved photos, thumbnail strip in the Profile Photo panel; click a thumbnail to activate it through the app's OWN upload pipeline, ✕ to remove; cloud-synced as prefs field `photoLibrary`) + the owner's 2026-07-13 button fuse: ONE upload control "＋ Add photos…" (multi-select; each image compressed under the 500KB cloud entry cap — 600px downscale + JPEG re-encode — and saved to the library; adding IS saving; first becomes active; oldest evict when full), native single-file "Change photo" hidden, "☆ Save current" removed; strip layout (1.51.393): no "Library:" label, strip-owned "↺ Reset" UNDER Add photos in the same teal-outline style — drives the hidden native Reset (default ant restored via the app's own path) + empties the library | `antcv-photo-library.js` (library 1.51.382, fuse 1.51.390/391, strip 1.51.393) | 2026-07-13. PHOTO-LIBRARY-001 (1.51.382, retro-registered) + PHOTO-BTN-FUSE-001 + PHOTO-RESET-CLEAR-001 + PHOTO-STRIP-RESET-001. The strip's extra buttons triggered PW-CJLR-PHOTO-LEAK-002 (CJLR cycler stuck over the photo card), fixed same push — OPEN_REGISTER 2026-07-13 CLOSED block; owner visual verify = row 81. Kill `antcv:disable-photo-library`. |
| FT-DATA-EXPORT | Download all stored data + analytics to a file (optional WebCrypto AES passphrase) | `antcv-data-export-360.js` | Button in the PRIVACY zone; "Save my data locally first" checkbox in the erase card. PR #176/#181/#185. |
| FT-DELETE-SAVE | Back up before account erase | same sidecar wraps `AntcvFullErase` | PR #176. |
| FT-CONSOLE-QUIET | Central console quieter (suppress ~70 boot/status banners; `localStorage.antcvVerboseConsole=1` to restore) | `antcv-console-quiet.js` | PR #188. |
| FT-SIDEBAR-COLOR | Preview sidebar follows the visual style (`var(--package-base)`, matches DOCX) | `antcv-sidebar-bg-token.js` | PR #210. |
| FT-LOADING-GATE | "Loading…" cover on login for returning users (masks the wizard flash + palette/tone settle) + orphan tone migration | `antcv-login-loading-gate.js` | PR #220. |
| FT-PHOTO-POS | Photo placements in preview (sidebar-top/bottom, header L/R, main L/R, band-overlap) | `antcv-photo-position.js` + app.js | Finders repaired for the single-table renderer (PR #196); the full position family + persistence closed 1.50.368–373 (see PHOTO-SIDEBAR-BRIDGE-001). |
| FT-DOCX-PHOTO | DOCX photo placement incl. `band-overlap` → top-of-sidebar | docx-worker `generate.js` | PR #200 (worker 1.14.17). |
| FT-VISUAL-PACKAGES | 7 visual style packages (Copenhagen Modern, Navy Executive, Warm Terracotta, Nordic Frost, Pampas Contemporary, Tokyo Precision, Delhi Technical) | registry + islands + app.js | Palette re-derives render colours on load (PR #226), but the default-palette "black mix" bug **PACKAGE-PALETTE-MIX-001 is still OPEN** — persisted id stays orphan `scandinavian`. Close-out = APPJS-ID-SCHEME-UNIFY. |
| FT-WRITING-STYLES | 12 writing systems (nordic-minimal … hybrid-balanced) + banned lists + tone chips | `writingSystems/registry.json`, proxy worker | Pre-existing; tone orphan default migrated this session. |
| FT-EXPORT | DOCX + PDF export (cv-proxy / docx-worker / CloudConvert) | workers | Pre-existing. |
| FT-ANALYSIS-REPORT | Branded AI-watermarked JD-analysis PDF | `antcv-analysis-report-pdf-360.js` | Landed in parallel work (#219 family). |
| FT-DATA-IMPORT | **Restore** a downloaded backup (counterpart to FT-DATA-EXPORT) — "⬆ Restore from backup" button beside "⬇ Download my data"; reads the backup file, decrypts (PBKDF2/AES-GCM) if it was passphrase-encrypted, confirms the overwrite, restores localStorage, reloads | `antcv-data-import-331.js` (1.50.331) + `dataRaw` added to `antcv-data-export-360.js` | DATA-IMPORT-001. Lossless via the new `dataRaw` map (exact localStorage strings — fixes the `JSON.parse` round-trip ambiguity, e.g. `doc:"cv"`); legacy plain backups fall back to `JSON.stringify(data[key])`. Round-trip verified headless (`pwa/test/diag-data-import.mjs`): plain + encrypted restore byte-for-byte, secrets excluded from plain backups, wrong passphrase rejected. **CONSOLIDATED (1.50.333):** the standalone Settings "Restore" button was removed; `antcv-data-import-331.js` is now a pure **library** (`window.AntcvDataImport` + `AntcvIsBackupEnvelope`). The single import entry point is the floating 📥 `antcv-data-importer.js` — its `handleJSON` detects an AntcvBackup envelope and delegates to the library for a decrypt-if-needed, confirm, lossless full restore + reload (all other dropped files keep the merge/diff flow). One import path. |
| FT-FOUNDATION-CONTROLS | Per-subsection controls (manual page break, CJLR alignment, Enhance, Fix/Fit) for BOTH FOUNDATION sub-parts (Hands-on + Professionally) | `antcv-foundation-controls-327.js` (1.50.327) | Owner spec 2026-06-09. Stores align/page in `antcv.foundationControls.v1` (worker `renderFoundation` + docx-client already honour it → preview AND export). Page break mirrors into `antcv:itemPages[foundation][0/1]` for the preview salmon. Enhance/Fix are local deterministic transforms (same lightweight pattern as the HOW-I-WOULD-CONTRIBUTE per-part controls, 245), not LLM. Preview alignment + page-break + transforms verified headless (`pwa/test/diag-foundation-controls.mjs`); panel-button placement (attaches when the foundation card is open) is owner-visual-verify. |
| FT-DEBUG-LOGGER | Crash-proof in-app error logger + on-device viewer (for mobile-only blue screens with no devtools). Captures uncaught errors + breadcrumbs to localStorage synchronously (survives crash+reload); plain-DOM overlay opens via `#antcv-debug`, a 4-tap top-right gesture, a Settings button, or `window.AntcvDebug.open()`. Copy/Share/Download/Clear. | `antcv-debug-logger.js` (v1.50.167) | Loads first; does NOT touch `window.fetch`. Regression-guarded by the `debug-logger` browser-QA check. Built to diagnose PERSONAL-DATA-CRASH-001. |
| FT-GEN-CYCLE-OPT | **Generation-cycle optimization** — cut the multi-call generate run + reduce sidecar tweaking. (1) per-mode provider fan-out width (`__fanWidth()`): quick=2/regular=3/thorough=4, quick-gen=3; (2) recruiter web-search auto-backfilled into NON-quick generations (skipped in quick); (3) the "Compress column"/"Make it fit" loop runs sections with bounded concurrency (cap 2); (4) a cost-quality-latency provider scorer wires the previously-vestigial `L[task]{qW,lW,cW}` weights into routing. | `app.src.js`+`app.js`, `antcv-analysis-merge-344.js`, `pwa/test/unit/llm-scorer.test.mjs` | ✅ CLOSED — 819 GEN-WIDTH-001, 820 RECRUITER-FOLD-001, 821 FIT-PARALLEL-001, 823 LLM-SCORER-001 + 829 LLM-SCORER-TUNE-001. Scorer: reorders only (never drops), pure-default path only, kill switch `antcv:disable-llm-scorer`; live-verified via `window.__antcvLlmScoreOrder` + unit test. Most of the original 5-lever plan was already shipped (char caps / canonical vocab / rationale fields / parallel consensus / model tiers); lever 5a (redundant analysis cycle) didn't exist. Detail in `docs/qa/SESSION_LOG_2026-06-23.md` + `docs/qa/ACTIVE_BUGS.md` (top). |

## OPEN — planned / in progress

| ID | Feature | Status | Notes |
|----|---------|--------|-------|
| FT-HEADER-APP-LINE | **DONE / NATIVE 1.51.2741-cl-appline-native** — Per-app **"Application for [Role] at [Company]" line under the slogan** on the COVER LETTER only (CV has none); the CL heading now shows the **specialisation** (not "Application: [role]"). Renders NATIVELY on preview + HTML/PDF + DOCX via `window.__antcvAppLineText(io)` — full EXPORT PARITY. | `app.src.js`/`app.js` CL render (`data-antcv-cl-flow` branch + `buildHTMLDoc`) + `antcv-docx-client.js` payload subtitle→spec + `workers/docx-worker/src/index.js` app-line paragraph | HEADER-APP-LINE-001 + CL-APP-SUBTITLE-HEADING-SWAP-001 (owner 2026-07-22). Preview sidecar `antcv-application-line-001.js` RETIRED to a no-op (legacy-node sweep) so the CL never double-renders. `cl_slogan` gen = sharp candidate×role observation. `role-founder-export` test updated. Suite 1431 green; worker deployed. |
| FT-PERSTYLE-KERNELS | **Per-writing-style unsolicited kernels** — keep a separate unsolicited (quick-generation) kernel for EACH writing style (Nordic Minimal, Research Formal, …), never overwriting another style's; manage them from Application History (load / copy-to-chosen-style / delete); auto-load the current style's kernel on style switch. | 🟡 Phase A+B CLOSED (access-relay auth-26 deployed + 1.50.619); **Phase C OPEN** | Owner-approved 2026-06-18 (Substrate B / copy-to-chosen / auto-load). **A (worker):** `/api/kernel-showcase?style=<slug>` → new composite-keyed table `kernel_showcase_styled` (PK user_hash,style_key) + `?list=1` + DELETE; empty `?style` keeps the legacy single slot byte-for-byte. **B (app):** `oo.getShowcase(style)/putShowcase(e,style)`+`listShowcases()`+`deleteShowcase()`; save sites (~25162/~15190) stamp `personalInfo.stylePrefs.style`; restore (~15364) loads the current style's slot → styles no longer overwrite each other; boot+312/312 green. **C (TODO):** auto-load on style switch + App-History selector (list / load-to-preview / copy-to-CHOSEN-style / delete) — new stateful panel in app.src.js + app.js mirror, reuse the Switch load path (~37334). Map in the 2026-06-18 handoff. |
| FEATURE-CONF-001 | Per-sentence **confidence overlay** (Application-tab toggle; red=low/yellow=medium; hover shows the issue) | ✅ v1 CLOSED 1.50.386 — deterministic grounding heuristic vs the user own source facts; Analysis-panel toggle, red/yellow block tints, per-sentence issues on hover; pluggable AntcvConfidence.setScores for the future worker LLM self-check pass. Test diag-confidence-overlay 5/5. Original spec: NOT in the locked docs — needs a worker self-check pass returning `{text,confidence,issue}` + app.js toggle/store + a preview renderer. Spec in `docs/plan/Batch_2026-06-04_feature-and-bug-triage.md`. |
| SHARE-TARGET-JD-URL-001 | **Share/Open-with a job link into AntCV.** On Android, when the user shares (or "open with") a job URL, the installed AntCV PWA appears in the chooser. When picked, if the user is **logged in**, AntCV opens with that URL pre-filled into the **JD URL** field (the "OR PASTE A JD URL" input) and is ready to Fetch JD; if not logged in, route to login first, then drop the URL into the JD field after auth. | ✅ CLOSED 1.50.375 — manifest `share_target` + `antcv-share-target-jd-375.js` (URL extraction, own-origin guard, sessionStorage stash across login, synchronous step→upload route, native-setter fill). Verified headless `pwa/test/diag-share-target-jd.mjs` 3/3. Owner device verify owed (installed-PWA share sheet). Original build spec retained: | Owner spec 2026-06-09. **Current state:** `pwa/manifest.json` has **no `share_target`** (so AntCV does not appear in the Android share sheet today). The app already (a) reads `window.location.search` via `URLSearchParams` for control params (app.src.js ~L11930), (b) has the JD-URL fetch path (the `Jt({...method:"url-fetch"...})` flow + `/api/fetch-jd-url`, app.src.js ~L12293), and (c) exposes login state via `window.AntcvAuth.isSignedIn()`. **Build:** (1) add a `share_target` to the manifest — GET form is simplest: `"share_target":{"action":"./","method":"GET","params":{"url":"shared_url","text":"shared_text","title":"shared_title"}}` (Android shares links mostly as `text`; capture both `url` and `text` and pick the first http(s) token). (2) Optional `web+`-style or http `url_handlers`/scope handling if we also want "open link with AntCV" (link-capturing) — needs `display_override` + origin association (`assetlinks`-style) and is heavier than share. (3) A small sidecar reads `shared_url`/`shared_text` on boot, extracts the URL, and: if `AntcvAuth.isSignedIn()` → set the JD-URL input value + focus/scroll (reuse the existing JD-URL setter path so Fetch JD is one tap); else stash the URL (e.g. `sessionStorage`) and re-apply after the login gate completes. (4) Strip the param from the address bar via `history.replaceState` after consuming (same pattern as the existing reset/logout handling). **Notes/caveats:** share_target only fires for an **installed** PWA; iOS Safari share-target is unsupported (Android/Chromebook + Chromium desktop only). Validate the captured value is an http(s) URL before filling. Verify: install PWA → share a LinkedIn job link from the browser/job app → AntCV appears → pick it → (logged in) URL lands in JD field. Bug tracker / spec: SHARE-TARGET-JD-URL-001. |
| PHOTO-SIDEBAR-BRIDGE-001 | True **sidebar-bridge** photo: split the candidate-header cell so the disc hovers on the header/sidebar seam (preview + DOCX) | ✅ CLOSED 1.50.368–372 + worker 1.14.51–53 | Shipped as `band-overlap` (split header row, floating medallion, 1.3× native size, equal air, contact squeeze) PLUS a full position family: main top L/R with circular crescent wrap, main bottom L/R, bridge-middle/bottom on the vertical seam with dual crescents — preview AND export. The 1.50.373 client fix (EXPORT-PHOTO-POS-CLAMP-001) was the missing export half: `readPhotoPosition` clamped `band-overlap` to sidebar-top before the payload left the browser. Tests: diag-photo-positions (12/12), diag-photo-bridge, diag-photo-positions-export (9/9), photo-position-forward unit (5/5). |
| BRIDGE-SIDEBAR-PALETTE-001 | **Bridge-mode sidebar tint** — when the photo is in bridge mode (`photoPosition === 'band-overlap'`, the `◐ Sidebar bridge` button), the preview sidebar bg shifts slightly BRIGHTER and the headings/text/lines slightly DARKER, so the bridging medallion reads against a subtly distinct sidebar. | ⚪ RETIRED 1.50.371 (owner direction) | Shipped 1.50.352, then REMOVED at the owner's 2026-06-11 round-3 feedback ("the font and color of sidebridge need to fit the other positions") — bridge mode uses the standard sidebar palette now. `__antcvShadeHex` stays in app.src.js (unused by bridge); do not re-enable without a new owner ask. |
| APPJS-ID-SCHEME-UNIFY | Unify app.js's package id scheme with the registry (`scandinavian→copenhagen-modern`, `copenhagen_executive→navy-executive`, …) + persist the selection through cloud-restore | ✅ CLOSED 1.50.387 — self-migrating stylePackage init + normalized cloud-restore (both sites), va/xa re-keyed to registry ids, default copenhagen-modern; closes the PACKAGE-PALETTE-MIX-001 root. Test diag-pkg-id-unify 3/3. The orphan-apply / loading-gate workaround sidecars stay as no-ops until an owner-confirmed clean cycle, then retire. |
| ~~DATA-IMPORT-001~~ | **Restore** a downloaded backup file | ✅ CLOSED 1.50.331 | Shipped — see FT-DATA-IMPORT in the CLOSED table. |
| PROCESSING-QUEUE-INDICATOR-001 | Per-subsection live work-state badge: pink **"processing"** while being worked (language change, new JD/kernel, compress, enhance), yellow **"queue"** when scheduled later in the same command (enhance-over-subsection → first pink, rest yellow). Plus: CJLR (Center/Justify/Left/Right) buttons working in **every** sub-subsection. | ✅ badge half ALREADY IMPLEMENTED (verified in source 2026-06-12) · 🟡 CJLR-everywhere remains | The registry note "no per-subsection lifecycle state exists" is STALE: `Ce` renders exactly this badge (app.src.js ~3835–3905 — yellow `pending` with "pending…", pink active states `working`/`translating`/`debating` with a spinner, tinted card background + colored left border), and the state is written per section: bulk Compress/Enrich mark every target `pending` then flip each to `working` as processed (~16491/16570/16595/17183/17364); generate/language paths mark all sids `pending` (~14864) and use `translating`. Owner: run any Enhance/Compress over a column to see first-pink-rest-yellow. CJLR-in-every-sub-subsection is the remaining slice — and it is NARROWER than this row suggested (gap analysis 2026-06-12): per-item CJLR ALREADY EXISTS for labeled_list / education / list via `antcv-item-align.js` (loaded, v1.40.178 — editor-row cyclers writing `antcvItemAlignment[sid]["items.{i}.…"]`, preview applied on `data-antcv-row-path` blocks), per-part for FOUNDATION + HWIC via their control sidecars, and section-default via `antcv-section-align.js`. The WORKER already honours every path generically (`paraAlignPath`: content / intro / items.N(+.t) / closing / hands_on / professionally / roles.N(.title/.bullets.N)) and the docx-client merges `antcvItemAlignment` into `item_alignment`. **(1) EXPERIENCE — SHIPPED 1.50.381 (CJLR-EXPERIENCE-001):** per-ROLE cycler on each role card (`data-antcv-item-row="experience"` → path `roles.{i}`), preview bullet rows marked `roles.N.bullets.M` + `data-antcv-role-path`, lookup chain per-bullet > marker > role > section default; export was already wired. Locked by `diag-cjlr-experience.mjs` (4/4). **(2) table rows — SHIPPED 1.50.383 + worker 1.14.58 (CJLR-TABLE-001), with a twist:** the preview/editor half ALREADY EXISTED — `antcv-core-competencies-row-controls-234.js` owns per-row table CJLR (storage `antcv.coreCompetencies.rowAlignment.v1` = `{"row-<i>": align}`, full-rows index, header at 0; its sweep stomps any other writer, so the generic item-align path deliberately stays out). The missing half was EXPORT parity: the storage was never forwarded. Now `buildPayload` merges explicit `row-N` entries into the table section's `item_alignment["rows.N"]` and the worker's `renderCompetencyTable` applies them to the EXPERTISE cell. Locked by `table-align-forward.test.mjs` (4/4), `diag-cjlr-table.mjs` (preview), `diag-cjlr-table-export.mjs` (DOCX jc). **CJLR-everywhere is now CLOSED — every sub-subsection type (lists, education, foundation, HWIC, experience roles/bullets, table rows) has working CJLR in preview AND export.** |
| AUTO-PAGEBREAK-BLOCK-001 | Automated page breaks: **always** show the salmon splitter when content exceeds one A4 page in preview; sliding is **block-level** — a whole sub-subsection moves to the next page (never partial, never the whole parent subsection). | 🟡 1.50.266 — hidden-group split + salmon page number + (CONT.) header | Owner spec 2026-06-06. `antcv-auto-pagebreak-block-001.js` covers: (1) whole-section move at any `data-sid` boundary; (2) plain-list intra-split for sections with header + N body blocks (EXPERIENCE roles, OUTCOMES bullets, CERTIFICATIONS/EDUCATION/PUBLICATIONS/ADDITIONAL lists, CL Foundation hands_on↔professionally, CL HWIC intro/bullets/closing); (3) group-aware intra-split — visible group labels AND **hidden group boundaries detected from `data-antcv-row-path` index gaps** (1.50.266, so REGULATORY with hidden "Sensing, imaging and optics" group still splits at the group boundary); (4) table-row intra-split for sections containing a table (CORE COMPETENCIES, CL "What I bring") — thead cloned onto each continuation page; (5) single-text/headers protected: header + 1 unsplittable child → whole-section move; (6) salmon splitter labelled `▼ PAGE N ▼ (auto)` (1.50.266) with running page number per primary chain; (7) section header re-cloned on every continuation page with " (CONT.)" suffix appended to title text (1.50.266). Idempotent (undo splits before re-splitting; INJECTED markers skipped during re-stitch so cloned headers/tables aren't duplicated back into the page-1 original), debounced MutationObserver, 6-split safety cap. Synthetic probes verified each case. **Follow-up status (1.50.430, 2026-06-13):** (a) **DONE for the sections that actually multi-split** — the 📄 sidebar + table page buttons show the EFFECTIVE page via `__antcvEffPageLabel` (max(manual, `antcv:autoPagesPreview`) with an "ᵃ" suffix when auto moved the row past the manual choice). RESIDUAL: experience-role + CL page buttons still show the manual page only (whole-role cascade, auto-drift rare). (b) **DONE in preview** — the slim repeat-header strip (PAGEBREAK-STYLE-OPTIONS-001, default OFF) now also carries a small **shape-correct photo medallion** (`data-antcv-repeat-photo`) on every page after page 1 when a photo exists and `photoPosition !== 'hidden'`; suppressed when Hidden. RESIDUAL: worker-side photo on the export continuation strip (the name+subtitle strip already exports; photo is a docx-worker follow-on — repeatHeader export caveats already noted under PAGEBREAK-STYLE-OPTIONS-001). (c) **DONE for export reconcile** — `buildPayload` forwards BOTH `antcv:itemPages` (manual) and `antcv:autoPages` (auto) and computes effective `max(manual,auto)` for table rows, experience roles, and sidebar lists; preview reads the same via `__antcvEffBucket`. Tests: `diag-pageflow-options` 4/4 (A page numbers + repeat strip + CONT.; B bare; **C photo in page-2 strip; D Hidden suppresses it**). |
| PAGEBREAK-STYLE-OPTIONS-001 | **Advanced-style-menu page-continuation options** (3 controls). (a) **Keep continuation headlines** — checkbox, **default ON**: when ON, a section spanning a page keeps its "TITLE (Cont.)" heading atop the new page (current behaviour); when OFF, suppress the "(Cont.)" headings and let content continue bare. (b) **Repeat candidate header on every page** — checkbox, **default OFF**: when ON, copy the candidate identity block (name + specialization/contact strip) to the top of every page after page 1 (preview + DOCX + PDF); when OFF, only page 1 carries it. (c) **Page numbers** — off / top-right / bottom-right: render a page number in the chosen corner, inside the margin so it never overlaps body text. | ✅ CLOSED 1.50.378–379 + worker 1.14.55–56 | PAGE FLOW group in Advanced styles (styleConfig keys `contHeadlines`/`repeatHeader`/`pageNumbers`, cloud-synced). Preview: Ce gates the "(CONT.)" heading + underline, slim navy strip on page-rows 2+, corner number per page-row. Export: every worker "(Cont.)" site gated; slim header row on cont pages (body min −900 DXA); footer PAGE field for CV and CL. **Export limitation:** top-right also renders via the footer — Word header parts serialize empty through the tree-shaken bundle, and a top number would sit on the navy band of the margin-0 pages; the preview shows the true corner. Tests: `diag-pageflow-options.mjs` 2/2, `diag-pageflow-export.mjs` 11/11. Owner visual verify owed. Original spec: | Owner spec 2026-06-08 (after the table-break work). Touches: app.src.js advanced-style menu (3 persisted prefs) + preview renderer (cont-heading gate in `__antcvBreaks` / segment titles; per-page header clone; page-number overlay) + docx-worker (cont-heading gate in renderSection/renderTextBullets/renderCompetencyTable; repeat candidate-header table on continuation pages; page-number field top-right/bottom-right). The "(Cont.)" gate is the inverse of the current always-on default — wire it as a payload flag the worker reads. Bug tracker: PAGEBREAK-STYLE-OPTIONS-001. |
| WIZARD-002 | Step 6d (default languages + settings hand-off) | ✅ CLOSED 1.50.431 — the final wizard slide (`antcv-wizard-language-slide-339.js`) already owns **default languages** (the two-table selector, WIZARD-LANG-SELECTOR-001) + the section-format showcase; this adds the **settings hand-off**: a "WHERE TO CUSTOMISE NEXT" block listing Settings→Personal (languages/tense/banned words), Settings→Layout (photo position+shape, style package), Settings→Advanced (tone, page flow) before the wizard closes. Replaces the old single generic hint. Locked by `diag-wizard-lang-two-table` check 1b (3 hand-off rows). | Step 6b scrollable was DONE; 6d hand-off now shipped. |
| PREVIEW-CHATBOT-001 | **In-preview chatbot for selection-driven edits.** Select any text/element in the preview → long-press (mobile) or right-click (desktop) opens a small contextual chatbot. User says what they want changed: wording, compression, location/move, color, tone, formatting. The bot edits the selected element accordingly, **respecting the application's specific restrictions** (banned words/phrases, length budget, language, ATS mode, style package, section-specific rules — PROFILE vs OUTCOMES vs CORE COMPETENCIES, etc.). After the edit, the bot **explains why it did what it did** (anchored to the source rules — e.g. "shortened to fit the ORPHAN rule (≥4 words on the final line)" or "swapped 'spearhead' → 'led' (banned-word list)"). Per-element, per-app, with undo. | ✅ CLOSED 1.50.412 (stages 1+2) — `antcv-preview-chatbot-405.js`: selecting preview text raises a 🤖 AI-edit pill (distinct from ✨ Enhance; guarded so resize-handles / contentEditable / caret clicks never raise it); the panel shows the quoted snippet + 4 quick actions (Shorten / More concrete / Calmer tone / Fix wording) + a free-text **multi-turn "Ask"** refinement; the rules-aware LLM call returns `{rewrite, reason, rules[]}` so the **why-panel** renders **rule-citation chips** (e.g. "keep-numbers: 95% retained", "one-line-budget"); Apply persists into the `sections` store + re-renders (latest turn wins), with **Undo**. Section-aware budgets included. Locked by `diag-preview-chatbot.mjs` (11/11). Earlier "not started" was STALE. **Deferred (owner 2026-06-13):** free-form DOC-WIDE chat (this is per-element only). | Owner spec 2026-06-07. Bug tracker: PREVIEW-CHATBOT-001. |
| WIZARD-LANG-SELECTOR-001 | **Wizard language step — two-table language selector.** (1) The wizard "language set" step must actually SHOW the languages selector (currently missing / not rendered on that step). (2) Build it as **two tables side by side**: left = **all available languages** (full supported set: en, da, sv, de, fr, es, …); right = **selected languages** (user's chosen subset). Move languages left↔right to add/remove. (3) The right table is **reorderable**, and its **order sets the default language — the first entry in the selected list is the default** (drives the generation/`meta` default language). Persist the ordered selected-language list + derived default. | ✅ CLOSED 1.50.412 — `antcv-wizard-language-slide-339.js` (v1.50.284-dom-lang-picker) renders the two-table modal at wizard close: LEFT "AVAILABLE LANGUAGES" (en/da/es/zh) ↔ RIGHT "SELECTED — first is the DEFAULT", reorderable via ↑/↓, ★ DEFAULT badge on entry 0, persisted to enabledLanguages + the primary `language` (JSON-stringified). Locked by `diag-wizard-lang-two-table.mjs` (3/3, all spec reqs). Earlier registry note "not started" was STALE. | Owner spec 2026-06-07. Relates to WIZARD-002 (the separate settings hand-off step) and the Settings → Personal LanguageCard. Bug tracker: WIZARD-LANG-SELECTOR-001. |
| SPELL-ANNOTATOR-001 | **Basic spelling annotator** for editable text fields. (1) Detects the current text language (from the per-section / global language setting; not from the user's locale). (2) Loads a dictionary for that language. (3) Underlines misspelled words in red while the user types or after a short pause. (4) Click / long-press the underlined word → small popover with **alternative suggestions** (pick one to replace inline). (5) "Add to my dictionary" option for proper nouns the user wants to keep. Per-language toggle in Settings. **Owner clarification: "not necessarily related to the application"** — works on any editable field independent of JD context. | ✅ CLOSED 1.50.384 — sidecar antcv-spell-annotator-384.js + vendored nspell; en/da/es (zh documented no-op: Hunspell cannot segment Chinese); ghost-overlay underlines, suggestion popover, personal dictionary; Settings toggle UI SHIPPED 1.50.389 (SPELLING block in the Settings privacy zone: master + per-language EN/DA/ES checkboxes). Test diag-spell-annotator 5/5. Original locked decisions:** (a) **Languages** — if the user has selected EN and/or DA in their language settings, load only those; otherwise load the full AntCV supported set (currently EN, DA, ES, ZH). Dictionary fetched on demand per active language, cached in IndexedDB. (b) **Scope** — CV + CL editable text only (bullets, role/company/subtitle, greeting/opening, all section content). NOT topbar (name/contact), NOT JD textarea, NOT Signals textarea, NOT the rendered preview. (c) **Implementation** — `nspell` + Hunspell-compatible dictionaries (`dictionary-en`, `dictionary-da`, `dictionary-es`, `dictionary-zh-cn` or equivalent). Custom red-underline overlay + click-for-alternatives popover + "Add to my dictionary" (persisted to localStorage `antcv:userDict:{lang}`). Bug tracker: SPELL-ANNOTATOR-001. |
| DATA-PORTABILITY-CLOUD | Persist corrected defaults (package/tone) to the cloud/database so per-load migration isn't needed | ✅ CLOSED 1.50.385 — antcv-orphan-cloud-persist-385.js PUTs changed stylePackage/toneRegister to the existing /api/prefs (D1 preferences); marker-diffed, throttled, orphan-guarded; end-to-end test diag-orphan-cloud-persist 2/2. Original note: Owner's "do the corrections in the database" idea — riskier cloud-write path, deferred. |
| ADV-INDENT-CONTROLS-001 | **Advanced-styles indent controls** — two range sliders in the Advanced styles panel: (1) **Indent from edge** (main-column content padding from the page edge, 4–40px) and (2) **Bullet / emoji list indent** (the bullet/emoji list body+continuation hang, 10–60px). | ✅ CLOSED — preview 1.50.350; export-parity shipped worker 1.14.47/49 (cell margins + numbering indent follow the sliders; bulletIndent forwards only off-default) | Owner spec 2026-06-10. Stored on the styleConfig (`mainEdgeIndent` default 10, `bulletIndent` default 24) so they persist + cloud-sync + reset with the rest of the style. Read at the 5 preview bullet render sites (`paddingLeft:bulletIndent; textIndent:10-bulletIndent`) and the `.antcv-document-main` padding. Controls mirror the FONTS group (range inputs → partial-merge setter `t`). Verified: `pwa/test/unit/indent-controls.test.mjs` 4/4; defaults reproduce the built-in look exactly (no change unless the user moves a slider). **FOLLOW-UP (export-parity):** the docx-worker still uses fixed cell margins (144 DXA) + bullet numbering (left 360 / hanging 200); to honour these controls in DOCX/PDF, pass `mainEdgeIndent`/`bulletIndent` in the docx-client style payload and map them to the worker cell margin + numbering indent. Until then, moving a slider changes the PREVIEW only — defaults keep preview≈export. |

## PARKED / descoped

| ID | Feature | Reason |
|----|---------|--------|
| FT-LOOP-DAMPER | Global MutationObserver coalescer | ⚪ reverted 1.50.89 (broke per-row control self-cleanup). |

---

### How to keep this current

- When a feature ships: move its row to **CLOSED**, add the PR + version.
- When the app.js source cleanups (APPJS-ID-SCHEME-UNIFY) land, retire the
  orphan-apply / loading-gate workarounds and note it here + in the bug tracker.
- Bugs (regressions, defects) go in `docs/qa/ACTIVE_BUGS.md`; net-new capability
  goes here.
