# AntCV — Feature Registry

A living registry of **features** (distinct from the bug tracker at
`docs/qa/ACTIVE_BUGS.md`). Each feature is **CLOSED** (shipped + live), **OPEN**
(planned or in progress), or **PARKED** (descoped / blocked).

Last updated: **2026-06-11** — CLOSED: SHARE-TARGET-JD-URL-001 (1.50.375), PHOTO-SIDEBAR-BRIDGE-001 (preview 1.50.368–372 + worker 1.14.51–53), ADV-INDENT-CONTROLS-001 export-parity (worker 1.14.47/49), EXPORT-PAGE2-001 print path (1.50.374) + export-preview page selector. BRIDGE-SIDEBAR-PALETTE-001 retired by owner direction (bridge uses standard sidebar palette since 1.50.371).

Status legend: ✅ CLOSED (shipped) · 🟡 OPEN (active/planned) · ⚪ PARKED.

---

## CLOSED — shipped & live

| ID | Feature | Where | Notes |
|----|---------|-------|-------|
| FT-DATA-EXPORT | Download all stored data + analytics to a file (optional WebCrypto AES passphrase) | `antcv-data-export-360.js` | Button in the PRIVACY zone; "Save my data locally first" checkbox in the erase card. PR #176/#181/#185. |
| FT-DELETE-SAVE | Back up before account erase | same sidecar wraps `AntcvFullErase` | PR #176. |
| FT-CONSOLE-QUIET | Central console quieter (suppress ~70 boot/status banners; `localStorage.antcvVerboseConsole=1` to restore) | `antcv-console-quiet.js` | PR #188. |
| FT-SIDEBAR-COLOR | Preview sidebar follows the visual style (`var(--package-base)`, matches DOCX) | `antcv-sidebar-bg-token.js` | PR #210. |
| FT-LOADING-GATE | "Loading…" cover on login for returning users (masks the wizard flash + palette/tone settle) + orphan tone migration | `antcv-login-loading-gate.js` | PR #220. |
| FT-PHOTO-POS | Photo placements in preview (sidebar-top/bottom, header L/R, main L/R, band-overlap) | `antcv-photo-position.js` + app.js | Finders repaired for the single-table renderer (PR #196); the full position family + persistence closed 1.50.368–373 (see PHOTO-SIDEBAR-BRIDGE-001). |
| FT-DOCX-PHOTO | DOCX photo placement incl. `band-overlap` → top-of-sidebar | docx-worker `generate.js` | PR #200 (worker 1.14.17). |
| FT-VISUAL-PACKAGES | 7 visual style packages (Copenhagen Modern, Navy Executive, Warm Terracotta, Nordic Frost, Pampas Contemporary, Tokyo Precision, Delhi Technical) | registry + islands + app.js | Palette re-derives render colours on load (PR #226), but the default-palette "black mix" bug **PACKAGE-PALETTE-MIX-001 is still OPEN** — persisted id stays orphan `scandinavian`. Close-out = APPJS-ID-SCHEME-UNIFY. |
| FT-WRITING-STYLES | 12 writing systems (nordic-minimal … hybrid-balanced) + banned lists + tone chips | `writingSystems/registry.json`, proxy worker | Pre-existing; tone orphan default migrated this session. |
| FT-EXPORT | DOCX + PDF export (cv-proxy / docx-worker / CloudConvert) | workers | Pre-existing. |
| FT-ANALYSIS-REPORT | Branded AI-watermarked JD-analysis PDF | `antcv-analysis-report-pdf-360.js` | Landed in parallel work (#219 family). |
| FT-DATA-IMPORT | **Restore** a downloaded backup (counterpart to FT-DATA-EXPORT) — "⬆ Restore from backup" button beside "⬇ Download my data"; reads the backup file, decrypts (PBKDF2/AES-GCM) if it was passphrase-encrypted, confirms the overwrite, restores localStorage, reloads | `antcv-data-import-331.js` (1.50.331) + `dataRaw` added to `antcv-data-export-360.js` | DATA-IMPORT-001. Lossless via the new `dataRaw` map (exact localStorage strings — fixes the `JSON.parse` round-trip ambiguity, e.g. `doc:"cv"`); legacy plain backups fall back to `JSON.stringify(data[key])`. Round-trip verified headless (`pwa/test/diag-data-import.mjs`): plain + encrypted restore byte-for-byte, secrets excluded from plain backups, wrong passphrase rejected. **CONSOLIDATED (1.50.333):** the standalone Settings "Restore" button was removed; `antcv-data-import-331.js` is now a pure **library** (`window.AntcvDataImport` + `AntcvIsBackupEnvelope`). The single import entry point is the floating 📥 `antcv-data-importer.js` — its `handleJSON` detects an AntcvBackup envelope and delegates to the library for a decrypt-if-needed, confirm, lossless full restore + reload (all other dropped files keep the merge/diff flow). One import path. |
| FT-FOUNDATION-CONTROLS | Per-subsection controls (manual page break, CJLR alignment, Enhance, Fix/Fit) for BOTH FOUNDATION sub-parts (Hands-on + Professionally) | `antcv-foundation-controls-327.js` (1.50.327) | Owner spec 2026-06-09. Stores align/page in `antcv.foundationControls.v1` (worker `renderFoundation` + docx-client already honour it → preview AND export). Page break mirrors into `antcv:itemPages[foundation][0/1]` for the preview salmon. Enhance/Fix are local deterministic transforms (same lightweight pattern as the HOW-I-WOULD-CONTRIBUTE per-part controls, 245), not LLM. Preview alignment + page-break + transforms verified headless (`pwa/test/diag-foundation-controls.mjs`); panel-button placement (attaches when the foundation card is open) is owner-visual-verify. |
| FT-DEBUG-LOGGER | Crash-proof in-app error logger + on-device viewer (for mobile-only blue screens with no devtools). Captures uncaught errors + breadcrumbs to localStorage synchronously (survives crash+reload); plain-DOM overlay opens via `#antcv-debug`, a 4-tap top-right gesture, a Settings button, or `window.AntcvDebug.open()`. Copy/Share/Download/Clear. | `antcv-debug-logger.js` (v1.50.167) | Loads first; does NOT touch `window.fetch`. Regression-guarded by the `debug-logger` browser-QA check. Built to diagnose PERSONAL-DATA-CRASH-001. |

## OPEN — planned / in progress

| ID | Feature | Status | Notes |
|----|---------|--------|-------|
| FEATURE-CONF-001 | Per-sentence **confidence overlay** (Application-tab toggle; red=low/yellow=medium; hover shows the issue) | ✅ v1 CLOSED 1.50.386 — deterministic grounding heuristic vs the user own source facts; Analysis-panel toggle, red/yellow block tints, per-sentence issues on hover; pluggable AntcvConfidence.setScores for the future worker LLM self-check pass. Test diag-confidence-overlay 5/5. Original spec: NOT in the locked docs — needs a worker self-check pass returning `{text,confidence,issue}` + app.js toggle/store + a preview renderer. Spec in `docs/plan/Batch_2026-06-04_feature-and-bug-triage.md`. |
| SHARE-TARGET-JD-URL-001 | **Share/Open-with a job link into AntCV.** On Android, when the user shares (or "open with") a job URL, the installed AntCV PWA appears in the chooser. When picked, if the user is **logged in**, AntCV opens with that URL pre-filled into the **JD URL** field (the "OR PASTE A JD URL" input) and is ready to Fetch JD; if not logged in, route to login first, then drop the URL into the JD field after auth. | ✅ CLOSED 1.50.375 — manifest `share_target` + `antcv-share-target-jd-375.js` (URL extraction, own-origin guard, sessionStorage stash across login, synchronous step→upload route, native-setter fill). Verified headless `pwa/test/diag-share-target-jd.mjs` 3/3. Owner device verify owed (installed-PWA share sheet). Original build spec retained: | Owner spec 2026-06-09. **Current state:** `pwa/manifest.json` has **no `share_target`** (so AntCV does not appear in the Android share sheet today). The app already (a) reads `window.location.search` via `URLSearchParams` for control params (app.src.js ~L11930), (b) has the JD-URL fetch path (the `Jt({...method:"url-fetch"...})` flow + `/api/fetch-jd-url`, app.src.js ~L12293), and (c) exposes login state via `window.AntcvAuth.isSignedIn()`. **Build:** (1) add a `share_target` to the manifest — GET form is simplest: `"share_target":{"action":"./","method":"GET","params":{"url":"shared_url","text":"shared_text","title":"shared_title"}}` (Android shares links mostly as `text`; capture both `url` and `text` and pick the first http(s) token). (2) Optional `web+`-style or http `url_handlers`/scope handling if we also want "open link with AntCV" (link-capturing) — needs `display_override` + origin association (`assetlinks`-style) and is heavier than share. (3) A small sidecar reads `shared_url`/`shared_text` on boot, extracts the URL, and: if `AntcvAuth.isSignedIn()` → set the JD-URL input value + focus/scroll (reuse the existing JD-URL setter path so Fetch JD is one tap); else stash the URL (e.g. `sessionStorage`) and re-apply after the login gate completes. (4) Strip the param from the address bar via `history.replaceState` after consuming (same pattern as the existing reset/logout handling). **Notes/caveats:** share_target only fires for an **installed** PWA; iOS Safari share-target is unsupported (Android/Chromebook + Chromium desktop only). Validate the captured value is an http(s) URL before filling. Verify: install PWA → share a LinkedIn job link from the browser/job app → AntCV appears → pick it → (logged in) URL lands in JD field. Bug tracker / spec: SHARE-TARGET-JD-URL-001. |
| PHOTO-SIDEBAR-BRIDGE-001 | True **sidebar-bridge** photo: split the candidate-header cell so the disc hovers on the header/sidebar seam (preview + DOCX) | ✅ CLOSED 1.50.368–372 + worker 1.14.51–53 | Shipped as `band-overlap` (split header row, floating medallion, 1.3× native size, equal air, contact squeeze) PLUS a full position family: main top L/R with circular crescent wrap, main bottom L/R, bridge-middle/bottom on the vertical seam with dual crescents — preview AND export. The 1.50.373 client fix (EXPORT-PHOTO-POS-CLAMP-001) was the missing export half: `readPhotoPosition` clamped `band-overlap` to sidebar-top before the payload left the browser. Tests: diag-photo-positions (12/12), diag-photo-bridge, diag-photo-positions-export (9/9), photo-position-forward unit (5/5). |
| BRIDGE-SIDEBAR-PALETTE-001 | **Bridge-mode sidebar tint** — when the photo is in bridge mode (`photoPosition === 'band-overlap'`, the `◐ Sidebar bridge` button), the preview sidebar bg shifts slightly BRIGHTER and the headings/text/lines slightly DARKER, so the bridging medallion reads against a subtly distinct sidebar. | ⚪ RETIRED 1.50.371 (owner direction) | Shipped 1.50.352, then REMOVED at the owner's 2026-06-11 round-3 feedback ("the font and color of sidebridge need to fit the other positions") — bridge mode uses the standard sidebar palette now. `__antcvShadeHex` stays in app.src.js (unused by bridge); do not re-enable without a new owner ask. |
| APPJS-ID-SCHEME-UNIFY | Unify app.js's package id scheme with the registry (`scandinavian→copenhagen-modern`, `copenhagen_executive→navy-executive`, …) + persist the selection through cloud-restore | ✅ CLOSED 1.50.387 — self-migrating stylePackage init + normalized cloud-restore (both sites), va/xa re-keyed to registry ids, default copenhagen-modern; closes the PACKAGE-PALETTE-MIX-001 root. Test diag-pkg-id-unify 3/3. The orphan-apply / loading-gate workaround sidecars stay as no-ops until an owner-confirmed clean cycle, then retire. |
| ~~DATA-IMPORT-001~~ | **Restore** a downloaded backup file | ✅ CLOSED 1.50.331 | Shipped — see FT-DATA-IMPORT in the CLOSED table. |
| PROCESSING-QUEUE-INDICATOR-001 | Per-subsection live work-state badge: pink **"processing"** while being worked (language change, new JD/kernel, compress, enhance), yellow **"queue"** when scheduled later in the same command (enhance-over-subsection → first pink, rest yellow). Plus: CJLR (Center/Justify/Left/Right) buttons working in **every** sub-subsection. | ✅ badge half ALREADY IMPLEMENTED (verified in source 2026-06-12) · 🟡 CJLR-everywhere remains | The registry note "no per-subsection lifecycle state exists" is STALE: `Ce` renders exactly this badge (app.src.js ~3835–3905 — yellow `pending` with "pending…", pink active states `working`/`translating`/`debating` with a spinner, tinted card background + colored left border), and the state is written per section: bulk Compress/Enrich mark every target `pending` then flip each to `working` as processed (~16491/16570/16595/17183/17364); generate/language paths mark all sids `pending` (~14864) and use `translating`. Owner: run any Enhance/Compress over a column to see first-pink-rest-yellow. CJLR-in-every-sub-subsection is the remaining slice — and it is NARROWER than this row suggested (gap analysis 2026-06-12): per-item CJLR ALREADY EXISTS for labeled_list / education / list via `antcv-item-align.js` (loaded, v1.40.178 — editor-row cyclers writing `antcvItemAlignment[sid]["items.{i}.…"]`, preview applied on `data-antcv-row-path` blocks), per-part for FOUNDATION + HWIC via their control sidecars, and section-default via `antcv-section-align.js`. The WORKER already honours every path generically (`paraAlignPath`: content / intro / items.N(+.t) / closing / hands_on / professionally / roles.N(.title/.bullets.N)) and the docx-client merges `antcvItemAlignment` into `item_alignment`. **(1) EXPERIENCE — SHIPPED 1.50.381 (CJLR-EXPERIENCE-001):** per-ROLE cycler on each role card (`data-antcv-item-row="experience"` → path `roles.{i}`), preview bullet rows marked `roles.N.bullets.M` + `data-antcv-role-path`, lookup chain per-bullet > marker > role > section default; export was already wired. Locked by `diag-cjlr-experience.mjs` (4/4). **(2) table rows — SHIPPED 1.50.383 + worker 1.14.58 (CJLR-TABLE-001), with a twist:** the preview/editor half ALREADY EXISTED — `antcv-core-competencies-row-controls-234.js` owns per-row table CJLR (storage `antcv.coreCompetencies.rowAlignment.v1` = `{"row-<i>": align}`, full-rows index, header at 0; its sweep stomps any other writer, so the generic item-align path deliberately stays out). The missing half was EXPORT parity: the storage was never forwarded. Now `buildPayload` merges explicit `row-N` entries into the table section's `item_alignment["rows.N"]` and the worker's `renderCompetencyTable` applies them to the EXPERTISE cell. Locked by `table-align-forward.test.mjs` (4/4), `diag-cjlr-table.mjs` (preview), `diag-cjlr-table-export.mjs` (DOCX jc). **CJLR-everywhere is now CLOSED — every sub-subsection type (lists, education, foundation, HWIC, experience roles/bullets, table rows) has working CJLR in preview AND export.** |
| AUTO-PAGEBREAK-BLOCK-001 | Automated page breaks: **always** show the salmon splitter when content exceeds one A4 page in preview; sliding is **block-level** — a whole sub-subsection moves to the next page (never partial, never the whole parent subsection). | 🟡 1.50.266 — hidden-group split + salmon page number + (CONT.) header | Owner spec 2026-06-06. `antcv-auto-pagebreak-block-001.js` covers: (1) whole-section move at any `data-sid` boundary; (2) plain-list intra-split for sections with header + N body blocks (EXPERIENCE roles, OUTCOMES bullets, CERTIFICATIONS/EDUCATION/PUBLICATIONS/ADDITIONAL lists, CL Foundation hands_on↔professionally, CL HWIC intro/bullets/closing); (3) group-aware intra-split — visible group labels AND **hidden group boundaries detected from `data-antcv-row-path` index gaps** (1.50.266, so REGULATORY with hidden "Sensing, imaging and optics" group still splits at the group boundary); (4) table-row intra-split for sections containing a table (CORE COMPETENCIES, CL "What I bring") — thead cloned onto each continuation page; (5) single-text/headers protected: header + 1 unsplittable child → whole-section move; (6) salmon splitter labelled `▼ PAGE N ▼ (auto)` (1.50.266) with running page number per primary chain; (7) section header re-cloned on every continuation page with " (CONT.)" suffix appended to title text (1.50.266). Idempotent (undo splits before re-splitting; INJECTED markers skipped during re-stitch so cloned headers/tables aren't duplicated back into the page-1 original), debounced MutationObserver, 6-split safety cap. Synthetic probes verified each case. **Follow-up status (1.50.430, 2026-06-13):** (a) **DONE for the sections that actually multi-split** — the 📄 sidebar + table page buttons show the EFFECTIVE page via `__antcvEffPageLabel` (max(manual, `antcv:autoPagesPreview`) with an "ᵃ" suffix when auto moved the row past the manual choice). RESIDUAL: experience-role + CL page buttons still show the manual page only (whole-role cascade, auto-drift rare). (b) **DONE in preview** — the slim repeat-header strip (PAGEBREAK-STYLE-OPTIONS-001, default OFF) now also carries a small **shape-correct photo medallion** (`data-antcv-repeat-photo`) on every page after page 1 when a photo exists and `photoPosition !== 'hidden'`; suppressed when Hidden. RESIDUAL: worker-side photo on the export continuation strip (the name+subtitle strip already exports; photo is a docx-worker follow-on — repeatHeader export caveats already noted under PAGEBREAK-STYLE-OPTIONS-001). (c) **DONE for export reconcile** — `buildPayload` forwards BOTH `antcv:itemPages` (manual) and `antcv:autoPages` (auto) and computes effective `max(manual,auto)` for table rows, experience roles, and sidebar lists; preview reads the same via `__antcvEffBucket`. Tests: `diag-pageflow-options` 4/4 (A page numbers + repeat strip + CONT.; B bare; **C photo in page-2 strip; D Hidden suppresses it**). |
| PAGEBREAK-STYLE-OPTIONS-001 | **Advanced-style-menu page-continuation options** (3 controls). (a) **Keep continuation headlines** — checkbox, **default ON**: when ON, a section spanning a page keeps its "TITLE (Cont.)" heading atop the new page (current behaviour); when OFF, suppress the "(Cont.)" headings and let content continue bare. (b) **Repeat candidate header on every page** — checkbox, **default OFF**: when ON, copy the candidate identity block (name + specialization/contact strip) to the top of every page after page 1 (preview + DOCX + PDF); when OFF, only page 1 carries it. (c) **Page numbers** — off / top-right / bottom-right: render a page number in the chosen corner, inside the margin so it never overlaps body text. | ✅ CLOSED 1.50.378–379 + worker 1.14.55–56 | PAGE FLOW group in Advanced styles (styleConfig keys `contHeadlines`/`repeatHeader`/`pageNumbers`, cloud-synced). Preview: Ce gates the "(CONT.)" heading + underline, slim navy strip on page-rows 2+, corner number per page-row. Export: every worker "(Cont.)" site gated; slim header row on cont pages (body min −900 DXA); footer PAGE field for CV and CL. **Export limitation:** top-right also renders via the footer — Word header parts serialize empty through the tree-shaken bundle, and a top number would sit on the navy band of the margin-0 pages; the preview shows the true corner. Tests: `diag-pageflow-options.mjs` 2/2, `diag-pageflow-export.mjs` 11/11. Owner visual verify owed. Original spec: | Owner spec 2026-06-08 (after the table-break work). Touches: app.src.js advanced-style menu (3 persisted prefs) + preview renderer (cont-heading gate in `__antcvBreaks` / segment titles; per-page header clone; page-number overlay) + docx-worker (cont-heading gate in renderSection/renderTextBullets/renderCompetencyTable; repeat candidate-header table on continuation pages; page-number field top-right/bottom-right). The "(Cont.)" gate is the inverse of the current always-on default — wire it as a payload flag the worker reads. Bug tracker: PAGEBREAK-STYLE-OPTIONS-001. |
| WIZARD-002 | Step 6d (default languages + settings hand-off) | 🟡 registered | Step 6b scrollable is **DONE**; only 6d remains. |
| PREVIEW-CHATBOT-001 | **In-preview chatbot for selection-driven edits.** Select any text/element in the preview → long-press (mobile) or right-click (desktop) opens a small contextual chatbot. User says what they want changed: wording, compression, location/move, color, tone, formatting. The bot edits the selected element accordingly, **respecting the application's specific restrictions** (banned words/phrases, length budget, language, ATS mode, style package, section-specific rules — PROFILE vs OUTCOMES vs CORE COMPETENCIES, etc.). After the edit, the bot **explains why it did what it did** (anchored to the source rules — e.g. "shortened to fit the ORPHAN rule (≥4 words on the final line)" or "swapped 'spearhead' → 'led' (banned-word list)"). Per-element, per-app, with undo. | 🟡 not started | Owner spec 2026-06-07. Heavier feature: needs (a) preview-element selection capture (text node range or section-cell ref), (b) a context-aware LLM call that includes the current rules/budgets + the selected snippet, (c) a diff/preview-before-apply UI, (d) a "why" panel surfacing the rule cited. Touches the writing-engine + section-format-prefs layers. Bug tracker: PREVIEW-CHATBOT-001. |
| WIZARD-LANG-SELECTOR-001 | **Wizard language step — two-table language selector.** (1) The wizard "language set" step must actually SHOW the languages selector (currently missing / not rendered on that step). (2) Build it as **two tables side by side**: left = **all available languages** (full supported set: en, da, sv, de, fr, es, …); right = **selected languages** (user's chosen subset). Move languages left↔right to add/remove. (3) The right table is **reorderable**, and its **order sets the default language — the first entry in the selected list is the default** (drives the generation/`meta` default language). Persist the ordered selected-language list + derived default. | 🟡 not started | Owner spec 2026-06-07. Relates to WIZARD-002 (default languages) and the Settings → Personal LanguageCard. Supported set lives in `writingSystems/registry.json` language partition. Bug tracker: WIZARD-LANG-SELECTOR-001. |
| SPELL-ANNOTATOR-001 | **Basic spelling annotator** for editable text fields. (1) Detects the current text language (from the per-section / global language setting; not from the user's locale). (2) Loads a dictionary for that language. (3) Underlines misspelled words in red while the user types or after a short pause. (4) Click / long-press the underlined word → small popover with **alternative suggestions** (pick one to replace inline). (5) "Add to my dictionary" option for proper nouns the user wants to keep. Per-language toggle in Settings. **Owner clarification: "not necessarily related to the application"** — works on any editable field independent of JD context. | ✅ CLOSED 1.50.384 — sidecar antcv-spell-annotator-384.js + vendored nspell; en/da/es (zh documented no-op: Hunspell cannot segment Chinese); ghost-overlay underlines, suggestion popover, personal dictionary; Settings toggle UI SHIPPED 1.50.389 (SPELLING block in the Settings privacy zone: master + per-language EN/DA/ES checkboxes). Test diag-spell-annotator 5/5. Original locked decisions:** (a) **Languages** — if the user has selected EN and/or DA in their language settings, load only those; otherwise load the full AntCV supported set (currently EN, DA, ES, ZH). Dictionary fetched on demand per active language, cached in IndexedDB. (b) **Scope** — CV + CL editable text only (bullets, role/company/subtitle, greeting/opening, all section content). NOT topbar (name/contact), NOT JD textarea, NOT Signals textarea, NOT the rendered preview. (c) **Implementation** — `nspell` + Hunspell-compatible dictionaries (`dictionary-en`, `dictionary-da`, `dictionary-es`, `dictionary-zh-cn` or equivalent). Custom red-underline overlay + click-for-alternatives popover + "Add to my dictionary" (persisted to localStorage `antcv:userDict:{lang}`). Bug tracker: SPELL-ANNOTATOR-001. |
| DATA-PORTABILITY-CLOUD | Persist corrected defaults (package/tone) to the cloud/database so per-load migration isn't needed | ✅ CLOSED 1.50.385 — antcv-orphan-cloud-persist-385.js PUTs changed stylePackage/toneRegister to the existing /api/prefs (D1 preferences); marker-diffed, throttled, orphan-guarded; end-to-end test diag-orphan-cloud-persist 2/2. Original note: Owner's "do the corrections in the database" idea — riskier cloud-write path, deferred. |
| ADV-INDENT-CONTROLS-001 | **Advanced-styles indent controls** — two range sliders in the Advanced styles panel: (1) **Indent from edge** (main-column content padding from the page edge, 4–40px) and (2) **Bullet / emoji list indent** (the bullet/emoji list body+continuation hang, 10–60px). | ✅ CLOSED — preview 1.50.350; export-parity shipped worker 1.14.47/49 (cell margins + numbering indent follow the sliders; bulletIndent forwards only off-default) | Owner spec 2026-06-10. Stored on the styleConfig (`mainEdgeIndent` default 10, `bulletIndent` default 24) so they persist + cloud-sync + reset with the rest of the style. Read at the 5 preview bullet render sites (`paddingLeft:bulletIndent; textIndent:10-bulletIndent`) and the `.antcv-document-main` padding. Controls mirror the FONTS group (range inputs → partial-merge setter `t`). Verified: `pwa/test/unit/indent-controls.test.mjs` 4/4; defaults reproduce the built-in look exactly (no change unless the user moves a slider). **FOLLOW-UP (export-parity):** the docx-worker still uses fixed cell margins (144 DXA) + bullet numbering (left 360 / hanging 200); to honour these controls in DOCX/PDF, pass `mainEdgeIndent`/`bulletIndent` in the docx-client style payload and map them to the worker cell margin + numbering indent. Until then, moving a slider changes the PREVIEW only — defaults keep preview≈export. |

## PARKED / descoped

| ID | Feature | Reason |
|----|---------|--------|
| FT-LOOP-DAMPER | Global MutationObserver coalescer | ⚪ reverted 1.50.89 (broke per-row control self-cleanup). |

---

### How to keep this current

- When a feature ships: move its row to **CLOSED**, add the PR + version.
- When the app.js source cleanups (APPJS-ID-SCHEME-UNIFY) land, retire the
  orphan-apply / loading-gate workarounds and note it here + in the bug tracker.
- Bugs (regressions, defects) go in `docs/qa/ACTIVE_BUGS.md`; net-new capability
  goes here.
