# AntCV — Autonomous Night Run Plan (2026-06-10 → 06-11)

Single source of truth for the unattended 8-hour run. Read this end-to-end before doing anything.

## 0. Current state (start of run)

- **Production: HEALTHY.** `https://antcv.pages.dev/app.js` = **852,577 bytes** (real bundle). Earlier tonight a concurrent session shipped a **7-byte stub** app.js to `main` → production blank for ~1h; restored in commit `bbde379`.
- **Branches all at `bbde379` and identical:** `main`, `claude/antcv-roadmap-bugs-L9Sqa`, `plan/2026-06-06-analysis-followups`. origin/main = `bbde379`.
- **Latest shipped:** 1.50.353 (PWA) + docx-worker 1.14.46 + relay/proxy/demo-proxy current. Nordic engine wired, kernel-history-keep, analysis-print-complete, bridge palette, bullet-edge, CL list (Cont.), data-loss guard, demo-relay-identity all live.

## 1. NON-NEGOTIABLE GUARDRAILS (the stub incident must never repeat)

1. **app.js is built ONLY via** `npx terser pwa/app.src.js --compress --mangle -o pwa/app.js`. NEVER `npm run build:app` (esbuild — prepends `"use strict"`, blue-screens). EDIT `pwa/app.src.js`, never `pwa/app.js` by hand.
2. **Before committing ANY change to `pwa/app.js`, assert ALL of:**
   - `node --check pwa/app.js` passes
   - first 12 chars are `(()=>{const ` (head `(()=>{`)
   - `0` occurrences of `use strict`
   - **`app.js` size ≥ 800,000 bytes AND `app.src.js` size ≥ 1,900,000 bytes** (a stub/empty bundle is the catastrophic failure mode — refuse to commit if smaller)
   - `node pwa/test/boot-smoke.mjs` prints `BOOT-SMOKE OK`
3. **After every PWA deploy, curl the live bundle:** `curl -s -o /dev/null -w '%{size_download}' https://antcv.pages.dev/app.js` — must be ≥ 800,000. If it returns a tiny number, production is down → restore from the last good commit immediately.
4. **Cache-bust trio on every app.js change:** bump `app.js?v=` in `pwa/index.html`, `CACHE` in `pwa/sw.js`, `TARGET_VERSION` in `pwa/antcv-version-override.js`; add the PREVIOUS version (never the current) to `STALE_VERSIONS`.
5. **Branch discipline:** before every push `git fetch origin main`; if origin/main moved, `git merge origin/main` (NOT a blind reset). Keep the 3 branches identical. **Never force-push `main`** except to recover from a confirmed broken-prod stub. Concurrent sessions exist — re-check origin before each push.
6. **Workers:** deploy via `gh workflow run deploy.yml -f target=<name> -f mode=deploy -f confirm=<name>`; one at a time; `proxy` and `demo-proxy` must stay mirrored (`writing-style-engine.js`, `jd-analysis.js`, `fetch-jd-url.js` byte-identical). docx-worker bump its `VERSION` const.
7. **Every change ships with a committed headless test** (`pwa/test/*.mjs`, `pwa/test/unit/*.test.mjs`, or `workers/*/test/*.mjs`). Diagnostic-first: reproduce → probe → patch. Don't guess on rendered PDF/preview geometry — those need the owner's eyes (queue them, don't finalize blind).
8. **Commit message footer:** `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.

## 2. Work queue — TIER A (autonomous-safe: implement + headless-verify + ship)

Do these in order. Each = diagnose → fix → test → cache-bust → commit → push → deploy → verify-live.

1. **HOWCONTRIBUTE-001** — "How I would contribute" bullets missing in template preview. Check the `text_bullets` / contribute renderer + `mergeHowContributeFromLocalStorage` path (export-side merge exists; preview drops bullets). Verify Preview↔DOCX parity. Likely app.src.js + a headless render test.
2. **SETTINGS-SUBTAB-001 / SETTINGS-NAV-Z-001 / APP-HISTORY-001 (cluster)** — Settings panel renders BEHIND the preview (z-index/stacking) when opened from Application-History / "EN" / "Open in Settings →". Sidecars: `antcv-app-history-zfix-291`, `antcv-preview-shell-sticky-341`. Sidecar z-index fix + headless test asserting the settings panel is on top.
3. **HARDREFRESH-001** — in-app Hard Refresh confirm does nothing after OK (no reload). Diagnose the handler; sidecar or app.src.js fix; headless test.
4. **PRIVACY-FAB-FLICKER-MOBILE-001** — top-bar privacy pill flickers on mobile. Probe first (don't blind-patch the central sidecar); dampen. Headless test.
5. **PRIVACY-DEMO-001** — Privacy LED not visible in demo mode. Gate fix in `antcv-privacy-led.js`; headless test.
6. **DEMO-TOGGLE-001** — add an in-app Demo⇄Paid toggle (Settings) calling `window.AntcvSetUserMode`. Sidecar + test.
7. **GEN-UNSOL-002** — generate must emit JD-grounded `company`/`role` (so a tailored gen never falls back to "Open Application — Unsolicited" when the JD names a company). Prompt/schema change in `workers/proxy` + `demo-proxy` `generate` path (mirrored); unit test on the output normaliser.
8. **PERF-003 / PERF-004 (convergence speed)** — trim consensus width to 1–2 providers; skip enrich↔compress cycle when no change. Worker logic in the `ee()` router / generate path; unit tests. (Improves the owner's "convergence speed" ask.)
9. **PB-WORKER-CONT-HEADER-001** — EXPERIENCE continuation header in the export. Worker `renderExperience` / heading-cont path; extend `diag-twocol-ownerlike` style test.
10. **RERENDER-STORM-001** — `requestAnimationFrame` violation flood. Diagnose the offending sidecar loop; dampen with a guard. Verify via the full-app diag (error count = 0).
11. **APP-SENTENCE-STYLE-001 / SPECIALISATION-EDIT-001** — application "Application: Role — Company" sentence + specialisation line editability. app.src.js; render test.

## 3. Work queue — TIER B (prepare, but DO NOT finalize without owner visual)

These touch rendered PDF/preview geometry — implement a best-effort change behind a flag/version, ship it, and **queue a morning validation question**. Do not iterate blind more than once.

- **PREVIEW-PDF-SIDEBAR-GEOM-001 (bullets/heading)** — shipped 1.50.353/1.14.46; owner to confirm the marker-at-edge + tight gap + tighter heading rule look right.
- **AUTO-PAGEBREAK-CV-MIDGROUP-001 / SALMON-PARALLEL-COLUMNS-001 / PREVIEW-PDF-PARITY-001** — export↔preview pagination geometry. The measurer→client→worker chain is verified for the CV sidebar (see `diag-sidebar-cont-e2e`); remaining gaps are pixel-level.
- **PHOTO-SIDEBAR-BRIDGE-001** — true header/sidebar-seam photo split (design build); bridge-tint palette already shipped (BRIDGE-SIDEBAR-PALETTE-001).
- **Indent-controls export-parity** — make the Advanced indent sliders also drive the docx-worker (pass `mainEdgeIndent`/`bulletIndent` in the style payload).

## 4. DO NOT TOUCH (blocked / risky / needs owner input)

- **REACT-185-EDIT-REGULATORY-001** — not reproduced on current build; needs the owner's console stack (maps to old 1.50.334). Leave the stress harness in place.
- **DEMO-PERSIST-001** — needs a live console probe on `51pegasib@gmail.com`; don't blind-patch the relay mode logic.
- **LOGIN-GATE-001** — app-shell boot path; a bad change blue-screens first paint. Owner decision.
- **BUILD-APP-BROKEN-001 / APPJS-REBUILD-001** — do NOT attempt to make `npm run build:app` work; terser is the only sanctioned builder.
- **WIZARD-LANG-SELECTOR-001 / EXPORT-OPTIONS island work** — needs the `antcv-react-islands.js` TS bundle rebuilt (`src/islands/…`); only attempt if a verified islands build + identity check is available, else skip.
- **Nordic-unsolicited PWA trigger** — engine composes when `unsolicited:true` is set; the PWA flag needs the islands `bo()` rebuilt. Skip unless the islands build is verified.

## 5. Loop protocol

Process TIER A top-to-bottom. For each: if confidence is high and it verifies headlessly → ship. If you hit a genuinely blocking ambiguity that only the owner can resolve → skip it, note it in this file under a `## Morning questions` section, and move on (do NOT stall the run). Re-read origin/main before each push. Keep the 3 branches identical. Append a `## Run log` entry per shipped item (id, version, test result, live-bundle-size check).

## 6. Issue ledger pointer

Full open/closed detail lives in `docs/qa/ACTIVE_BUGS.md` (newest at top) and `docs/FEATURES_REGISTRY.md` (CLOSED / OPEN / PARKED). This file is the prioritized, guard-railed execution plan for the night.

---

## Run log

(append shipped items here: `- <ID> <version> — <result> — live app.js <bytes>`)

- HOWCONTRIBUTE-001 1.50.354 — ROOT CAUSE: antcv-shape-guard.js normalizeItem() nulled
  every non-object, wiping text_bullets string items on every 'sections' write; it also
  stamped bullets:[] which poisoned the docx-client merge and the 245 sidecar's data-loss
  guard. Fixed all three (string items pass through; merges prefer NON-EMPTY bullets).
  Tests: diag-howcontribute-preview.mjs (was FAILED → OK), unit howcontribute-merge
  6/6, full unit 79/79, BOOT-SMOKE OK. Verified live: shape-guard 354 deployed, app.js
  852,577 bytes. No app.js change.
- SETTINGS-SUBTAB-001 / SETTINGS-NAV-Z-001 / APP-HISTORY-001 1.50.355 — ROOT CAUSE: the
  Settings modal JSX (`K && …`, z-10000) was rendered ONLY in the upload route, so every
  editor-route trigger (⚙ / "Open in Settings →" / route hook) set state and mounted
  NOTHING (reproduced: zero settings DOM in editor). Hoisted the 7,378-line modal
  expression into a component-scope closure `__antcvSettingsModal` (acorn-verified
  extent), called from BOTH routes; editor mounts it at the route root. app.src.js
  edited, app.js rebuilt via sanctioned terser AFTER passing the identity round-trip
  gate (unedited rebuild → BOOT-SMOKE OK). Gates: node --check OK, head `(()=>{const `,
  0 use-strict, app.js 852,595 / src 2,025,116 bytes. Tests: diag-settings-front.mjs
  (editor+upload: panel mounts, paints on top via elementFromPoint, app-history subtab
  active) + regression sweep (howcontribute, cl-salmon, login-clean-reload 6/6,
  sidebar-cont-e2e, analysis-print-complete) all OK. Verified live: app.js 852,595 bytes.
- HARDREFRESH-001 — CLOSED AS ALREADY-FIXED: the in-source hardening (fire-and-forget
  cleanup + 1.2s forced reload + replace fallback) verifies green headlessly INCLUDING
  with a controlling service worker (`pwa/test/diag-hardrefresh.mjs`, committed). The
  reopened report most likely observed SETTINGS-NAV-Z-001 (no settings modal mounted in
  the editor route pre-1.50.355). ACTIVE_BUGS updated; owner to re-confirm on device.
  No code change.
- PRIVACY-FAB-FLICKER-MOBILE-001 + PRIVACY-DEMO-001 1.50.356 — ROOT CAUSE (probed, not
  guessed): the islands PreviewToolbar's applyPreviewActions hides the NOT-YET-MOVED
  privacy FAB with an inline !important triple (display/visibility/pointer-events);
  when it wins the race before topbar-tools-347 relocates the pill, the inline hide
  beats the stylesheet visibility lock and sticks until a fresh node ("toggling the
  editor"). Also the pill's ONLY mount host was the overlay stack, so with the overlay
  absent (demo/mobile/Settings) a wiped pill never came back. Fixes: 347's
  stylePrivacyForTopbar clears the inline triple every sweep (the moved pill is exempt
  from the islands hide, so no ping-pong); privacy-led's ensureFab falls back to
  mounting straight into .antcv-top-tools. Test: diag-privacy-pill.mjs (mount visible
  on desktop+mobile-390px, recreated after React wipe, recreated WITHOUT overlay,
  resize-triggered hide pass cleared, 30-tick flicker probe = 0). BOOT-SMOKE OK. No
  app.js change. PRIVACY-DEMO-001 in TRUE demo mode still needs the owner's eye, but
  the only known kill mechanisms are fixed.
- DEMO-TOGGLE-001 1.50.357 — NEW sidecar `antcv-demo-toggle-357.js`: "ACCOUNT MODE"
  Demo⇄Paid toggle injected at the top of Settings → Standard → Account. Click calls
  window.AntcvSetUserMode (bridge-337 relay write), mirrors 'antcv:user-mode-cloud'
  (+meta source:settings-toggle), dispatches antcv:user-mode-reconciled, shows a
  reload hint. Observer re-injects after React wipes. Test: diag-demo-toggle.mjs
  (render, click spy on AntcvSetUserMode, LS+event+hint, wipe re-injection) OK.
  BOOT-SMOKE OK. No app.js change.
- GEN-UNSOL-002 1.50.358 — three layers: (1) schema example now DEMANDS JD-grounded
  meta.company/meta.role ("<EXACT employer name copied from the JOB DESCRIPTION —
  REQUIRED when a JD is present…>" instead of the empty-string example that invited
  empty output); (2) kernel-completeness-290 enforces the contract as the output
  normaliser — the fetch wrap records whether a generation body carries the real JD
  block (STRICT value-initial marker ':"JOB DESCRIPTION:' so a JD pasted into
  Additional Signals can never false-arm against the no-JD forced-empty rule), and a
  JD-armed parse with empty/unsolicited/placeholder meta throws PartialResponse →
  provider retry with addendum rule 7; (3) app.src.js __llmCo scrubs placeholder
  echoes so they can't render or count as a named employer. Unit test
  genunsol-meta-contract 8/8 (vm-loaded real sidecar, arm/disarm/false-arm paths);
  full unit 87/87. Terser rebuild gates passed (853,023 bytes, head OK, 0 use-strict).
  Regression sweep (settings-front, howcontribute, privacy-pill) OK.
- PERF-003 / PERF-004 1.50.359 — PERF-003: ee()'s failover ladder is capped at 2
  providers for the owner-confirmed MECHANICAL tasks (extract, extract_pdf, parse_jd,
  compress, fix_orphans); quality-critical tasks keep full width; forceProvider and the
  main generation's OUTER forced-provider retry ladder are unaffected, so resilience is
  preserved while a slow internal 4-provider cascade can no longer stall a mechanical
  step. PERF-004 (perf doc §4 "skip redundant refinement"): the post-generate
  tightening compress call is SKIPPED when the draft is already within the budgets the
  call itself enforces (profile ≤400 / work-style ≤200 / first-page bullets ≤130 /
  continuation ≤90 chars; empty fields never skip) — saves one full LLM round-trip per
  generate when the model wrote tight. Unit perf-mechanical-trim 6/6 (mirrored
  predicates + source-marker assertions); full unit 93/93. Terser gates passed
  (853,595 bytes). Sweep: settings-front, howcontribute, llm-health-seed OK.
  NOTE: PERF-004's "fixed 3×" enrich↔compress cycle from the 2026-06-05 doc no longer
  exists as a literal loop in current source (counts came from per-section telemetry);
  the within-budget skip implements the doc's §4 lever directly.
- PB-WORKER-CONT-HEADER-001 — CLOSED AS RESOLVED-BY-REWORK + regression-locked: drove
  the DEPLOYED docx-worker bundle in node with outcomes+experience and a role on
  page 2 — page 2's main column shows exactly one "PROFESSIONAL EXPERIENCE (Cont.)"
  heading, zero stray "SELECTED OUTCOMES", zero doubled plain heading (the 1.14.39
  per-page tables + 1.14.30 separator removed the merge that caused it).
  diag-twocol-ownerlike.mjs extended with the heading assertions (and a SELECTED
  OUTCOMES section in its payload); diag-twocol-paged still OK. No worker code change,
  no deploy needed. ACTIVE_BUGS updated.
- RERENDER-STORM-001 — CLOSED AS RESOLVED + regression-locked: built the
  mutation-source probe as a committed headless diag (`diag-rerender-storm.mjs`,
  thresholds total<30/s + worst<10/s + 0 errors). Steady state on the current build:
  desktop 3 mutations/s / 7.2 rAF/s; mobile-390px 8.2 / 28.2; worst source 1.8/s —
  vs the historic 150+/s flood. The 1.50.80–85 damper rounds hold; no remaining pump
  found, no code change. ACTIVE_BUGS updated.
- APP-SENTENCE-STYLE-001 / SPECIALISATION-EDIT-001 — CLOSED AS ALREADY-FIXED
  (v1.50.105/106 in candidate-preview-editor-341), verified headless and
  regression-locked by `diag-candidate-header-edit.mjs`: CL application sentence
  host adopts the template's exact color/font (matches the hidden original anchor,
  white on the dark header, never default black) with all three spans editable;
  CV specialisation line is contenteditable and an edit persists to meta.subtitle.
  No code change.

### Morning-feedback round (owner 2026-06-11)

- EDITOR-GEAR-UNDO-001 1.50.360 — the editor top bar had NO visible Settings entry and
  NO undo control (settings only had the hidden route hook; undo only Ctrl+Z, which the
  global handler also ignores inside editable fields). Added ⚙ (calls
  _antcvOpenSettings) and ↶ (calls the existing xr undo with dr-count tooltip,
  disabled when the stack is empty) as the last children of .antcv-top-tools
  (app.src.js, terser rebuild gates passed: 854,395 bytes, head OK, 0 use-strict).
  Test diag-editor-gear-undo.mjs: both buttons visible, undo disabled on empty stack,
  ⚙ click opens Settings ON TOP (elementFromPoint). Buttons confirmed present at
  390px mobile too.
- DEMO-TOGGLE-ADMIN-001 1.50.360 — ACCOUNT MODE toggle is now ADMIN-ONLY (owner
  directive): the sidecar reads is_admin from localStorage 'serverConfig:v1' (the
  app's persisted /config) and tears down any pre-resolved block for non-admins.
  diag-demo-toggle.mjs extended: admin path full-green + non-admin sees no toggle.
- ANALYSIS-MOBILE-SCROLL-001 1.50.360 — ROOT CAUSE (probed at 390px): the mobile
  bottom sheet is FIXED ~280px with overflow:hidden, and the injected 356/360 blocks
  are flex SIBLINGS of the app's flex:1 inner scroller — the scroller collapsed to
  28px and the export row was clipped outside the hidden sheet (report unreadable,
  export unreachable). Fix in analysis-report-pdf-360: when the sheet shows the
  analysis tab it gets .arx-mob-scroll — the SHEET scrolls (overflow-y:auto
  !important beats inline styles) and children take natural height; disarmed on
  other tabs. diag-analysis-mobile-scroll.mjs: usable window 279px (was 28), full
  2,541px report scrollable, "⬇ Download analysis (PDF)" reachable and in view.
  Export content completeness was already covered (analysis-print-complete still OK).
- TIER B: INDENT-EXPORT-PARITY 1.50.361 + docx-worker 1.14.47 — the Advanced indent
  sliders now drive the export: docx-client forwards mainEdgeIndent/bulletIndent
  (numbers, clamped 0–60 px) in payload.style; the worker converts px→DXA (×15) for
  the main bullet numbering indent (default 14px=210, identical to BULLET-EDGE-001)
  and the main-cell edge margins (default now 150 DXA = the preview's 10px; the old
  144 constant was a 0.4px parity drift). Sidebar bullets and all other cells
  untouched. Tests: diag-indent-parity.mjs (live worker round-trip: default 210/210 +
  edge 150, custom 24px→360/360 + 20px→300), unit indent-controls extended (payload
  forwards numbers, coerces strings, drops out-of-range) — full unit 95/95; worker
  battery green (twocol-ownerlike, twocol-paged, cl-list-cont 4/4, cv-table-width 4/4).
  docx-worker 1.14.47 DEPLOYED via deploy.yml (run 27332376997, success); live /schema
  carries both tokens.
- TIER B: PREVIEW-PDF-PARITY-001 prep 1.50.362 — FLAGGED OFF by default. Discovery:
  the ledger's "re-point Vi to worker geometry" already landed at 1.50.296 (466px /
  14px); the REMAINING drift is that 466 assumes the legacy fixed SIDEBAR_W while the
  worker ≥1.14.41 derives the main column from the live sidebar_ratio (SB slider) and
  ≥1.14.47 the edge margins from mainEdgeIndent. The estimator now computes the
  ratio-aware width ((11906×(1−ratio) − 2×edge×15)/15 ≈ 512px at defaults) ONLY when
  localStorage['antcv:parity-estimator']='1'; default behavior byte-for-byte 466.
  Owner validation: flip the flag, generate, compare preview↔PDF line breaks/orphans.
  Unit mirror tests added (512 default, slider/clamp cases). Terser gates passed
  (854,643 bytes). NOT auto-enabled per the TIER B don't-finalize-blind rule.
- TIER B remaining, NOT attempted (per plan §3/§4): PHOTO-SIDEBAR-BRIDGE-001 (design
  build, needs owner direction), PREVIEW-PDF-SIDEBAR-GEOM-001 owner look-check,
  islands-rebuild items (WIZARD-LANG-SELECTOR, nordic-unsolicited PWA trigger).
- BULLET-MARKER-OFFSET 1.50.363 + docx-worker 1.14.48 (owner export review
  2026-06-11: "bullets a bit more to the right, text stays") — the export markers
  were flush at the column edge (BULLET-EDGE marker-at-0). The numbering first-line
  now sits at 90 DXA (6px) with the text hang unchanged (default 210/14px; custom
  bulletIndent keeps marker at 6px). Sidebar bullets same. ALSO guarded a 1.50.361
  side effect: the untouched preview bulletIndent default (24) is no longer
  forwarded — it would have shifted the export TEXT from 14px to 24px on the next
  export, the opposite of "text stays". Tests: diag-indent-parity updated
  (210/120 + 360/270, marker invariantly at 90 DXA), unit indent-controls 9/9,
  twocol-ownerlike OK, BOOT-SMOKE OK. Owner re-export to confirm the look.
- ROUND 2 (owner 2026-06-11) 1.50.364 + docx-worker 1.14.49:
  - MARKERS AT 3PX everywhere (export 6px was too much, preview 10px wrong too; CL
    included): preview textIndent 10−bi → 3−bi (5 sites), worker hanging = left−45
    (main + sidebar numbering). diag-indent-parity updated (210/165, 360/315, marker
    invariant 45 DXA); indent-controls mirrors re-locked to the REAL src formula.
  - PRIVACY-LED-DOCWORKER-001: the pill flipped to "Demo proxy (3 calls)" because
    the DOCX/PDF export worker is a *.workers.dev host whose /generate path matched
    the LLM classifier. Document workers (ANTCV_DOCX_WORKER / C2PA global + the
    docx-worker./c2pa-worker. name prefix) are now exempt; unknown workers.dev hosts
    still classify level 2 and direct APIs level 3 (asserted in diag-privacy-pill).
  - EDU-MD-BOLD-001: literal **…** around degrees (model emits markdown for the
    "bold degree label" prompt rule) — stripped at render in the preview education
    case, in the worker renderEducation deg/sch normalize, and in the generation
    scrubber `a` for data hygiene.
  - STICKY-SETMENU-SPAM-001: preview-shell-sticky now acts ONLY in the editor route
    (localStorage step gate) — the set-menu unhide↔hide ping-pong (endless console
    warnings + rAF churn) is gone; antcv-overlay owns the set menu as designed.
  Gates: terser rebuild OK (854,718 bytes), BOOT-SMOKE OK; battery green
  (indent-parity, privacy-pill incl. classify, howcontribute, candidate-header).
- TABLE-WRAP-PARITY-001 docx-worker 1.14.50 — MEASURED the core-competency
  divergence instead of guessing: headless preview probe at 100% A4 gives table
  512px, Focus Area col 153px (ratio 0.299), cell padding 6px, expertise LEFT —
  the worker used ratio 0.326, 8px margins, JUSTIFIED, leaving the export's
  expertise text column ~5% narrower (justify also produced the word-gap rivers in
  the owner's screenshot). Worker defaults now match the measurement: ratio 0.30,
  margins 90 DXA, expertise cells LEFT. Forwarded tableRatio/tableWidth still win.
  Residual: export font 10pt (13.33px) vs preview 13px — a ~2% chars-per-line
  delta that can still flip a single word on long cells; if the owner's re-export
  still diverges, the next lever is aligning the table font scale. Locked by
  diag-cv-table-width 7/7 (ratio/margins/alignment assertions).
- CONCURRENCY NOTE: origin/main moved twice mid-round (PR #292
  sidebar-shrink-reclaim; PR #293 = this session's own 1.50.364 merged from the
  sibling branch). Both merged (never reset); PR #292's sidecar got the missing
  ?v= cache-bust. The first 1.14.49 worker deploy ran from pre-merge main; the
  table-parity build redeploys as 1.14.50.
- MARKER-INHERITED-INDENT-001 1.50.365 (owner: "preview now has no markers and the
  2nd part of the line is in indent") — probe found the ▪ marker span at width 0:
  CSS text-indent INHERITS into inline-blocks, so the row's negative indent
  re-applied inside the marker span, collapsing its box and painting the glyph left
  of the row (this also explains why preview markers always hugged the edge before).
  BM() now sets textIndent:0 on the marker span AND sizes its box from the 3px
  first-line position to the text hang (width = bulletIndent−3), so the first
  line's text and the wrapped lines are BOTH flush at bulletIndent — the exact Word
  model (probe: ▪ at x=3, 21px box, text at x=24 = hang). Battery green
  (boot-smoke, howcontribute-preview, settings-front).
- PHOTO-SIDEBAR-BRIDGE-001 (PREVIEW HALF) 1.50.366 — bridge mode
  (photoPosition='band-overlap', CV two-column, photo present) now does the real
  design, not just the palette: the candidate band is SPLIT (text inset by the
  sidebar width = the photo cell) and the medallion hoists by Zo/2+8 so its
  MIDLINE sits exactly on the header/sidebar seam — top half on the navy band,
  bottom half on the brighter bridge sidebar. Default 'sidebar-top' is untouched.
  diag-photo-bridge.mjs asserts midline-on-seam (0px deviation), band split ≈
  sidebar width, and the control mode unchanged. DOCX half deliberately WAITS for
  the owner's look at the preview before mirroring the geometry into the worker
  (per the TIER B don't-finalize-blind rule).
- PHOTO-BRIDGE-LIVE-001 1.50.367 (owner: "changed to sidebar bridge nothing
  happened") — the ◐ Sidebar bridge button is a SIDECAR that only wrote
  localStorage; React's photoPosition state (er) reads that at boot, so the
  native bridge render never engaged until a reload. Fixes: (1) app.src.js
  exposes window._antcvSetPhotoPosition (live tr() + persist); (2) the bridge
  button calls it (localStorage fallback kept; synthetic storage event still
  fires); (3) antcv-photo-position.js's own band-overlap CLONE path is retired
  (it targeted the old table preview's findSidebarTd and never stuck —
  PHOTO-POSITION-196) — the original photo stays visible and the native render
  owns the straddle. diag-photo-bridge extended with a LIVE-SWITCH pass
  (no-reload engage, midOnSeam 0, persisted) — OK.
- PHOTO-BRIDGE ROUND 2 1.50.368 + docx-worker 1.14.51 (owner approvals + asks):
  - EQUAL AIR: a commit-time ref measures the band and sets the medallion's
    bottom gap = top gap (bandHeight − size/2) — probe shows 34/34 regardless of
    band content height.
  - SLIDER-STUCK root cause: a layout effect auto-resized the photo from the
    sidebar content height ON EVERY RENDER (clamp 80–155 → the owner's 129) —
    any drag snapped back. A user-set photoSize now wins, bridge mode skips the
    auto-sizer, and Zo restores from storage at boot.
  - NATIVE BRIDGE SIZE: slider ×1.3 (capped 220) in bridge — 120→156 by default.
  - LEFT FLOW + ONE-LINE CONTACT: band text starts 28px left of the seam; the
    contact line is nowrap at ×0.88 font in bridge.
  - PDF HALF (1.14.51): bridge exports natively — the header row SPLITS on the
    page grid (sidebar-width photo zone + text cell, small left margin), the
    medallion is a FLOATING image anchored on the sidebar's first paragraph with
    a negative vertical offset (midline on the row seam), the anchor reserves
    the bottom half + 14px in the flow, the client forwards the effective
    photoSizePx (slider×1.3), and the bridge contact shrinks ×0.88.
  Tests: diag-photo-bridge (equal gaps 34/34, 1.3× size 80→156, left-flow, live
  switch, normal untouched) + worker diag-photo-bridge-export (split header,
  wp:anchor with negative posOffset, 156px extent, 1380-twip reserve, control
  inline) + full worker battery green. Owner: re-export to eyeball the PDF
  straddle (LibreOffice renders floating images; position may need a nudge).
- BRIDGE ROUND 3: CONTACT-SQUEEZE 1.50.369 + docx-worker 1.14.52 (owner: "decrease
  the spaces between contact details to 1 and make more tricks to squeeze on one
  line") — bridge contact separators are now " • " (one space; default mode keeps
  the wide ones). Preview: a commit-time ref SHRINK-TO-FITS the contact font
  (−0.25px steps, floor 8.5px, exact scrollWidth measurement) + letterSpacing
  −0.1px; ellipsis only as last resort. Worker: same 1-space separators + a
  width-fit font estimate (usable cell pt / 0.55×len, clamp [7pt, 0.88×contact]),
  CV-only. Locked in diag-photo-bridge-export (1-space separators in bridge, wide
  in control) — 9/9 checks.
- PROD-STUB-002 INCIDENT (2026-06-11, during the 1.50.369 push): a concurrent
  session's commit f39705b ("fix(HARDREFRESH-001) … hardrefresh-force-349")
  REPLACED pwa/index.html and pwa/sw.js with literal __INDEX_PLACEHOLDER__ /
  __SW_PLACEHOLDER__ stubs and it deployed — production served a 21-byte page.
  Detected via the merge conflict (this session's push was rejected, the merge
  surfaced the placeholders), restored within minutes by resolving the merge with
  the REAL files (e644c6b) and letting Pages redeploy. Verified live: index.html
  42,125 B, sw.js 5,203 B, app.js 855,962 B, TARGET_VERSION 1.50.369. The
  upstream commit's additive sidecar pwa/antcv-hardrefresh-force-349.js is kept
  ON DISK but NOT loaded (its index.html wiring was lost in the placeholder; the
  authoring session must re-wire it against the real index.html). docx-worker
  1.14.52 re-deployed from the restored main (the earlier dispatch pre-dated the
  merge). NOTE for all sessions: this is the second placeholder-commit incident
  (see PROD-STUB-001) — never commit generated/placeholder file contents; the
  night-run §1.2 size gates exist exactly for this.
- PHOTO-POSITIONS-NATIVE-001 1.50.370 (owner: "only sidebar top and sidebridge work
  in preview, not even hidden") — every other photo position relied on the
  photo-position CLONE sidecar, which targets the retired table preview and never
  stuck (PHOTO-POSITION-196). ALL positions now render natively in app.js:
  hidden (no render), sidebar-top, sidebar-bottom (after the page-1 sidebar
  sections), header-left/right (82px medallion floated in the band ≈ export's
  0.85in), main-left/right (115px floated at the main-column top ≈ export's
  1.2in), band-overlap (the bridge). The clone sidecar is now a cleanup-only shim
  (clears stale clones, never hides/moves the photo). New diag-photo-positions.mjs
  live-switches through all 8 modes via _antcvSetPhotoPosition — 8/8 OK;
  diag-photo-bridge still fully green.
- BRIDGE-EXPORT-NOT-VISIBLE follow-up: the LIVE worker (1.14.52) verified emitting
  the bridge DOCX (anchor + split header) via a direct POST — the owner's export
  simply predated the worker deploy. Re-export to confirm.
- POSITIONS ROUND 2 1.50.371 (owner review of 1.50.370):
  - PDF-BRIDGE VERIFIED END-TO-END: drove the LIVE /generate-pdf (cloudconvert/
    LibreOffice) with bridge payloads — single AND multi-page — and parsed the PDF
    content stream: the 117pt medallion sits at topY 19.9pt, midline exactly on the
    band/sidebar seam. The pipeline is correct; the owner's bridge-less PDF predates
    the 1.14.51/52 worker deploys. Re-export.
  - MAIN-PHOTO SIDE-BY-SIDE: with a main top photo, every [data-sid] section is a
    flow-root (CSS keyed on data-antcv-main-photo) — heading rules STOP before the
    figure and the text edge is uniform (no crescent), matching the export's photo
    ROW table (which never wrapped). Locked in diag-photo-positions (first section
    rect must not overlap the figure).
  - BRIDGE PALETTE RETIRED: band-overlap uses the standard sidebar fonts/colors
    (owner: "fit the other positions"); BRIDGE-SIDEBAR-PALETTE shading removed.
  - RENAMES + 4 NEW POSITIONS (preview-first): picker labels now "Main top
    left/right"; new main-left-bottom / main-right-bottom (medallion after the
    page-1 main sections, pinned to the side) and bridge-middle / bridge-bottom
    (medallion straddling the VERTICAL sidebar/main seam at mid-height / bottom).
    diag-photo-positions extended to 12/12 modes. EXPORT halves for the four NEW
    positions are NOT yet implemented (worker normalises unknown positions to
    sidebar-top) — they ship after the owner approves the preview looks, per the
    preview-first pattern.

## Morning questions

(append owner-validation questions here; ask only if extremely mandatory)

None blocking. Quick owner spot-checks when convenient (all shipped + headless-verified;
these are the eyes-on confirmations the harness can't do):
1. Settings now opens ON TOP from the editor (⚙ / History → "Open in Settings →") — 1.50.355.
2. HOW I WOULD CONTRIBUTE bullets render in the preview and survive reload — 1.50.354.
3. Privacy pill stays in the top bar on your phone (no disappearance) — 1.50.356.
4. One tailored generate with a real JD: application header must show the JD company/role
   (never "Open Application — Unsolicited") — 1.50.358.
5. TIER B (unchanged from §3): PREVIEW-PDF-SIDEBAR-GEOM-001 look-check on 1.50.353/1.14.46.

## Round 4 (1.50.372) - crescent fixes + 3-hour ride start

Owner round-4 feedback (with red-marked screenshot) fixed and shipped:

- MAIN-TOP CRESCENT (1.50.372): the main top left/right float now carries
  shape-outside: circle(50%) + shapeMargin 8, so body text hugs the photo's
  circular rim (the red-marked mismatch). Section HEADINGS stay flow-root via
  CSS keyed on data-antcv-main-photo (first child of each [data-sid]), so the
  heading underline rule still stops before the figure. diag-photo-positions
  main-left/right now asserts circle shape-outside + heading clearance.
- BRIDGE-MIDDLE/BOTTOM DUAL CRESCENTS (1.50.372): the vertical-seam medallion
  now pushes text away as crescents in BOTH columns. Two zero-height float
  spacers (data-antcv-bridge-spacer="sb"/"main") sit at the top of sidebar and
  main; the medallion's commit ref measures its own midline and gives each
  spacer height=min(cy+R, columnHeight) plus shape-outside: circle(Rpx at
  100%/0% cy px). Text in both columns wraps the half-circle. Locked in
  diag-photo-positions (spacer circle centres must align with the medallion
  midline +/-12px).
- DISPLAY-FLEX WIPE REGRESSION (root cause): antcv-photo-position.js
  (cleanup shim) still called setOriginalVisible(original, true), which wrote
  display:'' onto the medallion's PARENT - for vertical-seam bridges that
  parent is the flex page-row itself, so both columns stacked vertically. The
  shim now only clears stale clones; it never touches display.
- PHOTO AUTO-SIZER SKIP EXTENDED: the content-driven photo auto-sizer now
  skips bridge-middle/bridge-bottom too (it clamped Zo to 80 in bridge modes);
  user-set photoSize already won since round 3.

Gates: terser rebuild, node --check, head=(()=>{const , 0x use strict,
app.js 858,313 B / src 2,056,032 B, BOOT-SMOKE OK, PHOTO-POSITIONS 12/12 OK,
PHOTO-BRIDGE OK. Cache-bust trio applied (app.js?v=1.50.372-crescents,
photo-position ?v=1.50.372-no-display-touch, CACHE antcv-1.50.372,
TARGET_VERSION 1.50.372, 1.50.371 -> STALE_VERSIONS).

Owner directive 2026-06-11: "go for another autonomous ride - implement every
feature and bug you can in the next 3 hours." Queue: worker export halves for
the 4 new photo positions (1.14.53), then ACTIVE_BUGS.md / FEATURES_REGISTRY.md.
## Round 5 (1.50.373 + docx-worker 1.14.53) - export halves + the REAL bridge-export bug

- EXPORT-PHOTO-POS-CLAMP-001 (root cause found): antcv-docx-client.js's
  readPhotoPosition VALID set lagged the picker. 'band-overlap' was MISSING,
  so the client clamped every bridge export to sidebar-top BEFORE the payload
  left the browser - the worker was never the problem (my live-worker probes
  bypassed the client, which is why they looked correct). 'none' (the
  picker's Hidden value) was missing too, so a HIDDEN photo still exported.
  VALID is now a superset of the picker; 'none' maps to 'hidden'.
- photoSizePx forwards for EVERY visible position now (band-overlap keeps the
  1.3x native scale); worker applies it to sidebar + bridge positions
  (header stays 82px, main stays 115px - preview parity).
- PHOTO-POSITIONS-EXPORT-001 (worker 1.14.53): export halves for the four
  1.50.371 positions + a crescent-parity rework of main top:
  - main-left/right: FLOATING image (wp:anchor, wrapSquare bothSides,
    column-aligned) replaces the photo-row table - text runs beside the photo
    and reclaims the full column width below it, like the preview crescent.
  - main-left/right-bottom: inline medallion after the main sections, pinned
    left/right.
  - bridge-middle/bottom: floating medallion page-anchored on the vertical
    seam (posOffset = sidebarW - px/2 in EMU), vertically page-centred /
    24px above the page bottom, wrapSquare bothSides pushes BOTH columns'
    text away (the export's dual crescents).
  - 'none'/'hidden' normalised in the worker too (defence in depth).
- Tests: workers/docx-worker/test/diag-photo-positions-export.mjs (9/9:
  geometry offsets, wrap mode, inline-vs-anchor, hidden exports nothing,
  band-overlap + photoSizePx regression guards); pwa/test/unit/
  photo-position-forward.test.mjs (5/5, imports the real client module with a
  localStorage shim and locks the full picker->forward mapping). Full worker
  diag suite 7/7 files OK; unit suite 103/103.
- Cache-busts: docx-client ?v=1.50.373-pos-clamp, CACHE antcv-1.50.373,
  TARGET_VERSION 1.50.373 (+1.50.372 staled). app.js unchanged this round.
## Round 6 (1.50.374) - EXPORT-PAGE2-001 + export-preview page selector

- EXPORT-PAGE2-001 (diagnostic-first, headless repro): the export-preview
  modal's iframe clone DOES carry all content (2 page-rows, page-2 text
  present) - the defect was the PRINT path. The srcdoc print CSS keyed page
  breaks on legacy marker attributes (data-antcv-page-break-284 etc.) that
  the NATIVE page-row pagination never sets, so the print engine
  re-paginated the tall paper arbitrarily (mid-section cuts, 'page 2
  missing'). Also the title counted PAPERS (always 1 now), so multi-page CVs
  showed no page count.
  FIX (antcv-pdf-preview-gate.js 1.50.374-page2-print): (a) print CSS breaks
  on .antcv-page-row + .antcv-page-row and clamps each row to one sheet
  (max-height 297mm, overflow hidden); (b) @page margin drops to 0 when the
  clone carries native page-rows (each row IS a full A4 sheet - the 10mm
  margin made every row spill a sliver onto a blank page); legacy single-box
  render keeps 10mm + marker rules; (c) title and CV<->CL rebuild count
  page-rows via countPages().
- EXPORT-PREVIEW-FEATURES-001(c): page-selector chips in the modal (Page: 1
  2 ...) that smooth-scroll the iframe to that page-row. Renders only when
  >1 page; rebuilt on CV<->CL toggle.
- INCIDENT (caught before commit): a PowerShell Get-Content/-replace
  rewrite mojibake'd the sidecar's UTF-8 (PS 5.1 ANSI read). Restored from
  git, re-applied all edits with the Edit tool, 0 mojibake lines verified.
  Rule: only [IO.File]::ReadAllText/WriteAllText or the Edit tool on pwa/*.
- Test: pwa/test/diag-export-preview-pages.mjs (8/8: row count, title,
  iframe rows + page-2 content, row-break CSS, zero sheet margin, 2 chips,
  chip-2 scroll). Cache-busts: gate ?v=1.50.374-page2-print, CACHE
  antcv-1.50.374, TARGET_VERSION 1.50.374 (+1.50.373 staled).
## Round 7 (1.50.375) - SHARE-TARGET-JD-URL-001 shipped

- manifest.json declares a GET share_target (params shared_url/shared_text/
  shared_title), so the installed PWA appears in the Android share sheet for
  job links. Manifest link cache-busted (?v=1.50.375-share-target).
- New sidecar antcv-share-target-jd-375.js (index.html, after jd-watch; added
  to the sw SHELL): extracts the first http(s) URL from shared_url else
  shared_text, rejects own-origin, strips the params from the address bar
  (history.replaceState), stashes the URL in sessionStorage (10-min TTL,
  survives the login redirect), and SYNCHRONOUSLY routes step->'upload' (the
  JD intake - the only step that renders the JD URL field) before app.js
  reads localStorage.step. Then polls for the input and fills it through the
  NATIVE value setter + input event so the controlled React input commits;
  focus + scroll. Never auto-fetches; one tap on Fetch JD remains.
- Discovery cost: the first probe filled nothing because the JD URL input
  exists ONLY on the upload step (Nt state, default 'upload') - the editor
  and analysis tabs never render it. Hence the step rewrite.
- Test: pwa/test/diag-share-target-jd.mjs (3/3: editor boot + shared_url ->
  intake shown, input filled, bar cleaned; shared_text first-URL extraction;
  own-origin rejected with no step hijack). Boot-smoke OK.
- Note: share_target fires only for an INSTALLED PWA on Android/Chromium;
  iOS unsupported. Owner verify: install -> share a LinkedIn job ->
  pick AntCV -> URL lands in the JD field.
## Round 8 - PHOTO-PREVIEW-ALT-PERSIST-001 closed + tracker/registry sweep

- PHOTO-PREVIEW-ALT-PERSIST-001 RESOLVED by the 1.50.370-372 native rework
  (the clone/finder machinery the bug lived in is gone). Cold-boot
  persistence locked by pwa/test/diag-photo-position-persist.mjs (3/3:
  bridge-middle on the seam, main-right circular wrap, none -> no image,
  all on first paint with the position read from localStorage).
- Tracker + registry sweep: new 2026-06-11 section atop ACTIVE_BUGS
  (EXPORT-PHOTO-POS-CLAMP-001 / PHOTO-POSITIONS-EXPORT-001 / EXPORT-PAGE2-001
  / SHARE-TARGET); FEATURES_REGISTRY closes SHARE-TARGET +
  PHOTO-SIDEBAR-BRIDGE, retires BRIDGE-SIDEBAR-PALETTE (owner direction).
- Live verifies this ride: app.js 858,313 B (1.50.372), worker
  1.14.53-photo-positions probed on the live /generate (bridge-middle
  anchor + seam offset + wrap correct), manifest share_target live.
## Round 9 (docx-worker 1.14.54) - PDF blank middle page + lost sidebar lines

Owner evening report on the Trackman PDF export: blank page in the middle,
REGULATORY CONTEXT first group jumped page in the PDF but not the preview,
Customer Change role crossed pages in the PDF but not the preview.

- Diagnosed from the owner's actual PDF (pdfjs per-page text dump): 3 pages,
  page 2 EMPTY (14 draw ops, zero text), and the sidebar's REGULATORY CONTEXT
  heading + 'Systems, safety and cybersecurity' group label on NO page at all.
- Live repro against the PDF endpoint with an owner-like 2-page payload: 5
  PDF pages, pages 2 AND 4 blank - every content page spilled. ROOT CAUSE:
  the per-page body-row minimums fill each sheet EXACTLY (header budget +
  13860 = 16838; PAGE_H-200). LibreOffice renders the candidate band + row a
  sliver taller than the budget, so every stretched 'atLeast' row overflowed
  its sheet; the row split, the empty tail became a blank page, and on page 1
  the split swallowed the last sidebar lines. Word tolerated the exact fill -
  only the PDF path broke. This is also the QA index's PDF-LAYOUT-002 (their
  'repeat the group header' theory was wrong - the label was destroyed, not
  un-repeated).
- FIX (1.14.54): PAGE1_BODY_MIN 13860->13260, CONT_BODY_MIN PAGE_H-200->
  PAGE_H-600. Deployed; live re-probe: 3 content pages, ZERO blanks, heading
  + group label + first item all on page 1. Trade-off: navy bar stops
  ~0.5-1cm above the page edge. diag-twocol-ownerlike updated to the new
  constants; structural suite green.
- The role/ASPICE early-jump is the export break-map (USABLE_PDF) being more
  conservative than the preview budget - the concurrent session's
  EXP-PREVIEW-GAP-001 fix (e50973f, merged + cache-busted this ride) targets
  exactly that. Owner: hard-refresh, re-export, compare again.
- Also this round: merged 14 upstream commits (VERB-LED/SCE/CA-006/
  JD-ANALYSIS-PRINT + the PDF-LAYOUT-002 probe that gutted only the DEAD
  generate.js); cache-busted their two un-bumped sidecars
  (candidate-preview-editor-341, print-iframe-preview).
## Round 10 (1.50.376) - BRIDGE-TABLE-001 + nightly feature run start

Owner: 'bridge middle is unable to handle core competencies' (medallion
painted across the CORE COMPETENCIES table) + 'start nightly run of feature
implementations'.

- BRIDGE-TABLE-001 root cause (probed headlessly): CSS tables don't wrap
  shape-outside floats, AND the medallion's commit ref measured geometry ONCE
  at React commit - BEFORE the pagination sidecars stretched the page-row -
  so the crescent spacers were sized against stale positions and the table
  ended up under the medallion (31px x 50px overlap on the last row).
- FIX (app.src.js, terser rebuild): the ref's geometry pass is now a
  re-runnable closure executed at commit + 450ms + 1400ms (it first RESETS
  the medallion to its natural position, then measures). With correctly
  sized float spacers the table clears below the medallion natively
  (float-avoidance); a y-relocation backstop moves the band to the nearest
  table-free y if a table still intersects it.
- Verified: pwa/test/diag-bridge-table.mjs (medallion/table overlapY 0, no
  cell in the band, both spacers sized+shaped); diag-photo-positions 12/12;
  diag-photo-bridge; boot-smoke - all green. Gates: head/use-strict/size OK
  (859,088 B).
- Cache-busts: app.js?v=1.50.376-bridge-table, CACHE antcv-1.50.376,
  TARGET_VERSION 1.50.376 (+1.50.375 staled).

Nightly feature queue (owner directive): PAGEBREAK-STYLE-OPTIONS-001,
EXPORT-PREVIEW-FEATURES-001(a), PROCESSING-QUEUE-INDICATOR-001 - same
guardrails, preview+export halves, tests every round.
## Round 11 (1.50.377) - EXPORT-PREVIEW-FEATURES-001(a): Analysis in the export modal

- The document-export modal now offers 'Analysis (PDF)' as a third
  quick-export. antcv-analysis-report-pdf-360.js exposes
  window.AntcvAnalysisReportExport (the existing branded report exporter -
  reads everything from localStorage, works without the Analysis panel) and
  window.AntcvAnalysisReportAvailable; the gate renders the amber button
  ONLY when a report exists and delegates the click.
- Tests: diag-export-analysis-button.mjs (2/2: renders + delegates with
  analysis data; hidden without), diag-export-preview-pages still 8/8.
- Cache-busts: gate ?v=1.50.377-analysis-btn, 360 ?v=1.50.377-export-hook,
  CACHE antcv-1.50.377, TARGET_VERSION 1.50.377 (+1.50.376 staled).

## Nightly handoff - next features in the queue

The remaining owner-specced features for the nightly run, in order:
1. PAGEBREAK-STYLE-OPTIONS-001 (3 advanced-style controls): (a) Keep
   continuation headlines (default ON; OFF = bare continuation, gate BOTH the
   preview '(Cont.)' segment titles AND the worker contSuffix path);
   (b) Repeat candidate header on every page (default OFF; preview page-row
   clone + worker headerRow on cont pages); (c) Page numbers off/top-right/
   bottom-right (preview corner overlay per page-row + worker footer
   PageNumber field). Persist on styleConfig (cloud-synced); forward via
   antcv-docx-client buildStyle.
2. PROCESSING-QUEUE-INDICATOR-001 (pink processing / yellow queue badges) -
   needs a lifecycle hook around the ee() dispatcher per section.
3. EXPORT-PREVIEW-FEATURES-001(b/d) - download dir (File System Access API)
   + ATS-format compare preview.
Guardrails unchanged (terser-only rebuilds, gates, cache-bust trio,
merge-dance, live verify, owner-visual items flagged).
## Round 12 (1.50.378) - PAGEBREAK-STYLE-OPTIONS-001 slice 1: settings UI

- Advanced styles gains a PAGE FLOW group (after INDENTS, same styleConfig
  partial-merge pattern): 'Keep continuation headlines' checkbox (default
  ON), 'Repeat candidate header on every page' checkbox (default OFF), and a
  'Page numbers' select (off / top-right / bottom-right). Keys:
  contHeadlines / repeatHeader / pageNumbers on the styleConfig (cloud-sync
  + reset with the style). NOTHING reads the keys yet - this slice is the UI
  + persistence only, so it is behaviour-neutral.
- NEXT SLICES (continuation): (1) preview - gate the '(Cont.)' segment
  titles on ya.contHeadlines !== false; per-page-row corner overlay for
  pageNumbers; slim candidate-header clone on rows 2+ for repeatHeader;
  (2) client - forward the 3 keys in antcv-docx-client buildStyle;
  (3) worker - gate contSuffix, repeat headerRow on cont pages, footer
  PageNumber field for top-right/bottom-right; (4) unit + headless tests.
- Gates: terser, head OK, 860,510 B, BOOT-SMOKE OK. Cache-busts:
  app.js?v=1.50.378-pageflow-ui, CACHE antcv-1.50.378, TARGET 1.50.378.
## Round 13 (1.50.379 + docx-worker 1.14.55) - PAGEBREAK-STYLE-OPTIONS-001 complete

All three page-flow options now render in PREVIEW + DOCX + PDF:

- (a) Keep continuation headlines (default ON). OFF = bare continuation:
  preview gates BOTH the section-heading '(CONT.)' block in Ce and the table
  continuation heading; worker gates every '(Cont.)' site (segment wrapper
  titles render '' -> renderSection's if(s.title) skips the heading; the
  text_bullets/foundation/experience/simple-list headingParagraph pushes are
  gated on ctx.style.contHeadlines === false).
- (b) Repeat candidate header (default OFF). Preview: slim navy strip (name
  + subtitle from localStorage) above page-rows 2+. Worker: fresh
  makeSlimHeaderRow() per cont table (name + subtitle, headerBg), cont body
  row min shrunk 900 DXA so the sheet never overflows (PDF-BLANK-PAGE-001
  rule respected).
- (c) Page numbers off/top-right/bottom-right. Preview: absolute corner
  number per page-row (data-antcv-page-number). Worker: footer part with a
  PAGE field (duck-typed {options:{children}} - the bundle's public
  Header/Footer classes are tree-shaken out but File.addSection only reads
  .options.children). EXPORT LIMITATION: top-right also renders via the
  footer - Word header parts serialize EMPTY through this bundle (verified:
  header1.xml came out with a stray empty paragraph), and a top-of-page
  number would sit on the navy band of the margin-0 pages anyway. Preview
  keeps the true corner. Revisit if the bundle is ever rebuilt un-shaken.
- mergeStyle passes contHeadlines/repeatHeader (bools) + pageNumbers (enum)
  without hex coercion; client forwards only when off-default.
- Tests: pwa/test/diag-pageflow-options.mjs (preview, 2/2 scenarios);
  workers/docx-worker/test/diag-pageflow-export.mjs (10/10); full worker
  suite 8/8 files; boot-smoke OK.
## Round 14 (docx-worker 1.14.56) - CL page numbers + registry sweep

- The CL (linear) export gets the same footer PAGE field as the CV when
  styleConfig.pageNumbers is set (clPgNumBlock in buildLinearDocument;
  footer margin 60). diag-pageflow-export extended to 11/11 (CL case).
- PROCESSING-QUEUE-INDICATOR-001 verified ALREADY IMPLEMENTED in source:
  Ce renders the yellow 'pending' / pink 'working|translating|debating'
  badge + tinted card (app.src.js ~3835-3905); bulk Compress/Enrich queue
  all targets yellow then flip each pink (~16491/16570/16595/17183/17364);
  generate/language mark all sids pending (~14864). Registry updated - only
  the CJLR-in-every-sub-subsection slice remains open.
- FEATURES_REGISTRY: PAGEBREAK-STYLE-OPTIONS-001 CLOSED (1.50.378-379 +
  1.14.55-56) with the top-right-via-footer export limitation documented.
## Round 15 (1.50.380) - EXPORT-PREVIEW-FEATURES-001(b): choose save location

- 'Ask where to save' checkbox in the export modal's actions row (renders
  only where the File System Access API exists - Chromium). Persists
  localStorage antcv:askSaveLocation.
- antcv-docx-client triggerDownload: when the flag is ON and
  showSaveFilePicker exists, the DOCX/PDF blob saves through the OS picker
  (folder + name); a user CANCEL aborts cleanly (no silent fallback
  download); any other picker error falls back to the classic anchor
  download. Default behaviour unchanged when OFF/unsupported.
- Test: pwa/test/diag-save-location.mjs (toggle renders, persists, picker
  API stubbed). Cache-busts: gate ?v=1.50.380-save-where, docx-client
  ?v=1.50.380-save-picker, CACHE antcv-1.50.380, TARGET 1.50.380.
- Owner verify owed: real Chromium save dialog on a DOCX export with the
  toggle ON. Remaining EXPORT-PREVIEW item: (d) ATS-format compare preview.
## Round 16 - CJLR gap analysis + final gate (window close)

- CJLR-everywhere inventoried: per-item cyclers ALREADY EXIST for
  labeled_list/education/list (antcv-item-align.js, loaded), per-part for
  FOUNDATION + HWIC, section-default via antcv-section-align.js; the WORKER
  honours every alignment path generically (paraAlignPath: content, intro,
  items.N(+.t), closing, hands_on, professionally, roles.N(.title/
  .bullets.N)); the client merges antcvItemAlignment -> item_alignment.
  ACTUAL residual gap (registry updated with implementation pointers):
  editor cyclers for EXPERIENCE role bullets (roles.ri.bullets.bi - export
  already wired) and table rows (items.i.t); both need data-antcv-item-row
  markers on those editor rows or a row-finder extension in
  antcv-item-align.js. The experience editor's bullet rows carry no marker
  today (preview spans at app.src.js ~4983 do carry the edit path).
- Final consolidated gate, all green: unit 103/103, BOOT-SMOKE, PHOTO-
  POSITIONS 12/12, EXPORT-PREVIEW-PAGES 8/8. Live verified this window:
  1.50.379 + 1.50.380 (gate 42,478 B + picker refs in the live client),
  workers 1.14.55 + 1.14.56 deploy success.

## Six-hour window summary (rounds 12-16)

Shipped: 1.50.379 (PAGEBREAK-STYLE-OPTIONS preview+forwarding complete),
1.14.55 (worker pageflow halves), 1.14.56 (CL footer page numbers),
1.50.380 (Ask-where-to-save picker). Closed/corrected in the registry:
PAGEBREAK-STYLE-OPTIONS-001, PROCESSING-QUEUE badge half (stale 'not
started'), EXPORT-PREVIEW-FEATURES-001 3-of-4. Open queue, in order:
(1) CJLR residual gap above; (2) EXPORT-PREVIEW (d) ATS compare - needs an
owner design call; (3) SPELL-ANNOTATOR-001; (4) APPJS-ID-SCHEME-UNIFY.
## Round 17 (1.50.381) - CJLR-EXPERIENCE-001: per-role + per-bullet alignment

Owner: 'keep releasing features.' Closed gap (1) from the round-16 CJLR
analysis - EXPERIENCE alignment, the last big section type without CJLR:

- app.src.js: the experience editor's role cards carry
  data-antcv-item-row='experience' + idx (the item-align sidecar's cycler
  anchor); the preview bullet rows carry
  data-antcv-row-path='roles.N.bullets.M' + data-antcv-role-path='roles.N'.
- antcv-item-align.js (1.50.381-experience): itemPathFor maps
  experience -> 'roles.{i}' (per-ROLE cycler on each role card, same UX as
  list items); applyAllAlignments adds a lookup chain - exact per-bullet
  key, then the exact marker, then the row's ROLE path, then the section
  default. Per-bullet keys (roles.N.bullets.M) are honoured if written
  (e.g. by hand or a future per-bullet UI).
- Export half was ALREADY wired end-to-end: the client merges
  antcvItemAlignment -> item_alignment and the worker honours
  paraAlignPath('roles.N') + ('roles.N.bullets.M'). No worker change.
- Test: pwa/test/diag-cjlr-experience.mjs (4/4: markers, role-key applies
  to both bullets, per-bullet beats role key, untouched bullet keeps
  justify). Regressions: photo-positions 12/12, pageflow-options, unit
  103/103, boot-smoke - green. Cache-busts: app.js?v=1.50.381-cjlr-roles,
  item-align ?v=1.50.381-experience, CACHE antcv-1.50.381, TARGET 1.50.381.
- Owner visual verify: the cycler button on each role card in the
  experience editor (injection is the sidecar's existing self-healing
  MutationObserver path; headless covers storage->preview application).
## Round 18 (1.50.382 + docx-worker 1.14.57) - LANG-ES-ZH-001: full es/zh export

Owner: 'include the full spanish and chinese support.' The writing engine +
PWA UI already supported en/da/es/zh; the EXPORT was the gap - the worker
422'd anything but en|da, and BOTH client payload builders clamped es/zh to
en, so Spanish/Chinese users always got English exports.

- Worker 1.14.57: VALID_LANG + /schema enum now en|da|es|zh. Localized
  strings: closing 'Atentamente,' / '(zh) cordial closing', name placeholder
  'Tu nombre' / (zh), and the continuation suffix is now ctx.contSuffix at
  EVERY site - the segment-wrapper titles and the
  text_bullets/foundation/experience/simple-list heading pushes hardcoded
  ' (Cont.)' and ignored the language (zh now renders the fullwidth (xu)
  suffix; en standardises on '(CONT.)', matching the preview).
- Client: antcv-docx-client buildPayload + the gate's fallback payload
  builder forward da/es/zh (unknown still clamps to en - no 422 risk).
- Tests: workers/docx-worker/test/diag-lang-es-zh.mjs (5/5: es + zh CL
  strings, zh CV continuation suffix, unknown-language 422 kept, schema
  enum); diag-twocol-ownerlike updated to the standardized '(CONT.)';
  pageflow/cl-list-cont/twocol-paged green.
- Owner verify: a zh export's PDF needs a CJK-capable font on the
  CloudConvert LibreOffice image (DOCX itself is fine - Word substitutes);
  if the PDF shows boxes, we add an explicit CJK font fallback next round.
## Round 18 addendum - live es/zh verification (worker 1.14.57 deployed)

Live probes against production: zh CL DOCX 200 with the Chinese closing +
body text; es CL DOCX 200 with 'Atentamente,'; zh PDF export 200 and pdfjs
extracts the Chinese body + closing from the CloudConvert PDF (proper
ToUnicode maps + embedded font subset - not a tofu fallback). Owner
eyes-on still recommended for the zh PDF's visual glyph quality.
## Round 19 (1.50.383 + docx-worker 1.14.58) - CJLR-TABLE-001: CJLR-everywhere CLOSED

- Diagnostic detour that mattered: my first generic implementation (item-align
  markers on the table) was STOMPED in the preview by
  antcv-core-competencies-row-controls-234.js - which turned out to ALREADY
  own per-row table CJLR (storage antcv.coreCompetencies.rowAlignment.v1,
  'row-<i>' over the full rows array, sweep applies to every cell ~ on each
  mutation). The preview/editor half existed all along; the GAP was export
  parity (the storage never left the browser).
- FIX: reverted the conflicting generic wiring (editor/preview markers +
  sidecar 'table' case - a NOTE in itemPathFor documents the ownership);
  buildPayload now forwards explicit row-N entries to the table section as
  item_alignment['rows.N'] (header row-0 + invalid values dropped); worker
  1.14.58 renderCompetencyTable applies them to the EXPERTISE cell
  (full-array index, jc center/right/justify; Focus cell stays left).
- Tests: unit table-align-forward 4/4 (real buildPayload with a localStorage
  shim), diag-cjlr-table (preview via the 234 sidecar), worker
  diag-cjlr-table-export (DOCX jc per row). Regressions green: boot-smoke,
  cjlr-experience, photo-positions 12/12, unit 107/107, cv-table-width 7/7.
- CJLR-everywhere is CLOSED: lists/education (item-align), foundation + HWIC
  (control sidecars), experience roles/bullets (1.50.381), table rows
  (this round) - preview AND export for all of them.
## Round 20 (1.50.384) - SPELL-ANNOTATOR-001 shipped

Owner directive: implement SPELL-ANNOTATOR-001 + DATA-PORTABILITY-CLOUD +
FEATURE-CONF-001 + APPJS-ID-SCHEME-UNIFY (this round = the first).

- New sidecar antcv-spell-annotator-384.js + vendored
  pwa/vendor/nspell.browser.js (nspell 2.1.5 bundled standalone with esbuild
  - the app.js identity-gate prohibition is app.js-only). Lazy: vendor +
  dictionary load on first eligible focus.
- Scope per locked spec: textareas/text inputs inside
  .antcv-editor-side-panel only (the expanded section editors render OUTSIDE
  their section rows - discovered by probe); excludes the preview, the
  analysis JD block, and JD/signals placeholders.
- Engine: Hunspell dictionaries (dictionary-en/-da/-es) fetched from
  jsDelivr on demand, cached in IndexedDB ('antcv-spell'); zh is a
  documented no-op (no usable Hunspell dict for unsegmented Chinese).
- UI: ghost-overlay mirror behind the field with red dotted underline
  spans; click -> popover with up to 6 suggestions (native-setter replace +
  input event so React commits) + 'Add to my dictionary'
  (antcv:userDict:{lang}).
- Toggles: antcv:spell:enabled ('0' off), per-language antcv:spell:langs;
  window.AntcvSpell { setEnabled, check }. Settings-panel toggle UI deferred
  (API + storage keys are the contract).
- Test: pwa/test/diag-spell-annotator.mjs (5/5 against a fixture dictionary
  via window.__antcvSpellDictBase). sw SHELL + CACHE antcv-1.50.384, TARGET
  1.50.384.
## Round 21 (1.50.385) - DATA-PORTABILITY-CLOUD shipped

- New sidecar antcv-orphan-cloud-persist-385.js: pushes corrected
  stylePackage/toneRegister to the existing relay PUT /api/prefs (the relay
  already routes both to D1 user_kernel.preferences) whenever the local
  values differ from the last-pushed marker (antcv:orphanSync:v1). Triggers:
  boot +5s and +20s (late OAuth), storage events on the two keys; 60s
  throttle; never pushes the known-orphan 'scandinavian' or empty values;
  same relay-base/token lookup as the consent-sync sidecar; PUTs pass the
  shrink guard like every prefs write.
- End-to-end verified headlessly (diag-orphan-cloud-persist.mjs, PUT
  intercept): clean values push ONCE with the marker recorded; seeding the
  ORPHAN proves the full loop - the login-loading-gate migrates toneRegister
  to nordic-minimal during boot and THIS sidecar persists the corrected
  value; the literal orphan never reaches the cloud. Boot-smoke OK.
- Architecture per the relay exploration: /api/prefs PUT routes
  stylePackage/toneRegister to D1 preferences (workers/access-relay
  src/index.js ~963-1226); the orphan migrations were localStorage-only.
## Round 22 (1.50.386) - FEATURE-CONF-001 shipped (v1 grounding heuristic)

- New sidecar antcv-confidence-overlay-386.js: '🎚 Confidence' toggle chip
  in the Analysis (Application) panel report row, default OFF
  (antcv:confidence:on). When ON, preview blocks tint by the app's
  confidence in their content: red < 40%, yellow 40-70%; hover lists each
  flagged sentence + the issue. Export output untouched.
- v1 scoring = deterministic GROUNDING CHECK (no LLM cost): every sentence
  scores against the candidate's own source facts (personalInfo + kernel
  token corpus); content tokens and NUMBERS absent from the source lower
  confidence ('numbers 47, 9999 not found in your source facts'). The spec
  planned a worker LLM self-check pass; the scorer is PLUGGABLE for it -
  window.AntcvConfidence.setScores(map) overrides per normalized sentence,
  so the LLM pass can land later without touching the renderer.
- Granularity: per preview BLOCK (lowest-confidence sentence colours it;
  tooltip itemizes) - style-only mutation, same decoration class as
  item-align; no DOM structure changes inside React-owned text.
- Test: pwa/test/diag-confidence-overlay.mjs (5/5: default off, fabricated
  bullet tints low with the issue, grounded text untinted, toggle-off
  strips, setScores override wins). Boot-smoke OK. CACHE antcv-1.50.386.
## Round 23 (1.50.387) - APPJS-ID-SCHEME-UNIFY shipped

The fourth feature of the owner's list. app.src.js's legacy package ids
unify with the registry's clean ids, mirroring the tone register's proven
self-migration pattern:

- __pkgMap (scandinavian->copenhagen-modern, copenhagen_executive->
  navy-executive, warm_terracotta->warm-terracotta, nordic_frost->
  nordic-frost, latam->pampas-contemporary) + __pkgNorm hoisted at the root
  component top; the stylePackage state INIT migrates the persisted value in
  place (default now 'copenhagen-modern'); BOTH cloud-restore sites
  normalize before ka()/u.set() - a stale cloud snapshot can no longer
  re-orphan the id (the root of PACKAGE-PALETTE-MIX-001).
- The va package map + xa swatch map re-keyed to clean ids (5 packages);
  the reset handler default updated. The islands picker already wrote clean
  ids - va[Sa] now MATCHES them, so the palette self-heal finally fires for
  islands-selected packages too.
- DATA-PORTABILITY-CLOUD (round 21) completes the loop: the migrated clean
  id is pushed to D1 preferences, so other devices converge.
- Build detour: app.src.js had CRLF line endings - multi-line PowerShell
  replaces needed CRLF separators (single-line ones masked it).
- Tests: NEW diag-pkg-id-unify.mjs (3/3: legacy migrates in place, clean id
  honoured, default clean). Regressions all green: photo-positions 12/12,
  pageflow, cjlr-experience, photo-bridge, bridge-table, unit 107/107,
  boot-smoke. NOT yet retired: the orphan-apply + loading-gate workaround
  sidecars (now no-ops for migrated users) - retire after an owner-confirmed
  clean cycle, per the registry's close-out note.
## Round 24 (1.50.388) - ROW-CONTROLS-DEDUPE-001 (owner: duplicated buttons)

Owner report: some editor lines carry DUPLICATED controls - two page
splitters on one two-part row makes no sense; Enhance/Fit (and CJLR outside
tables) must act on the row's FULL content.

- Diagnostic-first: headless boots with synthetic labeled_list / table /
  outcomes / WIB data all rendered CLEAN single control sets per row (one
  page, one fit, one enhance, one CJLR - each already operating on the full
  item: 'Enrich this item/row/outcome'). The duplication is data/state
  specific to the owner's live document (groups, real CL shapes, sidecar
  interactions), not reproducible synthetically.
- FIX (general, injector-agnostic): antcv-row-controls-dedupe-388.js
  dedupes by SEMANTICS - per item-row host (nearest ancestor containing the
  row's field), buttons are classed page-split/fit/enhance/cjlr/delete/
  visibility; the FIRST of each class stays, later ones hide
  (display:none + data-antcv-deduped, reversible). Nested fields make
  nested hosts so group headers never pool with their items. Editor side
  panel only; preview untouched; disable via antcv:rowdedupe:off='1';
  AntcvRowDedupe.report() lists what was hidden (the owner can paste it if
  the wrong button ever disappears).
- Tests: diag-row-dedupe.mjs A) synthetic duplicated row -> 2nd page + 2nd
  fit hidden, singles untouched, report names them; B) FULL APP boot with
  all the section types -> ZERO false positives (nothing deduped on clean
  native sets). Boot-smoke OK. CACHE antcv-1.50.388.
## Round 25 (1.50.389) - loose ends closed; backlog exhausted for solo work

- SPELL-ANNOTATOR Settings UI (the round-20 deferral): SPELLING block
  injected after the Settings PRIVACY providers box (the data-export
  anchor pattern) - master 'Spelling underlines in the editor' toggle +
  per-language EN/DA/ES checkboxes bound to antcv:spell:enabled /
  antcv:spell:langs, with the zh limitation noted inline. Spell suite
  still 5/5; boot-smoke OK.
- CL preview page numbers: probed - the CL preview renders ONE tall paper
  with no .antcv-page-row boxes (different pagination model), so a corner
  number per printed page cannot be drawn truthfully there. DOCX/PDF carry
  the CL page number (1.14.56); documented asymmetry, no hack shipped.
- Registry hygiene: FT-PHOTO-POS stale 'persistence still OPEN' note
  corrected; spell row updated for the shipped Settings UI.
- LANG-EXPAND-001 NOT started deliberately: its prerequisite (BCP-47
  migration, LANG-EXPAND-001-A) is a breaking-internal dual-worker
  migration (registry + schema + writing-systems.ts + BOTH proxy engines +
  antcv-writer refs + lang-bar) through files the concurrent session is
  committing to today (VERB-LED/SCE). Starting it at a context-window tail
  risks a half-landed migration. It is the top item for a fresh session,
  in the spec's landing order (A -> Tier 1 it/pt-BR/en-US).
- Remaining open board after this round: PREVIEW-CHATBOT-001 (owner scope
  call), ATS-compare (owner design call), LANG-EXPAND (fresh session),
  WIZARD items (islands-blocked), AUTO-PAGEBREAK follow-ups + SO-004
  React #185 + GEN-SCE-FLAG/GEN-LANGFAB (concurrent session's active
  investigations).
## Round 26 (1.50.390) - OUTCOMES-METRIC-GUARD-001 (owner: numerical outcomes always present)

- Verified the enforcement chain end-to-end first: the SCE Selected-Outcomes
  numeric-metric check + 3-attempt retry (the concurrent session's
  4091afc/365ef3f) IS DEPLOYED in BOTH live proxies (grep of the live
  cv-proxy + antcv-demo-proxy bundles via the CF API: findMissingMetricHits
  present). NEW committed test workers/proxy/test/diag-sce-outcomes-metric.mjs
  (4/4, drives the real engine module): metric-free outcomes flag; digits
  pass; written multipliers pass; the retry fix-instruction carries the
  metric guidance and the third draft returns flagged:true.
- The remaining hole for 'ALWAYS': flagged drafts render SILENTLY
  (GEN-SCE-FLAG-001). LAST line of defence shipped client-side:
  antcv-outcomes-metric-guard-390.js - whenever the on-screen SELECTED
  OUTCOMES carries no numeric token (same heuristic as the engine), an
  amber '⚠ No numeric outcome - add an on-record number' chip pins to the
  section in the preview; clicking it scrolls/highlights the outcomes row
  in the editor (native ✨ Enhance is the repair path). Never writes a
  number itself (the engine's own anti-fabrication rule). Checks on boot,
  sections storage changes, preview mutations; kill switch
  antcv:metricguard:off; API AntcvMetricGuard.check().
- Test pwa/test/diag-outcomes-metric-guard.mjs (3/3: metric-free flags +
  data fix clears; numeric start = no chip; written multiplier = no chip).
  Boot-smoke OK. CACHE antcv-1.50.390. No proxy deploy needed (no engine
  change). The GEN-SCE-FLAG surfacing of OTHER violation types remains the
  concurrent session's investigation.
## Round 27 (1.50.391) - GEN-UNSOL-003: a specific application is never 'Unsolicited'

Owner: 'when it is a specific application, Application is not unsolicited
but for the company.'

- Hole found in the generate keep/force gate (app.src.js ~21779): generating
  FROM the Unsolicited-kernel context (io.company === 'Unsolicited') WITH a
  JD attached, an LLM that failed to emit meta.company fell into the
  stale-kernel fallback and the SPECIFIC application was relabelled 'Open
  Application - Unsolicited'. FIX: that fallback is now gated on __noJD -
  with a JD present it can never fire; an empty company stays empty (the
  header shows the role) instead of the WRONG Unsolicited label.
- Second leak closed: a literal 'Unsolicited'/'Open Application'/'n/a' echo
  from the LLM with a JD present is scrubbed to '' (same treatment as the
  schema-placeholder echoes), so the literal can never render as the label
  of a specific application.
- Unchanged guarantees: no-JD generations still ALWAYS force Unsolicited
  (CL-GHOST anti-hallucination), explicit showcases still force, JD-named
  companies still kept.
- Test: pwa/test/unit/gen-specific-application.test.mjs (6/6 truth table
  mirroring the gate). Full unit suite 113/113; photo-positions +
  pageflow regressions green; boot-smoke OK. CACHE antcv-1.50.391.
## Round 28 (1.50.392) - owner batch 2026-06-12 PM (7 items)

1. EXP-HIDDEN-ROLES-001 coverage AUDITED + COMPLETED: part A (editor
   backfill of kernel roles as populated+hidden, matched by id or
   title+company) was already present in app.src.js (~22546 - re-applied
   after the stub incident at some point); part B (the generation-prompt
   rule) was MISSING - the STORED WORK HISTORY block now demands EVERY
   stored role returned in experience_roles (irrelevant ones on:false,
   never dropped, never blank).
2. QUICK-GEN-001: '⚡ Quick generation' checkbox under the Generate button
   (upload step). SESSION-ONLY by construction - uncontrolled checkbox
   (defaultChecked false) writing window.__antcvQuickGen; resets on every
   refresh, never persisted. With a JD present it prepends QUICK GENERATION
   MODE to the prompt: start from the EXISTING application (current
   sections JSON embedded as baseline, capped 28k chars), keep content
   essentially verbatim, change ONLY company/role references + minimal JD
   coverage adjustments. No-JD generations ignore the flag.
3. JD-ANALYSIS-PRINT follow-up ('re-export still reaching CV page setup'):
   the EXPORT MODAL's Save-as-PDF cloned/printed the CV even with the
   Analysis view foreground - the window.print divert (1.50.376) never sees
   that path. The gate now runs the SAME foreground check
   (AntcvPrintIframePreview._analysisViewIsForeground) and diverts to
   AntcvAnalysisReportPdf360._export.
4. CERTIFICATIONS renamed 'CERTIFICATES & COURSES' in both skeleton titles;
   da/es/zh translations added (CERTIFIKATER & KURSER / CERTIFICADOS Y
   CURSOS / 证书与课程) with the LEGACY keys retained so existing user data
   still translates.
5. Methods visible-group likelihood: the STORED TOOLS & METHODS prompt now
   STRONGLY PREFERS visible group labels (named header per cluster; flat
   only under 4 items).
6. INTERESTS-SECTION-001: new sidebar section 'INTERESTS' in the skeleton
   (type bullets, verb+description like SELECTED OUTCOMES); hobbies REMOVED
   from the skeleton's additional list; new cv_overrides.interests_items
   prompt key (2-4 items from stored hobbies only) + merge branch;
   da/es/zh title translations.
7. OUTCOMES-MODE-001: 'Display: Outcomes section | Results per role'
   selector in the outcomes editor (localStorage outcomesMode, default
   section). 'results' hides the SELECTED OUTCOMES section in the preview
   and renders a 'Results:' line under each role - outcomes matched by
   title/company token overlap, unmatched ones attach to the first visible
   role so none are lost. 'section' shows the table and NO per-role lines.
   PREVIEW-FIRST: export parity (worker skip + per-role results) is the
   follow-up, per the established pattern.

Tests: unit owner-batch-0612-strings 6/6 (bundle contracts incl. legacy
translation keys), diag-owner-batch-0612 5/5 (default mode, results mode
incl. unmatched attachment, editor selector, quick-gen checkbox session
flag, analysis divert in the modal). Full suite: unit 119/119,
photo-positions, pageflow, cjlr-experience, metric-guard,
export-preview-pages - green. Gates: terser, head OK, 867,798 B,
boot-smoke. CACHE antcv-1.50.392.

## Round 29 (1.50.393 + docx-worker 1.14.59) - photo air + outcomes export parity

Owner: "keep implementing features / for the figure make sure blank space
above and below it are equal."

1. PHOTO-AIR-EQUAL-001: the main-left/right floating figure carried 2px top
   / 6px bottom margins - the only knowingly-asymmetric photo position.
   Preview float margins now 4px/4px (app.src.js), worker float margins
   19050/57150 EMU -> 38100/38100 (4px each). main-photo-bottom marginTop
   10 -> 8 to match the column's 8px bottom padding. sidebar-top was
   already symmetric (8/8) - untouched.
2. OUTCOMES-MODE-001 export half (round-28 follow-up): export now mirrors
   the preview when the Display selector is 'Results per role'.
   - Client (antcv-docx-client.js): applyOutcomesMode() runs in
     buildPayload for doc=cv when localStorage outcomesMode='results' -
     drops the SELECTED OUTCOMES section from the payload and attaches a
     per-role `results` string (same matcher as the preview: >=4-char
     title/company token overlap; unmatched outcomes -> first visible
     role). Role whitelist forwards `results`. Default/section mode:
     payload byte-identical to before.
   - Worker (1.14.59): renderExperience renders role.results as a
     "Results: " line (bold-italic label + plain text) after the role's
     bullets.

Tests: unit outcomes-mode-forward 3/3 (results mode drop+attach+unmatched,
default unchanged, explicit section mode); worker diag-role-results-export
5/5 (label once, bold, italic, text, ordering after r0 bullet / before
r1). Regression: unit 122/122, photo-positions, pageflow, cjlr-table,
indent-parity, lang-es-zh - green. Gates: terser, head OK, 867,797 B,
boot-smoke, photo-positions. CACHE antcv-1.50.393.

## Round 30 (1.50.394 + docx-worker 1.14.60 + proxy/demo-proxy SCE) - owner evening batch 2026-06-12

Owner: spacing controls in Advanced style; RECOMMENDATIONS section after
experience; Gabriel JSON updates (specialization, Kanzen, accessibility,
contact line); nordic-minimal hard rules; "discuss" banned; "-" not "—".

1. ADV-SPACING-CONTROLS-001: seven new Advanced-style sliders (page
   top/bottom padding, sidebar edge padding, sidebar-main seam gap, main /
   sidebar / letter-body subsection gaps, candidate-header gap). Preview:
   sidebar + main cell padding, Ce section marginBottom per area, header
   row margins. Export: forwarded only when off-default (style tokens,
   clamped 0-60), worker applies px->DXA x15 as DELTAS from its reviewed
   constants (cell tcMar, heading spacing-before, header spacing-after);
   seamGap widens the seam side only, sidebarPosition-aware.
2. RECOMMENDATIONS-SECTION-001: skeleton main section after experience
   ("Danish and international recommenders on request."), idempotent
   backfill effect for existing stored sections (inserts after experience
   unless a recommendations-like section exists), REFERENCER /
   RECOMENDACIONES / Chinese title translations, core-protected via me().
3. SPEC-CATCHY-001: GABRIEL_BG injects the standing specialization line
   (personalInfo.specialization, default "Processes*Products*People");
   subtitles must be simple and catchy (max 3 concepts); UNSOLICITED
   drafts get it verbatim (also in the __neutralCo clause).
4. Prompt rules: work_style MUST END with a people skill; PATENT NUMBERS
   ARE NEVER DROPPED; Accessibility row states HEARING-IMPAIRED
   explicitly (schema example updated); PUNCTUATION DASH RULE ("-" never
   an em dash).
5. LINKEDIN-CLICK-001: preview contact renders LinkedIn as a real <a>
   (https-prefixed, _blank, noopener); worker renders an
   ExternalHyperlink run (underlined) with the external relationship.
6. CONTACT-LOCAL-FORM-001: contact location normalises Copenhagen ->
   "2300 København" (country word stripped; postcode only added to the
   bare city; other cities untouched).
7. NO-JUSTIFY-GAPS-001: sidebar text defaults LEFT in preview (8 ternary
   sites) and worker (2 list renderers) - justified narrow columns gaped.
   CJLR per-item alignment choices still win; main column unchanged.
8. SCE engine (proxy + demo-proxy mirrored, registry synced): banned
   words discuss/discusses/discussed/discussing (+ da diskutere-forms);
   em-dash detector feeds the retry loop ("replace '—' with '-'", capped
   3 sites); new integrity rules (punctuation-dash, patent-number,
   work-style-people-close, accessibility-explicit,
   specialization-catchy).
9. Gabriel example JSONs (en/da/es/zh): Kanzen "Founder" removed + bullets
   reworded, end year 2026, location "2300 København"; recommendations
   section added; top-level specialization field; Accessibility group
   states hearing-impaired (relocation items relabelled Mobility);
   contacts "2300 København"; all em dashes -> "-";
   parser_safe_separator "hyphen" (schema doc updated, em_dash legacy);
   patent entries carry a number. nordic-minimal.md gains the
   owner-locked Hard rules section; cv-skeleton.md documents the
   recommendations section + patent-number/accessibility rules.

Tests: unit 135/135 (spacing-forward 4, owner-evening strings 9 new);
diag-owner-evening-0612 5/5 (backfill + idempotence, LinkedIn anchor +
local form, sidebar left-align, live spacing geometry); worker
diag-spacing-linkedin-export 11/11; proxy suite 50/50 +
diag-sce-discuss-emdash 7/7 + diag-sce-outcomes-metric 4/4. Regression:
boot-smoke, owner-batch-0612, photo-positions (pwa+worker), pageflow,
cjlr-table/experience, role-results, indent-parity, lang-es-zh,
twocol-ownerlike, metric-guard, export-preview-pages - all green.
Gates: terser, head OK, 871,787 B, 0x use strict. CACHE antcv-1.50.394.

## Round 31 (1.50.395 + proxy/demo-proxy SCE) - experience tense toggle + nordic one-line caps

Owner: "add a checkbox in advanced styles that changes the professional
experience between past and present tense (present as default). also for
nordic minimal allow up to one line per bullet and one line per table."

1. EXP-TENSE-001: new EXPERIENCE group in Advanced styles with a checkbox
   "Experience bullets in past tense (default: present)" -
   styleConfig.expPastTense (false default, cloud-syncs, resets with the
   style). The generation prompt reads it at draft time and injects an
   EXPERIENCE TENSE rule after the include-all roles rule: unchecked =
   EVERY experience bullet in PRESENT tense (incl. past roles, the
   default); checked = past tense (incl. the current role). Verb-variety
   rules unchanged either way.
2. NORDIC-ONELINE-001: nordic-minimal (the default style) now hard-caps
   ONE rendered line per bullet and per table row.
   - SCE (proxy + demo-proxy mirrored): cell cap tightens 90 -> 55 chars
     for nordic only (findOverlongCellHits parametrised); new
     findNordicOverlongBullets flags >95-char SELECTED OUTCOMES /
     EXPERIENCE bullets (JSON path + heading-scoped plain-text fallback -
     CL prose bullets like HOW I WOULD CONTRIBUTE stay exempt). Hits feed
     the existing 3-attempt retry loop.
   - Prompt side: the worker style guidance and the PWA's nordic-minimal
     style description both carry the ONE-LINE RULE (~95 chars bullets,
     ~55 chars expertise cells) so drafts comply on the first pass.
   - nordic-minimal.md Hard rules gains item 9; memory updated.

Tests: unit 139/139 (exp-tense-oneline 4 new: checkbox key + default,
both tense rules, PWA one-line strings, proxy==demo-proxy mirror with the
caps); proxy diag-sce-nordic-oneline 7/7 (nordic cell flags at 55, other
styles keep 90, bullet caps via JSON + heading-scoped plain text, CL
exempt, retry guidance); proxy suite 50/50 + discuss-emdash +
outcomes-metric green. Regression: boot-smoke, owner-evening-0612,
pageflow-options, photo-positions - green. Gates: terser, head OK,
873,204 B, 0x use strict. CACHE antcv-1.50.395. Workers: proxy +
demo-proxy redeploy (docx-worker untouched this round).

## Round 32 (1.50.396) - MCP handoff tasks: GEN-PROFILE-001, contact line "2300, København S", CI hygiene

Owner: handoff file antcv-mcp-session-handoff-2026-06-12.md, tasks 1-7.

1. T1: workers/proxy/test/diag-sce-outcomes-metric.mjs DELETED. Note: this
   session had already fixed its regexes (it was passing), but its 4 checks
   are duplicated in writing-style-engine.test.mjs (ab624d9) and the
   standing rule is that CI's bare `node --test` swallows everything in
   test/. The other diag-*.mjs in proxy/test are deterministic,
   no-network, exit-0-when-green - CI-safe; left in place.
2. T2: GEN-PROFILE-001 logged in docs/qa/AntCV_QA_backlog_index_v4.md
   (session header + full entry with implementation status).
3. T3: GEN-PROFILE-001 encoded in BOTH halves - cv-skeleton.md profile
   rules (opener-by-application-type block) AND the live PWA prompt's
   unsolicited clause (__neutralCo): unsolicited sentence 1 = "IT
   professional with 15+ years in consumer and regulated markets" variant,
   never the optics/EO-architect identity; optics depth -> Selected
   Outcomes + sidebar; JD-driven keeps the specialist opener.
4. T4: contact line corrected "2300 København" -> "2300, København S"
   (postcode + district): __localForm in app.src.js (only the bare city /
   bare "København S" normalises; an explicit other district or existing
   digits pass through), cv-skeleton.md new Candidate-header contact-line
   rules, nordic-minimal.md hard rule 7, all 4 example JSONs (8 sites),
   diag fixtures, memory. Kernel check (T4b) is owner-side: if Settings ->
   Personal already holds "2300, København S" the render normalisation
   never fires; if generations still render "Copenhagen, Denmark" after
   this round, that is a new normalisation bug to log.
5. T7: deploy.yml actions/checkout + actions/setup-node v4 -> v5 (Node 20
   deprecation, forced 2026-06-16); dry-run dispatch after push.

Tests: unit 140/140 (GEN-PROFILE string lock added; local-form lock now
asserts the comma+district form), boot-smoke, diag-owner-evening-0612
5/5 (updated to "2300, København S"), proxy suite green after the diag
removal, worker spacing/linkedin diag green. Gates: terser, head OK,
873,835 B, 0x use strict. CACHE antcv-1.50.396. No worker deploys needed
(engines untouched; T5 dual-sync verified deployed earlier today).

## Round 33 (1.50.398, sidecar-only) - PREVIEW-EDIT-PERSIST-001 + mono shield + triage

Owner queue 2026-06-12 late: "not interested in DEMO-TOGGLE-001 / make
sure all text edits in preview persist (not just groups) / privacy fab
flicker resolved - but make the shield single-colour on mobile / then go
with SO-004, GEN-SCE-FLAG-001, PREVIEW-PDF-PARITY-001, PERF-002/005,
GEN-MODELROLE-001, EXPORT-PREVIEW-FEATURES-001(d), AUTO-PAGEBREAK
follow-ups, ENGINE-PAGESPLIT-001."

1. PREVIEW-EDIT-PERSIST-001 (closes PB-PREVIEW-GROUPNAME-EDIT-001).
   DIAGNOSIS (headless, evidence-first): preview inline edits are NOT
   committed by app.js's React onBlur - text-edit mode is off by default,
   so B renders the non-editable branch (no contentEditable, no onBlur).
   The REAL editor is antcv-preview-bullets-dedup-341.js: it promotes
   every [data-antcv-editable-text] span to contenteditable and persists
   on blur by TEXT-MATCHING the original text into the sections store.
   Its walker covered ONLY section-level strings, arrays of strings, and
   table rows - so {b,t} outcomes items, {l,v}/{group} labeled-list
   items, {deg,sch} education, EXPERIENCE roles (title/company/years/
   bullets) and the section TITLE looked edited until the next re-render,
   then silently reverted. FIX: the walker now covers every text-bearing
   shape (object items via a shared key list, roles incl. role bullets,
   title, hands_on/professionally). Same best-effort semantics
   (identical-text collisions update all copies - inherent to the
   text-match design).
   Verified pwa/test/diag-preview-edit-persist.mjs: 9 edit types each
   located -> committed -> survives reload -> re-renders (the diag uses
   REAL click/type/Enter interactions; synthetic focus/blur does not
   reach the handlers and false-fails - and addInitScript re-runs on
   reload, so the seed is guarded or it falsely "wipes" edits).
2. PRIVACY-FAB-COLOR-001: mobile (<=900px) renders the shield as a
   single-colour silhouette (transparent text + fg text-shadow); desktop
   keeps the native glyph. The colour-emoji white/red segments no longer
   scream on the chip. PRIVACY-FAB-FLICKER-MOBILE-001 marked RESOLVED
   (owner-confirmed). diag-privacy-mono.mjs 2/2; diag-privacy-pill
   regression OK.
3. DEMO-TOGGLE-001 -> PARKED (owner declined).

Sidecar-only round: no app.js rebuild. Cache-bust: privacy-led ->
1.50.398-mono-shield, preview-bullets-dedup -> 1.50.398-persist-all,
CACHE antcv-1.50.398, TARGET_VERSION 1.50.398 (+1.50.397 stale).
Boot-smoke OK. NEXT (owner order): GEN-SCE-FLAG-001 bypass closure.

## Round 34 (1.50.399 + proxy/demo-proxy) - GEN-SCE-FLAG-001 both halves

1. ROOT CAUSE of the empty telemetry channel (corrects the round-32
   verdict): executeSceWithRetry's sce-eval KV put was fire-and-forget
   (void ...) without ctx.waitUntil - Cloudflare cancels un-awaited
   promises at isolate teardown, so NO event ever landed in 90 days
   regardless of which mechanism ran. Client-side the
   _antcv_writing_style envelope DOES attach to LLM-shaped POSTs through
   window.fetch (verified headless with a capture server), so the SCE
   most likely runs live and the exposure is mechanism (a): a still-dirty
   3rd draft renders silently.
2. Server (proxy + demo-proxy mirrored): sce-eval put AWAITED;
   early-return paths log reason-coded sce-skip events ('parse-fail',
   'no-llm-text') and surface X-AntCV-Sce-Skip;
   Access-Control-Expose-Headers added in BOTH workers' corsHeadersFor -
   the X-AntCV-* SCE headers were set but unreadable cross-origin, so no
   UI could ever have surfaced them.
3. Client: antcv-sce-flag-toast-399.js - read-only fetch-response
   inspector; X-AntCV-Flagged:1 raises a dismissible amber toast with the
   banned word/phrase counts (8s debounce, 14s auto-hide); sce-skip
   console-warns. Never touches request/response bodies (safe in the
   wrapper chain).

Tests: diag-sce-telemetry-await 5/5 (awaited put, skip events+headers,
flagged 3rd draft); diag-sce-flag-toast 5/5 (install, clean no-toast,
flagged toast with counts, debounce, no errors); proxy suite green;
boot-smoke OK. Acceptance: one owner regeneration, then read
writing-engine:* KV - events now land and settle (a)/(b) definitively.
CACHE antcv-1.50.399. Deploys: proxy + demo-proxy (PWA auto).

## Round 35 (1.50.400 + access-relay) - KV quota fix, console hygiene, brand-fit checkbox

Owner: "demo proxy url is not saved inside the admin subtab. check and
fix all errors and repeating warnings [console dump]. next to generate
add a checkbox that fits the colors/font/style to the specific company."

1. KV-QUOTA-001 (the demo-URL save failure ROOT CAUSE, from the owner's
   console: 'kv_write_failed - KV put() limit exceeded for the day').
   - Relay /api/prefs PUT rewrote the per-user KV blob on EVERY call
     (updated_at always changes), so interval-driven client syncs burned
     the daily KV write quota on no-ops -> every cloud save 500'd,
     including /api/admin/demo (whose unguarded kv.put threw a bare 500).
     FIX: compare-before-write (timestamp-stripped) skips no-op puts;
     the admin-demo put returns the readable kv_write_failed envelope.
   - Client: ai-consent-cloud-sync backs off ~10 min after 3 consecutive
     failures (was retrying every ~60s forever) and logs once, not every
     attempt. NOTE: the daily quota resets at midnight UTC - saves work
     again after the reset even without the fix; with it they stop
     burning quota at all.
2. Console hygiene: manifest share_target gains enctype (silences the
   Manifest warning); setup-chips transition log rate-limited (was
   re-logging every detection flap); diag-probes HARDREFRESH probe now
   matches only the actual Hard Refresh BUTTON (was firing on any click
   inside a container mentioning it, producing bogus 'NO reload'
   warnings); the API-keys panel hosts its four password inputs in a real
   <form> (submit prevented) - silences the Chrome password-field DOM
   warnings. The [Violation] rAF/reflow lines are Chrome perf hints from
   the sidecar MutationObservers, not errors - left as-is.
3. COMPANY-BRAND-FIT-001: 'Brand fit' checkbox next to Generate
   (session-only, like quick-gen). With a JD present the prompt asks for
   a top-level brand_fit {navy, accent, head_font, body_font, source}
   derived from the company's branding (ad/company-site text first, else
   publicly known identity; instructed to OMIT rather than guess). Merge
   applies it with strict validation: 6-digit hex only, navy must pass a
   darkness check (white header text), fonts from the 10-font whitelist;
   navy via the persisted navy setter, accent/fonts via the styleConfig
   partial-merge (flips package to custom). Both persist + cloud-sync.

Tests: unit 144/144 (brandfit-kvquota-strings 4 new), diag-brandfit-
checkbox 1/1, boot-smoke OK. Gates: terser 877,540 B, head OK, 0x use
strict. Cache-bust: app.js 1.50.400-brandfit-kvfix, manifest, consent
sidecar, diag-probes, setup-chips (via 357-loader), CACHE antcv-1.50.400.
Deploys: access-relay + PWA (auto).

## Round 36 (1.50.401) - SPACING-COMFORT-DEFAULT-001: the comfort recommendation ships as the default

Owner: "generate a pdf template by the recommendation" (after the
simulated-PDF comparison round).

1. The comfort spacing is now the DEFAULT for new + untouched configs:
   mainEdgeIndent 14 (was 10), bodyEdgePad 12 (8), sidebarEdgePad 11 (8),
   seamGap 6 (0), mainSectionGap 14 (8), sidebarSectionGap 12 (8),
   bodySectionGap 16 (8), candidateGap 5 (3). Updated in: the styleConfig
   defaults object, the Advanced slider defaults, and every render
   fallback (sidebar/main cell padding, bridge spacers, Ce per-area
   section gap - sidebar 12 / CL 16 / main 14, candidate header rows).
2. PARITY: the docx-client now forwards the EFFECTIVE value (stored,
   else the comfort default) whenever it differs from the WORKER's
   reviewed constants - so untouched users export the same comfort look
   they preview; dialling a slider back to the old value (8/8/0/8/8/8/3,
   edge 10) forwards nothing and reproduces the original tight look
   exactly. mainEdgeIndent now forwards its effective value always.
3. Template PDFs regenerated through the live /generate-pdf at the new
   default look: Downloads/antcv-spacing-sim/TEMPLATE-cv-comfort-default.pdf
   + TEMPLATE-cl-comfort-default.pdf (identical to the round-32 comfort
   sims, now the shipped default).

Tests: unit 144/144 (spacing-forward rewritten for the new semantics:
worker-look values forward nothing, untouched forwards comfort,
garbage falls back to comfort; indent-controls untouched default now
forwards 14), diag-owner-evening updated (unseeded edge fallback 14),
boot-smoke OK. Gates: terser 877,576 B, head OK, 0x use strict.
CACHE antcv-1.50.401. No worker changes (delta model unchanged).

## Round 37 (1.50.402) - owner queue: parity inc-2 closed, PERF-002/005, MODELROLE design, ATS compare, effective 📄 labels, PAGESPLIT closed

1. PREVIEW-PDF-PARITY-001 increment 2 (horizontal width) - CLOSED BY
   CONSTRUCTION: with the R36 spacing forwarding both sides derive every
   horizontal dimension from the same ratio x page width - px-equal
   margins. Numeric audit at comfort: CV main 498.0px preview vs 497.8px
   worker; CL 780 vs 780.4; sidebar 240 vs 239.9. Residual line-break
   drift = browser-vs-LibreOffice font shaping, sub-word, irreducible.
2. PERF-002: consensus QUORUM + TIMEOUT. Both consensus waves (signal
   poll + deliberation) now resolve at 2 successes or a 20s budget via
   __quorumSettle instead of awaiting ALL providers - one hung provider
   can no longer stall the generation. Stragglers are abandoned.
3. PERF-005: same-input jd-analysis cache (antcv-overlay.js). The exact
   same JD + candidate summary within 24h renders from
   antcv:jdAnalysisCache without re-paying the LLM cycle (the full
   fold-into-generate stays reverted per GEN-EMPTY-001).
4. GEN-MODELROLE-001: design doc shipped
   (docs/plan/GEN-MODELROLE-001_design.md) - env-driven MODEL_ROLES map
   (writer/supervisor/coherence), task->role routing at the cascade head,
   fail-soft to current behavior; three owner decisions queued.
5. EXPORT-PREVIEW-FEATURES-001(d): antcv-ats-compare-402.js - a
   "⇄ Legacy ATS view" toggle in the export-preview modal renders the
   flattened single-column legacy-parser view (canonical headers,
   tables -> "Focus - Expertise" lines, no photo) for side-by-side
   comparison with the modern preview.
6. AUTO-PAGEBREAK-BLOCK-001 follow-up: the 📄 page buttons show the
   EFFECTIVE page - max(manual, measurer auto map) with an "ᵃ" suffix
   when the auto split moved the row - at both button sites (outcomes
   rows, sidebar group rows).
7. ENGINE-PAGESPLIT-001: un-paused, reviewed, CLOSED as superseded - the
   blocker (APPJS-REBUILD-001) is fixed and per-item on-screen pagination
   shipped piecewise (sidebar group-aware splits, table-row splits with
   re-cloned header, HWIC/Foundation part splits), all reading the
   effective manual∪auto bucket; tracker entry updated with the evidence.

Tests: unit 149/149 (queue-r37-strings 5 new), boot-smoke, brandfit +
owner-evening diags green. Gates: terser 878,091 B, head OK, 0x use
strict. Cache-bust: app.js 1.50.402-queue-r37, antcv-overlay
1.50.402-ja-cache, ats-compare 1.50.402, CACHE antcv-1.50.402.

## Round 38 (1.50.403 + access-relay) - PERSONALITY-KERNEL-001 (handoff T8)

Handoff tasks 1-7 were already complete (rounds 32-35); T8 is new.

1. Canonical source committed:
   skills/antcv-writer/assets/gabriel-kernel-personality-v1.json (v1.1 -
   six trait clusters with evidence phrasings, work_style_line en/da,
   3-part profile_structure, cover-letter Who-I-Am fragment, render
   constraints, kernel fact corrections).
2. Kernel schema (T8a): NO migration needed - the relay's
   splitPersonalInfo identity catch-all already round-trips unknown
   personalInfo keys through D1 user_kernel.identity. 'personality' +
   'specialization' added to PI_IDENTITY_KEYS explicitly so the schema
   documents them. The owner can paste the JSON into Settings -> Personal
   (personalInfo.personality) and it persists + cloud-syncs today.
3. PWA prompt (acceptance path): GABRIEL_BG now injects the PERSONALITY
   KERNEL - stored personalInfo.personality (serialized, capped 4.5k)
   when present, else the embedded v1.1 default: the six traits as
   behaviour evidence (never raw adjectives), the canonical Work-style
   line (en/da) as the work_style_content base, the THREE-PART PROFILE
   STRUCTURE (who-i-am by register / body-mind with "passionate" still
   banned + foreningsarbejde kept in English text / max ONE
   special-capabilities sentence), render constraints (never raw
   "people's person"/"team player"/"empathy"/"moral"/"reads the room"),
   and the CL Who-I-Am fragment. PROFILE budget bullet updated to the
   3-part shape (50-70 words).
4. cv-skeleton.md (T8b): Three-part PROFILE structure + personality
   render constraints encoded next to the GEN-PROFILE-001 rules.
5. Fact corrections (T8c) in all four example JSONs: Volunteer ->
   "Operations manager & assistant coach, Copenhagen Wolves RFC
   (foreningsarbejde)"; certifications now carry BOTH "AI-Practitioner -
   Teknologisk Institut" AND "AI course (Copilot) - IDA Ingeniørforening".
6. Acceptance (T8d) owner-side: regenerate an unsolicited Profile ->
   three sub-parts, foreningsarbejde in Additional Info, IDA attribution
   in certifications, no banned words (the SCE toast + telemetry from
   1.50.399 will report any flagged drafts).

Tests: unit 154/154 (personality-kernel 5 new), boot-smoke OK. Gates:
terser 881,704 B, head OK, 0x use strict. CACHE antcv-1.50.403.
Deploys: access-relay (PI_IDENTITY_KEYS) + PWA (auto).

## Round 39 — 2026-06-12 (PREVIEW-CHATBOT-001 stage 1 + GEN-SPEED-001) — 1.50.406

Owner: "implement stage 1 of the standard ai bot. design its operation and
make sure UI is clear and user friendly and ready also for step 2 ...
think of ui features and elements that improve ease of use (and especially
prevent long convergence time for the generation)".

1. PREVIEW-CHATBOT-001 stage 1 (NEW pwa/antcv-preview-chatbot-405.js,
   wired into index.html; design doc docs/plan/PREVIEW-CHATBOT-001_stage1.md):
   select preview text -> "AI edit" pill -> anchored panel with the
   selection quoted, 4 quick actions (Shorten / More concrete / Calmer
   tone / Fix wording), free instruction + Ask. One buffered LLM call
   through the cv-proxy root pipeline (x-provider) so the rewrite gets the
   writing-style envelope + SCE checks + retries server-side. Result +
   one-line Why -> Apply (text-match into the sections store, walks every
   text-bearing shape) / Discard / Undo (snapshot restore). Step-2-ready
   DOM: conversation log + rule-chip containers already present.
2. GEN-SPEED-001 (app.src.js): persisted Generation Speed preset -
   Fast / Balanced / Thorough pills under Generate (antcv:genSpeed).
   Fast = one provider per task + consensus waves skipped (shortest
   convergence). Balanced = default, PERF-003 mechanical cap + quorum
   consensus as today. Thorough = mechanical cap lifted, full ladder.
   __genSpeed() helper; dispatcher + consensus gate read storage live.

Tests: diag-preview-chatbot 6/6 (pill, panel, mocked rewrite + Why,
Apply persists + re-renders, Undo, no errors), diag-gen-speed 5/5
(render, balanced default, fast persists + survives reload), unit
156/156 (perf-mechanical-trim extended with GEN-SPEED lock). Gates:
terser 878,710 B, head OK, 0x use strict, boot-smoke OK.
CACHE antcv-1.50.406. Deploys: PWA (auto on push).

## Round 40 — 2026-06-12 (GEN-MODELROLE-001 v1 + speed-on-screen) — 1.50.407 + proxy/demo-proxy

1. GEN-MODELROLE-001 v1 (fail-soft, INERT until env set): parseModelRoles +
   roleHeadOrder in multi-llm.js (mirrored byte-identical to demo-proxy);
   callAnyLLMForJSON takes opts.role and reorders the cascade head from the
   optional env MODEL_ROLES map (reorder, never remove). Roles wired:
   supervisor grounding check -> 'supervisor'; auto-repair (re-writes
   prose) -> 'writer'; gen-coherence review -> 'coherence'; both index.js
   provider-fallback wrappers prefer MODEL_ROLES.writer as cascade head for
   detectCVTask-shaped generation calls on the SERVER-KEY path (BYOK
   untouched). MODEL_ROLES is NOT set in production - behaviour is
   byte-identical until the owner answers the 3 design decisions (QnA).
2. Generating screen (app.src.js): the active Speed preset
   (Fast/Balanced/Thorough) now shows on the elapsed-time line, and the
   over-60s hint points at the Fast preset when not already on it.

Tests: workers 68/68 (model-roles.test.mjs 7 new incl. dual-sync +
call-site locks), pwa unit 156/156, diag-gen-speed 5/5, boot-smoke OK.
Gates: terser 879,078 B, head OK, 0x use strict. CACHE antcv-1.50.407.
Deploys: PWA (auto), proxy + demo-proxy via deploy.yml.

## Round 41 — 2026-06-12 (full V&V sweep) — no version bump

Sweep on the shipped 1.50.407: pwa unit 156/156, workers unit 68/68,
docx diags 12/12, boot-smoke OK, diag-preview-chatbot 6/6,
diag-gen-speed 5/5, diag-brandfit OK, diag-preview-edit-persist OK.
Live checks: antcv.pages.dev serves CACHE antcv-1.50.407 and the
chatbot sidecar (13,828 B); proxy + demo-proxy deploy runs green.

Finding fixed: `npm run test:smoke` pointed at the retired
workers/docx-worker/test/smoke.js, which imports src/generate.js -
an 11-byte PLACEHOLDER since the worker moved to the bundled
src/index.js. The entry now runs scripts/run-docx-diags.mjs (NEW),
which executes every docx diag against the live bundled fetch handler.
The legacy smoke*.js files remain as history but are not runnable.

QnA handoff (new AI bot + owner decisions) delivered in chat.

## Round 42 — 2026-06-13 (owner batch: bot guards, throttle-resist, cost ceiling, LLM lab, MODEL_ROLES live) — 1.50.408 + proxy/demo-proxy

Owner items, all five:
1. AI-edit non-interference (pwa/antcv-preview-chatbot-405.js): icon now
   🤖 (distinct from the per-item ✨ Enhance); mouseup on any resize
   affordance (column scaler, seam splitter, table external-border
   scaler - cursor col-/ew-resize, resize-labelled, or mid-drag body
   cursor) never raises the pill; caret clicks never raise it; typing
   dismisses it; the pill never steals focus from the span being edited.
2. GEN-THROTTLE-RESIST-001 (app.src.js): every provider call WAITS for
   tab visibility before firing; a network-class failure while hidden
   retries after visibility WITHOUT burning the transient ladder (max 3);
   the generating screen holds a screen wake lock (re-acquired on
   visibility) so mobile runs survive screen-off/backgrounding instead
   of restarting.
3. GEN-COST-CEILING-001 (owner decision 3): antcv:genCostCeiling input
   next to the Speed pills; per-run meter window.__antcvGenCost (reset on
   Generate, accumulated per call); past the ceiling the run finishes in
   single-provider mode with consensus skipped - cheaper, never aborted.
   Live run cost (and ceiling) shown on the generating screen timer line.
4. LLM-ONBOARD-001 (NEW pwa/antcv-llm-lab-408.js + dispatcher wiring):
   add an OpenAI-compatible LLM (label, base URL, model, key, $/1M
   pricing) in Settings -> API keys; AUDIT battery (instruction-following,
   strict-JSON, banned-word avoidance [all critical], latency, est.
   cost/call); Approve unlocks only when the battery passes; approval
   writes antcv:llmRegistry evidence and the model joins the BACK of the
   routing ladder as custom:<id>, where quality-demotion memory promotes
   or demotes it - the cost-quality function decides its place.
5. GEN-MODELROLE-001 ARMED (owner decisions 1+2): MODEL_ROLES set in both
   wrangler.toml [vars] - writer=anthropic, supervisor=mistral,
   coherence=anthropic; assignment is a cost-quality decision revisited
   from telemetry; SCE retries stay on the writer.

Tests: diag-preview-chatbot 9/9 (guards + icon added), diag-llm-lab 7/7
(NEW), diag-gen-speed 5/5, preview-edit-persist OK, boot-smoke OK, pwa
unit 161/161 (gen-resilience-cost.test.mjs NEW). Gates: terser 883,230 B,
head OK, 0x use strict. CACHE antcv-1.50.408. Deploys: PWA (auto),
proxy + demo-proxy via deploy.yml (MODEL_ROLES goes live with them).

## Round 43 — 2026-06-13 (account-scroll reset + JD URL trim) — 1.50.409

1. SETTINGS-SCROLL-RESET-001 (owner: "in the account menu, scrolling down
   to the end the app resets"): console showed a clean reboot with data
   intact - a browser RELOAD, not a crash or erase. Desktop headless
   repro (account + keys subtabs, 6 scroll-to-end passes, LLM-lab active
   and neutralised) shows no reload and no page errors - the mechanism is
   mobile scroll-CHAINING: when the settings panel scroll hits its end the
   gesture chains to the viewport, where the browser pull-to-refresh or
   overscroll navigation reloads the app. Fix: overscrollBehavior contain
   on BOTH settings scrollers (backdrop + panel) and html-level
   overscroll-behavior none in index.html (body alone is not the viewport
   scroller, so the existing body rule did not block pull-to-refresh).
2. JD-URL-TRIM-001 (owner: "why is the fetch url very long? how to fix?"):
   job boards append filter and tracking query params that change nothing
   about which posting is fetched - the pasted NVIDIA Workday URL carried
   locations and locationHierarchy1 GUIDs. __normJdUrl now runs before
   the fetch-jd-url call: myworkdayjobs.com loses its whole query string
   (the job id lives in the path), all hosts lose known tracking params
   (utm_*, gclid, fbclid, gh_src, lever-origin, trk, ...) and fragments;
   meaningful params (id) are kept; unparseable input passes through.

Tests: pwa unit 165/165 (settings-scroll-jdurl.test.mjs NEW - the owner
Workday URL is a fixture), boot-smoke OK, diag-account-scroll-reset
(NEW, desktop non-repro documented), diag-llm-lab OK, diag-preview-chatbot
OK. Gates: terser 883,776 B, head OK, 0x use strict. CACHE antcv-1.50.409.
Deploys: PWA (auto). No worker changes.