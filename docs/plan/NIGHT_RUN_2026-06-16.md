# AntCV Nightly Run — 2026-06-16

Source-of-truth: `docs/qa/ACTIVE_BUGS.md` top block still wins for the active queue.
This run targets the **reconciled old-open backlog** (`docs/qa/AntCV_old_open_reconciled_2026-06-16.md`),
working one cluster at a time, spec-first.

Discipline (non-negotiable): edit `pwa/app.src.js` → mirror minified `pwa/app.js`; worker edits go
in `workers/docx-worker/src/index.js` (inlined bundle, no build step) with manual `wrangler deploy`,
one deployer at a time; a fix counts only if it holds in Preview + DOCX/PDF, desktop + mobile;
files above ~50KB are never written inline. Spec before code on every cluster.

---

## Cluster 1 — Watermark (WM) — SPEC COMPLETE, IMPLEMENTATION PENDING

Spec: `docs/qa/WM_AI_NOTICE_ANCHOR_SPEC_2026-06-16.md`.

- IDs: WM-001, WM-002, WM-004, WM-005 (+ active `AI-WATERMARK-EXPORT-LOCATION-001`). WM-003 closed.
- Owner decision: AI notice anchors to the **actual bottom of the last page**, uniform; CV keeps
  dynamic horizontal corner, CL stays bottom-right.
- Root: preview (`antcv-watermark-page-anchor-341.js`) is already correct; the worker still emits
  the notice as a FLOWED paragraph. One worker-only change closes all five rows.
- Keystone: spec §4.1 (strip flowed notice from both builders) + §4.2 (inject last-page-anchored
  VML text frame in `postProcessDocx`). §4.3 clearance is a parameter on the same shape.
- Carrier decision (apples-to-oranges resolved): NOT a docx-lib footer (repeats every page →
  re-breaks WM-005) and NOT the section header (serializes empty in this bundle). Use the same
  post-process raw-XML VML layer the DEMO mark already proves survives CloudConvert, attached to
  the LAST page's content rather than the section header.
- Risk: DOCX→PDF survivability of a bottom-anchored VML frame through CloudConvert/LibreOffice —
  verify a real PDF, not just the DOCX.
- Acceptance: the 8 cases in spec §6. Add the unit test in spec §7 (no flowed `AI-assisted` run;
  exactly one anchored shape; it lives in the last page's XML).
- Status: **spec committed, code not started.** This IS a good nightly target — the keystone is a
  single worker change, and the failure mode (XML structure) is unit-testable headlessly; only the
  final CloudConvert PDF check needs the owner.

---

## Cluster 2 — List-row controls — INSPECTED, NOT SAFE FOR AUTONOMOUS WORK

IDs: PP-001, PP-002, PP-003, PUB-ROW-MULTIROW-001, SO-001, SO-002, TB-001, TB-003,
MERGED-MOVE-CONTROL-001.

**Verdict: do NOT implement in an autonomous nightly run. Owner-present, probe-driven session
required.** Reasons (verified, not inferred):

1. Highest-regression-risk zone on record. `docs/plan/PP-003-regression-history.md` documents
   SEVEN prior failed iterations on Publications row controls alone (pub267→269→271→control→
   injected→mini-kind→273). Plan rule: never add a new `data-antcv-pub*` prefix family.
2. The failure modes are all live-rendered-layout (buttons floating outside the row, cluster width
   overflow, control acting on the wrong field, wrong button order, duplicate Fit after re-render).
   None is verifiable headlessly or by boot-smoke. This is exactly why PP-001/PP-003 are PROBE-FIRST.
3. A correct fix is an architectural MIGRATION, not a patch: the plan mandates refactoring row
   controls onto the shared `SectionControlBar` (`antcv-section-control-bar.js`, loaded), gated by
   the `TC-028` Publications-stress fixture — not a happy-path test.
4. Multiple overlapping sidecars (273 editor, 278 panel, 211 canonical panel, 388 dedupe guard, 352
   reaper) run on broad `document.body` observers and re-trigger on each other's writes. Touching
   one without the live DOM produces iteration #8.

Resolved open question from the regression doc: `antcv-section-panel-211.js` IS still loaded
(`?v=1.50.418`) and is now the CANONICAL panel (206/207/208 retired; 211 owns headline-CJLR and
wins the CSS cascade). Not dead weight — do not retire it.

Next action when scheduled WITH the owner: run the live-DOM probes first (`antcv-apphist-zindex-probe`
pattern, plus a Publications-row DOM capture), confirm which surface each control mis-attaches on,
THEN plan the SectionControlBar migration. No blind edits.

---

## Cluster 3 — Pagination / page-break — INSPECTED, MOSTLY ENGINE-CLOSED

IDs still open in reconciled list: PB-001..006, PAGEBREAK-002, PAGEBREAK-005, PB-SIDEBAR-001,
PDF-LAYOUT-001. (PB-004 PROBE-FIRST.)

**Verdict: the hard engine work already shipped; the remainder is live-pagination behaviour that
needs real documents + preview/DOCX/PDF parity — owner-present, not autonomous.** Evidence:
the PB-WORKER-* / PB-PREVIEW-* / PB-AUTO-OVERFLOW-001 internals are FIXED in ACTIVE_BUGS, and
PB-007 / ENGINE-PAGESPLIT-001 (the structural per-item engine) shipped via the dual-map measurer.
What's left (manual-break-to-all-outputs, continuation headers, table row splits, sidebar
per-subsection breaks) is verified only by tapping page controls on a real multi-page document and
comparing a fresh PDF — fails the headless bar.

---

## Bottom line for tonight

Only **Cluster 1 (Watermark)** clears the "verifiable headlessly, single coherent change" bar for
an autonomous run. Clusters 2 and 3 are real but require an owner-present, probe-first session
because their acceptance is live-rendered and their blast radius is high. Recommend: implement WM
per its spec tonight; schedule list-row + pagination for a co-working session with the live UI.

Inspection rule (applied): read the DEPLOYED code (worker + owning sidecar + index.html load order)
before trusting any status label; describe issue + gap; get the owner decision on any ambiguity;
only then spec or defer.
