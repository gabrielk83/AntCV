# AntCV register — CLOSED

Finished rows and their evidence. Split out of `OPEN_REGISTER.md` on 2026-08-26.
Nothing here needs a nightly slot; it is kept so a back-reference to an old row number still
resolves. Row text is verbatim.

## REGISTER-HYGIENE-001 — the split itself (2026-08-26, owner-approved)

One 524 KB file became four. 155 source rows in, 155 placed, 0 dropped, 0 duplicated (round-trip
asserted by the builder). 94 ACTIVE / 16 archived. Five colliding row numbers renumbered (see the
redirect map in `OPEN_REGISTER.md`). Enforced by `scripts/check-register.mjs` +
`pwa/test/unit/register-hygiene.test.mjs` (12 tests, every rule negative-controlled).
Full detail in the `ACTIVE_BUGS.md` top block and `REGISTER_RUNLOG.md`.

## Rows archived from the OPEN queue (2026-08-26)

Archived only when the row announced its own closure up front AND carried no open-work marker
anywhere in its text (no REMAINING / still owed / partial / owner-gated / pending / BLOCKED /
not started / follow-up). Anything ambiguous stayed ACTIVE.

| # | Item | Source / detail | verified |
|---|------|-----------------|----------|
| 4 | ~~rich_block RULE editor decoupling~~ CLOSED (stale-open; verify-first 2026-07-03): the EDITOR half shipped 1.51.16 + wk 1.14.108 (commit 7810b90 "rule editable independently of the headline" — the "— Rule" button in antcv-rich-block-editor.js toggles a standalone `headlineRule` while headlineOff, plus the "│ Cue" control); export parity was 1.51.64 (WHY-RULE-EXPORT-PARITY-001); app.js preview render reads headlineRule. Nothing remains | 2026-06-28 queued → shipped 1.51.16/64 | 2026-07-03 |
| 5 | ~~Candidate-header click + rules~~ CLOSED (batches 17-18, 1.51.83-85 + wk 1.14.125): HEADER-ROW-CLICK-001 (button stopPropagation + row-click interactive-child early-out — the double-toggle collapse is dead) + HEADER-ROW-DBLCLICK-001 (dblclick anywhere on the row opens the detailed editor) + SECTION-ROW-DBLCLICK-001 (1.51.84 — the SAME ends-open dblclick on the shared section-row component used by the SIDEBAR, MAIN and BODY panels) + HEADER-ITEM-RULE-001 COMPLETE (engine 1.51.83/wk 1.14.125; UI + parity 1.51.85: antcv-header-rule-control.js = per-field 'Rule line below' controls INSIDE each header field's DETAILED EDITOR in the side panel (owner corrected the placement, 1.51.86: on/off + 0.5-2pt + colour + auto-theme, injected into the ← Back panels next to the CJLR control) — AND the __antcvHdrRuleDiv/Html/Style render helpers consumed by the live preview + HTML export via guarded splices with exact-legacy fallbacks; preview/HTML/DOCX/PDF read ONE store; absent store = copenhagen-modern default = the previous look; 3 unit tests). Owner eyeball: open a header field editor → the Rule-line row is at its bottom; a Name-rule toggle round-trips preview+export | ACTIVE_BUGS batches 17-18 | 2026-07-03 |
| 7 | ~~CL slogan/signature F3~~ CLOSED (1.51.82, CL-FORMAT-PANEL-001): new antcv-cl-format-panel.js builds a distinct "COVER LETTER FORMAT" panel in Settings→Layout and re-parents the signature + slogan/sign-off controls into it (controls keep their markers/stores/self-heal — re-parenting is isConnected-safe; slogan follows the signature naturally). diag-cl-format-panel.mjs 9/9 (mount-once, wrap both, re-render dedup, kill switch). Owner eyeball on the live panel | memory cl-slogan-signature → batch 17 | 2026-07-03 |
| 9 | ~~Cluster demand model: worker pipeline + nightly recruitment-site refresh~~ **CLOSED 2026-07-13 (in production)** — full pipeline built + live: JD-extraction + recompute + fit (source='jd'), the source='research' WRITER (POST /api/cluster-demand-research + `insertResearchQualifications` union, access-relay deployed) + `scripts/cluster-demand-research-push.mjs`, the weekly refresh routine (`antcv-demand-seed-weekly`, LIVE Fri trigger), and both fuse unions (writer floor-retain + client SEED∪D1). D1 populated + verified 2026-07-13 (181 rows). See the TO-DO-summary row 9 above + google-cse-setup §8 + SESSION_LOG_2026-07-13. **Run 2026-07-24** (first under the STANDING RULE 0 preflight/heartbeat gate, after the 07-17 fire silently no-op'd): all 9 clusters refreshed (11-day-stale catch-up, larger-than-usual shift set), client SEED → `1.51.3722-demand-seed-refresh`, D1 push `total_inserted:198`, pwa suite 1464/1464 green. See SESSION_LOG_2026-07-24. **Run 2026-08-01** (on-cadence, 8 days since 07-24, no missed-fire gap): 9 parallel background-agent WebSearch passes vs the shipped baseline, mostly evidence-backed reorders — no items silently dropped. Notable: "evaluating/reviewing AI-generated output" promoted as a skill distinct from AI-tool fluency (engineering_software r16→r4; consulting NEW item r17); Excel re-promoted in finance (r5→r1) and data_analytics (r9→r2) on fresh posting-frequency evidence; consulting ESG/CSRD demoted (EU Omnibus cut mandatory CSRD scope ~85%). Client SEED → `1.51.4086-demand-seed-refresh`, D1 push `total_inserted:207` (9/9 clusters recomputed), pwa suite 1570 (1563 pass/7 skip/0 fail), cache-bust gate OK. Draft PR: https://github.com/gabrielk83/AntCV/pull/358 (D1 write already live per the routine's design; PR is the code/doc trail). See docs/analysis/cluster_top20_research_2026-08-01.json. **Run 2026-08-14** (13-day missed-fire catch-up — the scheduled 08-08 Fri 22:00 fire produced no run, similar gap class to 07-24): 9 parallel background-agent WebSearch passes vs the shipped baseline; 6/9 clusters changed (mostly reorders/wording, one add+drop pair), 4 confirmed unchanged after fresh-evidence recheck. Notable: consulting's AI-augmented-delivery item moved #2→#1 (56% wage-premium + ~7x posting-mention growth evidence); photonics_eng's silicon-photonics-test item broadened to cover co-packaged optics for AI-datacenter interconnects (new hyperscaler-driven hiring niche); executive swapped "Market & competitive strategy" for "Customer & client centricity / experience ownership" (82% C-suite JD prevalence) plus a reorder toward commercial/operational items; engineering_software/research_phd/photonics_eng got minor tool-list wording updates (RAG, MATLAB, git). Client SEED → `1.51.4126-demand-seed-refresh`, D1 push `total_inserted:214` (9/9 clusters recomputed, verified live), pwa suite 1570 (1563 pass/7 skip/0 fail), cache-bust gate OK. PR (merged): https://github.com/gabrielk83/AntCV/pull/359. See docs/analysis/cluster_top20_research_2026-08-14.json. **Run 2026-08-18** (ad-hoc dispatch, only 4 days after 08-14 — NOT the Fri cron; short interval stated in the artefact, so no market movement is claimed and every change is a gap-fill from a source earlier runs had not reached): 9 cluster research passes run inline via WebSearch/WebFetch (13 searches + 1 direct fetch), 6/9 changed, 3 re-confirmed unchanged (research_phd, data_analytics, executive) with no forced churn. Theme — **hard posting-share data beats AI-hype in rank order**: IT-Branchen’s quantitative Jobindex analysis (security 18.8% of Danish IT postings > cloud 14.5% > AI/ML 12.3% — the FIRST Danish site-scoped posting data available to this model since the CSE proxy died) moved engineering_software secure-coding 7→5 / cloud 5→6 / CI-CD 6→7; two HR datasets (data analysis 36% vs AI 31% of new-skill postings; AI named in only ~9% of HR postings vs ~45% in data roles) moved people_soft people-analytics 6→3 / AI-literacy 3→4. No AI item was demoted below top-5 in any cluster. Second theme — the EU regulatory calendar moved consulting: the AI Act high-risk deadline (2026-08-02) added “AI governance, risk & EU AI Act conformity advisory” at r16, and Omnibus I (ESRS scope cut to >1000 employees / >€450M turnover, ~85% fewer companies) dropped the ESG advisory item out of the top 20. Wording broadenings: pm_process r3 names emotional intelligence (Ceipal 2026), photonics_eng r15 names photonic packaging & assembly, finance r17 adds MBA (FP&A MBA requirement +13pp to 39%). Client SEED → `1.51.4306-demand-seed-refresh`, D1 push `total_inserted:218` (9/9 clusters recomputed), pwa suite 1574 (1567 pass/7 skip/0 fail), cache-bust gate OK. PR (merged `1cbd082`): https://github.com/gabrielk83/AntCV/pull/360. See docs/analysis/cluster_top20_research_2026-08-18.json. | memory cluster-demand-model | CLOSED 2026-07-13 |
| 10 | ~~Ordering spec: JD-cluster top-20 skills weighting~~ CLOSED (1.51.78, ORDERING-JD-CLUSTER-001): the gen prompt now injects the classified cluster's top-20 demanded qualifications (antcv-cluster-demand classifyJD + seed) as a SELECTION/ORDERING weighting rule (__clusterRule src / __cr app.js in the tense/lang/brandFit chain); silent no-op when unsolicited/unclassified; byte-parity locked. Worker pipeline + nightly refresh remain row 9 | ACTIVE_BUGS batch 17 | 2026-07-03 |
| 13 | ~~PAN-IDRAET-BULLET-NEARDUP-001~~ CLOSED (1.51.78): preview half shipped as antcv-neardup-preview-hide.js — INDEX-SAFE by construction (display:none + marker, stored arrays and roles.N.bullets.M paths untouched, fully reversible) using THE EXPORT'S OWN predicate via window.AntcvCollapseRoleBullets (exposed by docx-client — zero drift); 5 unit tests incl. cleaner-line-wins + KEEP_MIN parity. Export half was 1.51.70 | ACTIVE_BUGS batch 17 | 2026-07-03 |
| 15 | ~~GEN-UNSOL-STALE-JD-001 Patch D~~ CLOSED (1.51.77): antcv-unsol-company-scrub.js — meta.company empty/Unsolicited + prior antcv:activeAppCompany → word-boundary scrub of cv+cl prose to a neutral phrase; EMPLOYER names protected (workHistory + experience roles); write-on-change only; kill-switch; 6 unit tests | ACTIVE_BUGS 1.51.54 block → batch 17 | 2026-07-03 |
| 104 | RELAY-TUNE-NAN-FALLBACK-001 (found + FIXED by weekly cost-quality tune 2026-07-22): `scoreRows` in `scripts/relay-cost-quality-tune.mjs` guarded the quality composite on `Number(r.health_score) != null`. Since `Number(undefined) === NaN` and `NaN != null` is `true`, the intended fallback to `success_rate` NEVER fired when a snapshot lacked `health_score` — poisoning `quality` (and `costQuality`) with NaN. This is exactly the offline snapshot the routine builds from D1 `llm_calls` (no `health_score` column) whenever the relay admin token is unavailable (the 2026-07-22 and 2026-07-15 runs). Fix: guard on the raw field (`r.health_score != null ? Number(...) : Number(r.success_rate) || 0`) + a covering test ("missing health_score falls back to success_rate"). Pure scoring-script change, no deploy, no version consumed; suite 14→15 green. | `scripts/relay-cost-quality-tune.mjs:92` + `scripts/tests/relay-cost-quality-tune.test.mjs`; COST_QUALITY_WEEKLY_2026-07-22.md | DONE 2026-07-22 (docs/script only) |
| **79** | ~~JOBTRACKER-SAVE-409-SIGNALS-001~~ **CLOSED 1.51.392 (commit 1f28ab7, SAVE-409-MERGE-001)** — `persist()` now rebases ONCE onto the server doc on a 409 (server-added rows/keys survive — discovery rows, app-written rationale/scores, pin/park/discovered ledgers; this session’s per-row edits win) and re-PUTs with the server rev; the hard-conflict "re-apply your edit" path remains as fallback. Note "Saved ✓ (merged a concurrent update)" shows when the rebase fired. Original hazard: — the tracker doc PUT uses rev-based optimistic concurrency, and the app now auto-PUTs analysis rationale onto the row after Open (1.51.361) → the island's rev goes stale and its NEXT save 409s. On conflict `persist()` REPLACES local state with serverDoc, so signals typed-but-not-saved before pressing Open can be silently dropped (the owner's NVIDIA/Tech-Mahindra rows had NO signals/sigfiles keys in D1 — consistent with this path). Fix: 409 → auto-merge-retry (re-apply the local field deltas onto serverDoc) or force a save before prepareAndOpen proceeds. | this session (2026-07-12, code-read + D1 evidence) | no |
| **80** | ~~BRAND-FIT-READBUTTON-PALETTE-001~~ **CLOSED 1.51.392 (commit 1f28ab7)** — the Read-from-Cloud restore (occ-2) now APPLIES `meta.styleConfig` via the same setter as cold-restore (`Ja`/`wa` binding verified in-scope before patching; app.js surgical ×1 mirrored in app.src.js). Original scope: — the Read-from-Cloud path (occ-2) now ARMS the brand-fit flag/checkbox (1.51.365 `__bf2`) but still does not APPLY `meta.styleConfig` (only cold-restore occ-1 calls `wa(styleConfig)` per BRAND-FIT-OPEN-001). Tracker Open always reloads → occ-1 covers it, so this only shows when a user presses "Read from Cloud" manually on a brand-fitted app. Fix = mirror the occ-1 styleConfig apply at occ-2. | this session (2026-07-12) | no |
| **84** | ~~MATRIX-CONFLICTS-OWNER-CALL-001~~ **CLOSED 1.51.397 (owner rulings 2026-07-13)** — (a) CORE-COMP-ROWSPEC-001: owner clarified TABLE ROWS vs cell LINES are different dimensions — the spec is 3-4 TABLE rows with every cell rendering in <=2 column-width lines; matrix caps.core_comp_data_rows 2→4 + _min 3, prompt block rewritten, 290 guard floor back to 3, corecomp test restored (2 rows retries). (b) EU-CITIZEN-CANON-SWEEP-001: "matrix is correct" — canonical is "EU citizen"; swept 10 app.src/app.js sites + 4 sidecars (outputs/placeholders/prompt examples lowercased; dict keys gained a lowercase sibling and 292's reverse map an alias so LEGACY stored "EU Citizen" stays recognized). (c) ROLE-BULLETS-GEN-CAP-001: role_bullets_max is a GENERATION cap — wired into the gold prompt block ("at most 3 bullets per role"). | owner rulings on the containment audit | 2026-07-13 |
| **85** | ~~DENSITY-WIP-RESCUE-001~~ **CLOSED 2026-07-13 (density session, same day)** — PR #343 was CONFLICTING because its first commit duplicated 07df2e5 already on main; the ONE unique commit 084aa06 (the rescued density-script fixes) cherry-picked onto main CLEAN as **a7cbdfe**, suite 1257/1257, PR #343 closed with explanation, branch `wip/1.51.396-matrix-containment` deleted. The rescued content: PUB-CANONICAL-ORDER-001, DASH-MATCH-001 + hyphenation-tolerant cell fallback, bidirectional header-ink audit probe, refined narrow-cell label lever (hard 34 / prefix strip / min-12). | this session (2026-07-13, nothing-lost sweep) → density session | 2026-07-13 |
| **87** | ~~SETTINGS-PERSONAL-DEDUP-001~~ **CLOSED 1.51.438-personal-dedup (2026-07-13, shift worktree; PR #344 MERGED 2026-07-13, live via auto-deploy)** — the 5 contact fields (Location, Citizenship, Email, Phone, LinkedIn) were editable BOTH in Settings → STANDARD → Personal AND in the "Review & Edit my data" dialog (same personalInfo keys, two surfaces). A prior sandboxed session investigated only (two screenshots, traced both render sites, pulled the SETTINGS-PERSONAL-FREEZE-001 precedent; no live browser, so no app.js edit under hotfix discipline). This session removed the Settings copies — static render-tree deletion in app.js (count-1 anchored, 611 chars) mirrored into app.src.js; Full Name + Headline kept (owner call). `antcv-quick-contact-collapse.js` patched so the owner's 2026-06-17 identity lift survives zero contact rows (Full Name placeholder fallback; collapse group skipped when empty); hook-283's localStorage restore path untouched. Verified: suite 1257/1257, freeze diag FROZEN:false 0/8 misses, churn stable, NEW `pwa/test/diag-settings-personal-dedup.mjs` 7/7 GREEN. Owner visual pass welcome (one glance at the slimmer Personal tab). Detail: ACTIVE_BUGS 2026-07-13. | this session (2026-07-13, owner ask) | 2026-07-13 |
| **88** | ~~PHOTO-REMOVE-LAST-RESET-001~~ **CLOSED 1.51.458-remove-last-reset (2026-07-13, owner report)** — the 1.51.418 carousel's ✕ on the LAST library photo left the previously ACTIVE photo in place ("use ↺ Reset" by design); owner: removing both X-ed photos must return the block to the default ant. `removeCurrent()` now drives the app's native (hidden) Reset when zero entries remain — same mechanism as "↺ Reset", idempotent with the PHOTO-RESET-CLEAR-001 capture listener. Non-last ✕ unchanged (neighbour activated). NEW `diag-photo-remove-last-reset.mjs` 9/9 GREEN on a synthetic panel (live account never touched); suite 1257/1257. Owner glance on live welcome. | this session (2026-07-13, owner report) | 2026-07-13 |
| **90** | ~~PHOTO-CAROUSEL-SHAPE-001~~ **CLOSED 1.51.499-owner-followups (2026-07-13, owner report; PR #347 MERGED, live via auto-deploy)** — the 1.51.418 carousel overlay `<img>` hardcoded a circle, so Square/Rounded/Pentagon photos previewed round. `antcv-photo-library.js` now reads `personalInfo.stylePrefs.photoShape` (`readPhotoShape`) and mirrors it onto the pic (`applyShapeTo`: square→0, rounded→12px, pentagon→clip-path, else 50%), keyed into the overlay signature + `antcv:photo-shape-changed` listener. Style-only writes. NEW `diag-photo-carousel-shape.mjs` 8/8. RE-LAND: first orphaned on the already-merged PR #345 branch (1.51.498), never reached main; re-landed off current main. Suite 1257/1257. | this session (2026-07-13, owner report) | 2026-07-13 |
| **91** | ~~SPEC-SLOGAN-LANG-001~~ **CLOSED 1.51.499-owner-followups (2026-07-13, owner report; PR #347 MERGED, live via auto-deploy)** — the CL slogan input PLACEHOLDER (`subtitleFallback` in `antcv-cl-slogan-control.js` + `-element.js`) read the stale `kernelShowcase` (raw generation-language, never re-langed) FIRST, so a Danish specialization triad showed on an EN/ES/ZH app. Now reads `personalInfo.specialization` FIRST (current-ribbon, kept live by the babel-fish translate pass); `kernelShowcase` last resort; + a non-Latin script guard blanks a Latin candidate on a zh/ar/he/ru ribbon → generic hint. NEW `diag-slogan-placeholder-language.mjs` 6/6 GREEN. Suite 1257/1257. | this session (2026-07-13, owner report) | 2026-07-13 |

### TO-DO SUMMARY twins of the rows above (verbatim)

| # | One-line remaining scope | TO DO |
|---|---|---|
| 9 | Cluster demand model — worker pipeline + nightly recruitment-site refresh | **CLOSED 2026-07-13 (in production)** — verified: 2026-08-23 (E1 sweep, CI nightly) writer pipeline RE-CONFIRMED on current source: `recomputeClusterTop20` at `access-relay/src/index.js:2496`, invoked on JD extraction at :2574 and :2900, rank-scaled (NOT flat) weight comment at :2592 (`RESEARCH_WEIGHT*(21-rank)/20`); still CLOSED/in-production; prior: verified: 2026-07-07 (E1 sweep) worker WRITE pipeline now EXISTS (recomputeClusterTop20, access-relay:2113, invoked :2191 on JD extraction, source='jd'; CLUSTER-DEMAND-GLOBAL-001 shipped 2026-07-05 — supersedes the old "no worker writes table" note). **Weekly demand-seed CLIENT refresh now on a LIVE trigger (scheduled task antcv-demand-seed-weekly, Fri 22:00).** Run 2026-07-13 (1.51.478-demand-seed-refresh): re-researched all 9 clusters (WebSearch fallback — CSE still 403/no token), applied 5 bounded evidence-backed shifts (pm_process +AI/EU-AI-Act item; executive/software/HR/data AI+cybersecurity rank bumps), 4 clusters confirmed unchanged; docs/analysis/cluster_top20_research_2026-07-13.json. **D1 research WRITER now BUILT 2026-07-13** (was the last-open leg): POST /api/cluster-demand-research (access-relay, token CLUSTER_RESEARCH_TOKEN) + insertResearchQualifications (rank-scaled weight `RESEARCH_WEIGHT*(21-rank)/20` so order is deterministic AND ≤0.4 < real-JD 1.0) + scripts/cluster-demand-research-push.mjs (the routine's write step, replacing the 2026-07-10 manual D1 write). Tests: cluster-demand-research-writer.test.mjs (12) + cluster-demand-research-push.test.mjs (6); full access-relay suite 79/79. **DEPLOYED 2026-07-13** — access-relay deployed (deploy.yml), CLUSTER_RESEARCH_TOKEN secret set by owner; live-verified /health 200 + POST /api/cluster-demand-research 401-gated (route live, fail-closed). **FUSE shipped 2026-07-13 (1.51.558-cluster-fuse, owner "fuse the deterministic and research list so nothing is lost")**: (a) WRITER union — insertResearchQualifications now RETAINS a qual dropped from the new top-20 at a floor weight (RESEARCH_WEIGHT/40, below rank-20) instead of a destructive replace, so nothing leaves application_qualification and a dropped qual can resurface; (b) CLIENT union — app.js/app.src.js __clusterRule now UNIONs the live D1 top-20 with the static deterministic SEED (dedup by normalized text, live leads, SEED-only appended, slice widened 20→24) instead of live-OR-seed. Tests: writer 14/14, cluster-rule-live-preference 8/8 (incl. executable union lock), pwa suite 1260/1260. **COMPLETE + IN PRODUCTION 2026-07-13**: CLUSTER_RESEARCH_TOKEN set (relay secret + Windows User env var); access-relay redeployed with the UNION writer; FIRST PUSH RUN — `total_inserted:181` (pm_process 21 = 20 fresh + 1 retained), verified in D1: rank-scaled weights step 0.400→0.020, "Obsolescence management" retained at floor 0.010 (nothing lost), rollup rank-5 = the new "AI/GenAI + EU AI Act" item (deterministic order). Client union live-verified on antcv.pages.dev/app.js?v=1.51.579-cluster-fuse. Future Fri runs write D1 automatically (SKILL.md step 4b — URL hardcoded, token from env). No wrangler cron needed. See docs/deployment/google-cse-setup.md §8 + SESSION_LOG_2026-07-13. **ROW CLOSED.**  **REFRESH 2026-08-26 (weekly run, `1.51.4386-demand-seed-refresh`, PR #363 / `3179f67`):** all 9 clusters re-researched, 7 changed / 2 re-confirmed; 3 promotions (photonics packaging 15→9, consulting change-management 15→11, finance business-partnering 8→4), 1 add+drop (engineering_software gains an enterprise-platform item, drops perf profiling), 5 wording broadenings. Seed verified row-for-row equal to the artefact (180/180); suite 1621/1614/0/7; D1 push `{"ok":true,"clusters_updated":9,"total_inserted":226}`. Row STAYS CLOSED (this is data refresh of a closed feature, not a feature increment). One NEW row filed against the routine's search leg — see row 102 (DEMAND-SEED-SEARCH-TOKEN-MISSING-001). |

## CLOSED (evidence; most recent first)

- **2026-08-21 — LLM-COST-EFFECTIVE-FROM-001: the D1 price lookup now honours `effective_from` (access-relay, DEPLOYED `d447d450-5c3b-4d50-a5b8-cbed2eff4068`, commit `60d8b07`).** `telemetry.js` prices every `llm_call` from `llm_provider_costs` before the row is written, but the lookup was `WHERE provider = ? AND model = ? ORDER BY effective_from DESC LIMIT 1` with **no cutoff** — the newest row won unconditionally, so `effective_from` meant insert order rather than start date, and a future-dated row would price today traffic from the moment it was inserted. That defeats the column purpose: staging an announced price change ahead of its start date. Found while live-verifying the 08-20 gemini/mistral correction against production D1, where the 2024-05-01 `gemini-2.5-flash` row at {0.075, 0.30} proved to be the actual source of every logged gemini cost (the PWA meter then carried {0.15, 0.6} and never reached the log). Nothing was mispriced — the 08-20 correction was back-dated to 08-20 — and the guarded query was **verified against production D1** to return the same four in-traffic rates as the unguarded one: `gemini-2.5-flash [0.3, 2.5]`, `mistral-large-latest [0.5, 1.5]`, `claude-sonnet-5 [3, 15]`, `gpt-5.4-mini [0.75, 4.5]`. NULL `effective_from` stays always-in-effect; when every row is future-dated the relay falls through to its own `model-rates` table, never to the client self-reported cost. `pwa/test/llm-cost-effective-from.test.mjs`, 5 tests, negative control 3/5 fail without the guard. Suite 1946/1946 at ship.

- **2026-07-22 — LANG-GUARD-KEY-001: generation no longer "starts in Chinese" (PWA `1.51.2200`, eb36a8e, live-diagnosed).** The three loss-guards (CL prose / outcomes / core competencies) cache the last real generated content keyed by `company|role` and re-inject it during a later generation’s skeleton window. They were LANGUAGE-BLIND, so a Chinese cache poisoned a fresh English generation ("starts in Chinese, switches to English") — the generation-time leg of the same family as APP-SWITCH-CONTENT-LANG-DETECT-001 (`1.51.2012`, selector-side). Fix = append the current output language to each guard’s cache key (`company|role|lang`) so a generation only restores from a same-language snapshot; the stale lang-less zh caches no longer match. Test `lang-guard-key.test.mjs` (12) + updated guard behavioural tests; suite 1401/1401; live-verified the new `…|en` key does not match the CJK caches.

- **2026-07-22 — EXPORT-PREVIEW-ZOOM-DECOUPLE-001: export preview no longer follows the editor zoom and stops chopping pages ~half (PWA `1.51.2085`, 745b88d, live-verified).** One root cause: the editor bakes its zoom as an inline `transform: scale()` on `.antcv-preview-paper` (`app.src.js` ~51102) and the export modal clones the paper via `p.outerHTML`, so the clone inherited the zoom AND double-scaled it against the modal’s own fit zoom (a transform’s layout box stays full-size → page-row desync → half-page clip). Fix = `.antcv-preview-paper { transform: none !important }` in the gate srcdoc `<style>` (beats the inline transform); the preview now fits independently via body `zoom: var(--antcv-fit)`. Test `export-preview-zoom-decouple.test.mjs`; suite 1378/1378; live on deployed 2085: computed transform `none` (DECOUPLED) with editor at `scale(0.955)`, single fit `zoom=0.512`.

- **2026-07-22 — HTML-TO-PDF-001: JD-analysis report exports via CloudConvert (PWA `1.51.2072`, docx-worker `1.14.162`, shipped + live-verified).** Owner: "add the analysis to export with CloudConvert." The analysis report printed via offscreen-iframe `window.print()` (ATS-illegible, no ToUnicode CMap); now it renders server-side through the docx-worker’s new `POST /generate-analysis-pdf` (CloudConvert Chrome HTML→PDF, `convertHtmlToPdf`), same gates + BYOK as `/generate-pdf`. PWA `exportPdf()` tries the worker first (gated by `__antcvUseServerPdf`+`isPdfWorkerAvailable`) and keeps the browser-print path as `printViaIframe()` fallback. Test `html-to-pdf.test.mjs`; worker CI 17/17; PWA 1365/1365; live `/health` `1.14.162-analysis-html-pdf`; direct POST = 200 `application/pdf` 30KB; driven e2e on the deployed PWA — POSTed to `/generate-analysis-pdf`, downloaded the PDF, `window.print` 0 calls.

- **2026-07-17..19 — Brand-colour engine + application data-hygiene family (desktop session; 7 PWA versions + 3 relay fixes, all deployed + live-verified).** Worker exonerated (`palette.js`/`mergeStyle` honour `payload.style`); all causes PWA/relay-side. **BRAND-EXPORT-PARITY-001** (`1.51.1544`): `buildStyle` resolved `--header-bg`/`--sidebar-bg` from `document.body` (package token) while an active brand sets those vars inline on the paper-WRAPPER + `:root` has a default, so export reverted to the package band even for a brand-fitted app; an active brand now wins over the token. **BRAND-PREVIEW-PARITY-001** (same): publish `antcv:brandV2` on a FRESH apply, not only restore. **BRAND-PERSIST-PERAPP-001** (same): stamp `meta.brandV2`, both restore gates arm on it → a reloaded saved app repaints. **Relay (deployed):** **HYGIENE-CATEGORY-DOWNGRADE-001** (a JD-less save must not downgrade a real targeted app to unsolicited — the root of the live Ibsen row stored unsolicited, which cleared the JD on reopen), **DEDUP-BY-EMPLOYER-ROLE-001** (dedup by employer+role, not the JD-text hash — killed the duplicate-row spawning), **TARGETED-SENTINEL-001** (`targeted` is a valid non-unsolicited placeholder, auto-upgrades). **Client:** MANUAL-SAVE-CATEGORY-001 (`1.51.1576`), AUTO-COMMIT-FRESHEST-001 (`1.51.1580`, sections were committing to a stale duplicate → "regen doesn't stick"), MERGE-COMPONENT-SWALLOW-001 (`1.51.1557`). Data repair: Ibsen row 796 given the real SBC JD + `program_management`; dup row 809 deleted. Full detail in ACTIVE_BUGS top block + `docs/plan/AntCV_Generation_Upgrade_Plan_2026-07-17.md`.
- **2026-07-19 — AUTO-ANALYSE-ON-JD-LOAD-001 (`1.51.1768`).** Analysis now auto-runs when a JD is loaded (headless runner in `antcv-analysis-panel-jd-block-356.js`), not only after opening the Analysis panel; shares the `AUTORUN_KEY` fingerprint (≤1 LLM run per JD). See FT-AUTO-ANALYSE-ON-JD-LOAD. (Transient error follow-up = OPEN row 93.)
- **2026-07-19 — SALARY-ENVELOPE-CHECK-001 (`1.51.1794`).** The Job-Tracker salary check now runs even when pay is not stated (market ESTIMATE, suffixed `(est)`) and compares stated-OR-estimated against the ~55-75k DKK/month Dream Envelope. See FT-SALARY-ENVELOPE-CHECK. Note: `1.51.1794` was ALSO used by a parallel session's APP-SWITCH-CONTENT-LANG-001 — a version-number collision (both live under distinct suffixes; flagged in ACTIVE_BUGS).

- **2026-07-21 — EXPORT-PDF-PANEL-WORKER-001 end-to-end LIVE-DRIVEN (verification only, no ship).** The 07-19 reconcile registered the fix (`1.51.1556`, bea9cf6) on live-served + suite evidence; the end-user click-through had never been driven. Driven on live `antcv.pages.dev` (`1.51.1943`) against the owner s signed-in account: fixed gate confirmed running (`__antcvPdfPreviewGateInstalled = 1.51.1556-pdf-panel-worker`) and the `app.js` policy exposure confirmed to have survived later app.js re-touches (`__antcvUseServerPdf()` -> `true`). Root cause reproduced (Preview tab: export button present -> Analysis tab: absent, while the preview paper + export FAB remain). **Result: "Save as PDF" with the Analysis panel open called `exportPdfViaWorker(doc=cv)` -> real POST to `docx-worker...workers.dev/generate-pdf`, modal auto-closed on success, `window.print` called 0 times.** Follow-up (only one, minor): the modal s double-`rAF` build stalls in a throttled/backgrounded tab (`island-raf-freeze` class) — consider a `setTimeout` fallback in `antcv-pdf-preview-gate.js`; no evidence of a live user hitting it.

- **2026-07-21 — SAVED-APPLICATION LIFECYCLE CLUSTER + LINKEDIN-CARD-EXTRACT-001 (desktop session, Opus 4.8; full prose in the ACTIVE_BUGS top entries).** Five owner-reported defects on the saved-application / JD-list path, all live-verified: **SWITCH-OPEN-JDONLY-001** (`1.51.1708`, 35b6120) — History Switch refused JD-only apps (cloud row has `cv_sections:null`); `__hasReal` widened to open any record carrying a JD/identity, me() floor supplies the template. **WIPE-NONDESTRUCTIVE-RESTORE-001** (relay 0af16b9, deployed `27def9ec`) — the ROOT of "many apps have null sections": `wipe-generated` NULLs the ACTIVE app at regen START, so an abandoned/mis-routed regen loses the CV forever. New `cv_sections_bak`/`cl_sections_bak` columns (ALTERed live on `ant_memory`); the wipe snapshots before nulling; the EXPLICIT single-app GET restores + self-heals (never the `/api/prefs` active seed, which would re-seed contamination). Verified e2e: simulated wipe on #796 → reopen restored 15 cv + 8 cl, bak cleared. **DEDUP-ROLE-NORMALIZE-001** (`1.51.1800` 1ca51c9 + relay) — duplicate apps (Aimpoint ×3, Demant ×4, 3Shape ×2): `jd_hash` goes stale vs `jd_text` AND the employer+role dedup used an exact `jd_role` match that missed dash/suffix variants. Now matches a NORMALIZED role key within the employer; cleanup merged **11 duplicate rows** (freshest-with-content kept, active app protected, empty-role `主动申请` placeholders excluded) — 0 dup groups remain. **APP-SWITCH-CONTENT-LANG-001** (`1.51.1800`, 1ca51c9) — the selector was set from `jd_language` (the JOB's language) not the CV's content script; now derived from the content, falling back to `jd_language`. Entry-point half of the Chinese-stuck family (content-heal half = BABEL-* `1.51.1818`/`1.51.1838`, parallel session — complementary). **LINKEDIN-CARD-EXTRACT-001** (`1.51.1882`, 492ca1e; cv-proxy `105bfd9c` + demo-proxy `c37d0871`) — the LinkedIn guest fragment has NO `<title>`, so the tracker's manual-add Company/Role pre-fill went blank. Role/company now read from the top-card, preferring the body-confirmed trailing employer over the recruiter org-name (owner-confirmed: "DTU Wind fetched"). Remaining legs filed as rows 92-94.

- **2026-07-13 — Owner rulings on the matrix conflicts (row 84) shipped as 1.51.397-matrix-rowspec.** CORE-COMP-ROWSPEC-001 (3-4 TABLE rows / <=2 rendered lines per cell — two different dimensions; matrix, prompt block, 290 floor, tests all aligned), EU-CITIZEN-CANON-SWEEP-001 (canonical "EU citizen" everywhere new; legacy capital form still matched), ROLE-BULLETS-GEN-CAP-001 (generation cap in the gold prompt block). quality_pass trims to the new cap automatically (reads caps.core_comp_data_rows from the site).

- **2026-07-13 — Desktop refresh + nothing-lost sweep (owner ask "refresh my desktop and make sure nothing is lost").**
  - **Clone:** was parked on the already-merged `fix/cse-search-brave-first-regression` (PR #340) with mixed staged/unstaged work while a parallel session shipped 1.51.396 in the SAME checkout mid-sweep. End state verified: `main` checked out, clean, == origin (07df2e5); every tree change accounted for by diff — either contained in origin (PRs #340/#341 + 07df2e5) or rescued to `origin/wip/1.51.396-matrix-containment` (row 85). No force, no stash, conflict-marker + syntax scans clean before the rescue commit.
  - **Browser:** opened the app in the owner's desktop Chrome — 1.51.396-matrix-containment live, signed in, profile + sections (29 KB) + NVIDIA JD (4840 chars) intact, tracker button present, SW active. Pre-existing tabs need one manual F5 (extension can only drive tabs it opens).

- **2026-07-13 — JD-REMOVE-STICKY-001: removed JD no longer re-seeds on refresh (desktop session, owner repro; commits f231723 + c90b63e = 1.51.395-jd-remove-sticky).** The JD chip's "✕ Remove" cleared only local state (`Dt(null), Ft(null)`); the OPEN-JD-VISIBLE-001 cloud-restore seeding re-added the same `jd_text` every refresh — and that seeding is exactly what the tracker Open flow depends on, so removal and Open had to be DISTINGUISHED (the owner's ask). Device-local tombstone: Remove writes `antcv:jdRemoved` (`<len>:<first-48>` of the removed jd) + blanks `antcv:lastJdText`; cold-restore (occ-1) suppresses JD-file seeding on a tombstone match UNLESS `antcv:lastJdText` equals the jd — only a tracker Open/Reopen stages that right before its reload (`prepareAndOpen`), so that match = deliberate re-stage → tombstone cleared, seeds; the explicit "Read from Cloud" (occ-2) always clears + seeds; a DIFFERENT jd never matches, so uploads/other rows seed normally. Cloud `jd_text` is untouched (the tracker keeps the row's JD; signals seeding and the lastJdText mirror stay correct while suppressed). app.src.js + 4 surgical minified-app.js anchors (parse-gated); `jd-remove-tombstone.test.mjs` executes the predicate from the deployed bundle's own bytes (suppress / re-stage / different-jd / repeat-refresh). Suite 1257/1257. Shipped through a moving origin (PR #339 merge + night-shift docs mid-flight): stash-WIP → rebase → push; pre-push gate caught the version-override `?v` (c90b63e). Owner live re-check = row 83.

- **2026-07-13 — GOLDEN GATING MATRIX: role control + da/es/zh canon (owner: "make sure your work fits in the golden gating matrix role control, and if it does not already cover for it - I want also danish spanish and chenese canon"; 1.51.394-role-canon-lang).**
  - The matrix had NO roles section — the role-identity work (SAME-ID 1.51.353, BASE-ID/GEN-ID 1.51.358, ROLE-COVERS 1.51.362) was enforced in code but not expressed in the control site. **gold-rules.json 1.4.0 adds `roles`**: the identity rule (one visible role per canonical id; suffixed twins + gen schema ids collapse onto the canonical id) + **`canon_titles` — canonical titles for all 13 canonical role ids in en/da/es/zh** (da from the verified native gen, zh from owner pins 变更请求负责人/系统架构师/学生会代表/球队运营经理（协会志愿工作）+ established forms, es new this session).
  - **ROLE-CANON-LANG-001 enforcement** (antcv-sections-normalize-415.js 1.51.394): new `roleCanonTitles` pass runs AFTER dedupeRoles on en/da/es/zh ribbons — deterministic titles from the gold table (runtime gold-rules.json fetch, mirrored fallback), so LLM title drift can never fork a position across languages; he/am/ar keep the translate output; merged titles with more &-segments than the canon are never overwritten. CW-CANON-LANG now reads the same table (da/es localized for the volunteer role; was English-forced).
  - **Suite gate** `pwa/test/unit/gold-role-canon.test.mjs` (4 tests, in the pwa suite): all 13 ids × 4 languages non-empty; the sidecar fallback is an EXACT mirror of the gold JSON; ASCII-hyphen typography + owner pins locked; pipeline wiring (after dedupe) asserted. Suite 1249+4 green.
  - Narrative row added to GOLD_STANDARD_LOCKER.md § 3b. Follow-up: row 82 (audit leg in gold_audit.py once the concurrent WIP lands; owner eyeball of es/zh wordings).
- **2026-07-13 — NIGHT SHIFT hardening + scheduled-routine framing + weekly relay cost-quality tune (desktop, owner ask "fix 1-5 + frame scheduled routines + relay quality-cost reviewed & modified weekly"; full detail `docs/qa/NIGHT_SHIFT.md` + `docs/qa/SCHEDULED_ROUTINES.md`).**
  - **Shift-hardening leftovers #1-#5.** #1 CLAUDE.md § Sync discipline now carries the CLAIM-A-LANE step; #2 `shift.mjs` reads the ledger from `origin/main` (fetch + `git show`), not the local working copy — a dirty tree (can't `pull --rebase`) can no longer cause a stale read / double-claim (all commands rebased on `originLedgerClaims()`); #3 NIGHT SHIFT pointer added to 5 standing prompts (BACKLOG_RECONCILE, NEXT_SESSION_PROMPT, NIGHTLY_PAGES/JD_CAPTURE/PREVIEW_BUTTON_AUDIT); #4 pre-push hook soft-reminds (never blocks) a versioned pwa push with no active claim; #5 documented origin-authoritative reads + intentional never-reuse of ranges.
  - **SCHEDULED_ROUTINES.md (new).** Register of every recurring routine + the STANDING RULE that each syncs + claims before pushing; each routine's own prompt should point here.
  - **RELAY-COST-QUALITY-TUNE-001 (new weekly routine).** Reviews the week's `llm_provider_health` + `llm_quality_signals` per role AND modifies the router head (`MODEL_ROLES` / `roleHeadOrder()`) so cost-per-acceptable-output trends down while quality holds — bounded per-role flips, adequacy floor + keep-fallback guardrails, dated before→after reports with a logged rollback. **Automation SHIPPED:** `scripts/relay-cost-quality-tune.mjs` reads `MODEL_ROLES` + the `/api/llm-health` snapshot and emits the proposed `MODEL_ROLES` diff + per-role rationale (pure core unit-tested 8/8; `--apply` writes both wrangler.toml but never deploys — apply+deploy stay agent/owner-gated since it affects every user's generation).
  - Registered in FEATURES_REGISTRY (FT-SCHEDULED-ROUTINES, FT-RELAY-COST-QUALITY-TUNE) + ACTIVE_BUGS. Docs + coordination tooling only; no cache-bust.

- **2026-07-13 — Register rows 78-80 closed (owner ask "fix 78,79,80"; commit 1f28ab7 = 1.51.392-row-fixes; live-verified on the deployed build).**
  - **Row 79 / SAVE-409-MERGE-001** — tracker-doc save conflicts no longer drop local edits: one automatic rebase onto the server doc (per-row maps overlay incl. signals/sigfiles/pin/park/discovered; server-only rows survive; local-only rows appended; envelope local-wins) then re-PUT with the server rev.
  - **Row 80 / BRAND-FIT-READBUTTON-PALETTE-001** — occ-2 Read-button restore applies meta.styleConfig (occ-1 parity).
  - **Row 78 verification** — full Open flow exercised live on 1.51.392 (fresh Browser-pane device): JD switched to the opened row (NVIDIA 4840 chars), brand-fit flag + checkbox armed, 📋 button opens via the delegated listener, and the relay per-device pointer row was created by the island's device-stamped Open (D1-verified). Suites 1249/1249. Shipped through an ACTIVE same-tree race with the 1.51.393 session — their in-flight ?v tag lines kept verbatim per the same-tree-race rule.

- **2026-07-13 — Photo panel: CJLR leak guard + upload-button fuse + strip layout (desktop session, owner report; commits 2f65c3d/6d0d957/caff74e/0e2dcc4 = 1.51.390/391-photo-fuse, 7d5d3c2 = 1.51.393-photo-strip; feature FT-PHOTO-LIBRARY; memory same-tree-commit-race).**
  - **PW-CJLR-PHOTO-LEAK-002 (1.51.390)** — the profile/work-style CJLR cycler got stuck over the upload menu's Profile Photo card. Root cause: the photo-library strip (1.51.382) pushed the card to >=3 buttons — exactly `panelRows()`' section-row threshold — and the PW-CJLR-PHOTO-LEAK-001 guard `/^profile\s*photo\b/` failed because sibling-div textContent concatenates WITHOUT spaces ("Profile PhotoDefault photo active…" has no word boundary after "photo"). Fix in `antcv-profile-workstyle-cjlr-238.js`: boundary-free guard + belts (reject any ancestor row holding `[data-antcv-photo-library]` or a file input; never anchor on strip buttons; stale-button sweep extended to remove already-leaked cyclers).
  - **PHOTO-BTN-FUSE-001 (1.51.390, owner ask)** — one upload control. "＋ Add photos…" is the single path: multi-select, every image COMPRESSED under the 500KB cloud entry cap (600px downscale + JPEG re-encode, stepping quality/scale until it fits — oversized images are no longer SKIPPED) then saved to the library, first becomes active through the app's own hidden-input pipeline. Native single-file "Change photo" hidden as redundant (app.js untouched); "☆ Save current" removed (adding IS saving); the Add button stays visible at MAX_ENTRIES (oldest entries evict). **PHOTO-RESET-CLEAR-001** — the native Reset (restores the embedded default ant) now ALSO empties the library, via a capture-phase click listener scoped to the photo panel. Retro-registers PHOTO-LIBRARY-001 (1.51.382, never given a registry row) as FT-PHOTO-LIBRARY.
  - **PHOTO-STRIP-RESET-001 (1.51.393, commit 7d5d3c2, owner follow-up)** — the "Library:" label span is removed, and the strip renders its OWN "↺ Reset" in a column directly UNDER "＋ Add photos…" (same `btn()` factory → identical teal-outline style; column flex stretches both to equal width). The strip Reset clicks the app's own native Reset (restores the embedded default ant through the app's normal path) then empties the library; the native Reset joins "Change photo" as hidden-not-removed (`hideNativeButtons`, strip-owned buttons skipped). Always visible — a no-op when the default ant is active and the library is empty. Suite 1249/1249.
  - **SAME-TREE COMMIT RACE (process incident + fix, 1.51.391)** — the concurrent cell-cascade session committed+pushed 633fbd6 90s before this session's commit, sweeping this session's in-flight version plumbing (seed/CACHE/TARGET already at 1.51.390) into its commit; production briefly referenced the new photo `?v` tags BEFORE the sidecar content landed (window clients would pin stale bytes under new tags + same SW CACHE name), and this session's revert of the other session's WIP `?v` line briefly regressed the bullet-targets cache-bust (fixed 6d0d957). Unpinned with a fresh full quartet 1.51.391-photo-fuse (caff74e); the pre-push gate then caught the version-override.js `?v` drift (0e2dcc4). Lesson (memory same-tree-commit-race): never revert another session's plumbing line — bump forward; the gate catches asset-without-?v-bump but NOT tags-shipped-before-content. Suite 1245/1245. Owner visual verify = row 81.

- **2026-07-12 — Job-tracker Signals: attach MATERIALS + three Open-flow fixes (desktop session, owner repro; commits a681e82 + 7348d72; memories signal-materials / active-pointer-device-stamp).**
  - **SIGNAL-MATERIALS-001 (1.51.364, commit a681e82)** — the Signals column was manual-text-only; every row now has a 📎 that attaches a file (PDF/text/image-scan) through the app's own extractor chain (PDF.js → LLM document → vision OCR), stored per row as `doc.sigfiles[uk]=[{name,text,added}]` (extracted TEXT only, 6k cap/file, blank lines collapsed so app.js's ADDITIONAL-SIGNALS regex keeps the whole block) with removable chips. Typed signals + materials compose into ONE block consumed everywhere: Open's `ADDITIONAL SIGNALS (owner-added)` in supporting_context (→ upload-panel additional info + in-app JD analysis), Top-5 (fit %, 📌 card line, ✨ refine + tracker Ask-AI), and the nightly gen-runner (`_signals_for(uk)`, prompt cap 2500→8000). Suite 1222/1222; live-verified (22 rows × 📎 render).
  - **STALE-JD-ON-OPEN / PARALLEL-GEN-POINTER-002 client half (1.51.365, commit 7348d72)** — owner opened Tech Mahindra, panel still showed NVIDIA. Root cause (D1-verified): the relay PREFERS the per-device active pointer; the island's createApplication/setActive sent no `device_id`, so only the legacy global row moved and the desktop's own device row (still 724/NVIDIA) won on reload — a concurrent gen-runner active-guard restore of the global pointer compounded it. Both island calls now stamp `AntcvJdScope.deviceId()`.
  - **BRAND-FIT-OPEN-002 (1.51.365)** — Open applied the sampled palette (meta.styleConfig) but the upload panel's session-only 🎨 Brand-fit CHECKBOX stayed off → next generation ran without the brand-fit rule. Both restore paths (cold + Read-button) detect intent (`meta.styleConfig` or the `

BRAND-FIT:` line in supporting_context), arm `window.__antcvBrandFit`, tick `input[data-antcv-brandfit]`; `defaultChecked` reads the flag for late renders. app.js surgical ×3 (script-file patcher, anchors counted 1 each), mirrored in app.src.js.

- **2026-07-11 — Parallel-generation isolation + parallel-session coordination + live-verify (desktop, owner: "run generations in parallel"; full log `docs/qa/SESSION_2026-07-10_PARALLEL_GEN_AND_LANG.md`). This is the SERVER-side foundation the 1.51.365 island device-stamping (07-12 block above) builds on.**
  - **PARALLEL-GEN-POINTER-002 (relay + client, 1.51.259; corrective bump 1.51.260).** The account active pointer is no longer a single `active_application` row — new D1 `active_application_device` (PK user_hash+device_id) gives each device its OWN pointer. Relay `writeActivePointer`/`readActivePointer` (both tables; per-device with legacy-global fallback); client `AntcvJdScope.devQ()` sends `?device_id=` on the prefs/active GETs. Table created live in prod D1. `scripts/tests/relay-per-device-pointer.test.mjs` 7/7. Evolves row 39a (TAB/DEVICE ISOLATION). RULE: never re-add a raw `INSERT INTO active_application … ON CONFLICT(user_hash)`. Corrective bump undid a stale-base merge that regressed the quintet to 245/246 below the deployed 259.
  - **TAB-DOC-ISOLATION wired (1.51.300, commit 3bb486e).** `antcv-tab-doc-isolation.js` (per-tab sessionStorage doc snapshot, shipped 1.51.253) was DEAD — served but no `<script>` in index.html — until wired now. Caught by the new sidecars-live check on its first run.
  - **WIZARD-NEW-USER (1.51.322, commit 3d6997c).** The onboarding wizard never opened for a signed-in new user (Gate 1 skips when authenticated; Gate 2 could only close). Fixed so Gate 2's fresh-user branch opens it when there is no flag + no REAL personalInfo; content heuristic aligned with WIZARD-AUTOLOAD-001. Live-verified by gate simulation. (Distinct from row 6 Wizard/Settings-UX, still TO DO.)
  - **NIGHT SHIFT coordination (tooling).** `docs/qa/NIGHT_SHIFT.md` + `scripts/shift.mjs` (claim a version RANGE from the true git high-water mark + work in a git worktree). SHIFT PROTOCOL in both MULTIRUN work orders + CLOUD_ROUTINE_PROMPT — the process fix for the cross-session version collisions repeatedly seen (e.g. the 245/246 regress).
  - **LIVE-VERIFY.** Manual in-app-Browser-pane procedure (`docs/qa/LIVE_VERIFY_BROWSER_PANE.md`) + automated `node scripts/browser-qa.mjs --only version-live|sidecars-live` (checks in `scripts/qa-checks.mjs`).
  - **BTN-DEAD-CLONE-001 (1.51.365)** — after Open the 📋 Job Tracker button did nothing: an app re-render serializes + recreates it (inline styles survive as attributes, the element click listener doesn't) and `ensureButton` keeps the dead clone as "already placed". Click handling is now a DELEGATED document-level capture listener on `#antcv-job-tracker-btn`.
  - **"No additional signals" was NOT a pipeline bug** — D1 showed the tracker doc had no `signals`/`sigfiles` keys and none of apps 724/794/800 carried an ADDITIONAL SIGNALS block; the seeding works once signals exist (relay upsert refreshes supporting_context on every Reopen). Residual hazard registered as row 79. Follow-ups: rows 78-80.

- **2026-07-13 — Job-tracker: bi-weekly position DISCOVERY + fit-ranked Top-5 pipeline (this session).**
  - **JOBTRACKER-AUTOFILL-TOP5-001 (1.51.388, commit 82d6eaa; spec docs/qa/JOBTRACKER_AUTOFILL_TOP5_SPEC.md)** — new `src/islands/JobTracker/rank.ts` (pure `computeTier`/`fitScore`/`orderTop5`, unit-tested 9/9) wired into JobTracker.tsx. (1) Top-5 is now DETERMINISTIC fit-ranked (pins first, then highest fitScore; closed + parked excluded), recomputed on every doc change so any add re-evaluates against the current Top-5. (2) Pin / Park / Reject split replaces the single "drop": Pin forces into Top-5 (stays in list); Park removes from Top-5 but keeps the row LIVE (not archived); Reject archives WITH a reason → envelope learning AND writes `doc.discovered[key]` so the discovery task never re-proposes it. (3) AUTO-TIER manual adds via `computeTier` (location gate → domain tokens → cluster blend) + learn-from-prior gen-tier + "Review & tailor" next-step. (4) Async LLM refine on add (rides analyzeJd): tier upgrade + extracts location/salary/deadline/hiring-manager + tailored next-step + envelope-conflict flags + auto brand-colour sample; deterministic baseline is the fallback, never fabricates, never blocks. LIVE-VERIFIED on antcv.pages.dev non-destructively (island boots 1.51.388 no errors; 23 rows + 5 cards each with Pin/Park/Reject; Top-5 fit-ranked — rank #6 surfaced above 4/5; Park refills by fit; doc left clean at rev 54). Deployed bundle == committed == fresh rebuild (identical sha256). Remaining: live add-flow test (row 75). Concurrent-session caution held (4 density files left untouched; only my 7 files committed).
  - **JOB-DISCOVERY-001 (scheduled task `antcv-position-discovery`, cron `0 22 * * 0,2` = Sun+Tue 22:00; helper commit d133762)** — twice-weekly agent run finds NEW openings vs the Dream Envelope, dedups, and adds them as PROPOSED rows (group "Proposed", 🔎, fit-tier band) for owner review. `scripts/job-tracker/discover-positions.py` does the safe persistence: `context` (dedup vs rows + a new `discovered` ledger) + `add` (append proposed rows, `queue[uk]=False` so they NEVER auto-generate until the owner flips ⏰, rev-safe PUT). Closes the "nothing sources positions" gap (memory position-discovery-task). Owner tip: click "Run now" once to pre-approve WebSearch/WebFetch/Bash.
  - **PROPOSED-INBOX-TAB-001 (commit 3c67eb3)** — `job-tracker-sync.py pull --render` injects a "Proposed Inbox" worksheet (tier-sorted discovered leads, clickable posting links, first-seen) after "Weekly Tracker"; read-only view (import-xlsx reads only Weekly Tracker); unit-verified on a synthetic workbook. Renders on the next Drive-connected sync (Drive is not filesystem-mounted in the sandbox).
  - **BANNED-DASH-MEASURE-001 (commit d162e47)** — `export_pdfs.py` now scans the rendered PDF for banned em/en/figure dashes + U+2010/2011 + minus (deliverable-standards §6/§7). Caught + fixed two live leaks (stale fixture clSlogan en-dash; app 792 CV U+2010). Full persisted batch (apps 790-812) re-verified full-fidelity in the density-era worker (1.14.151): 2pg CV / 1pg CL, AI-notice + sidebar-spine present, 0 blank pages.


- **2026-07-03/04 owner live wave 2 (1.51.113-117, this session):**
  - **MERGED-TITLE-JOIN-001 (1.51.113, rule 41)** — deterministic role-merge joins with " & " (closes row 28's rule-17a render leg; Innoviz ORDER confirmed correct by owner).
  - **CL-GUARD-SKELETON-CAPTURE-002 (1.51.113)** — guard classifier: ANY bracketed template segment in guarded CL prose = placeholder; poisoned buckets purge; stale row-restores now AUTO-HEAL (kills the re-clobber loop that forced same-tick edits; row 31 leg (b) practical mitigation).
  - **Hidden-group family (1.51.114-117, spec rules 42/42a/43/44)** — per-app "Hidden - <category>" review rows; least-space restore; long-press/right-click hide menu (per-token + whole element + Restore entries); visibility analytics -> gen-prompt feedback (VISIBILITY-FEEDBACK-001); UNDO for hide + sidebar/table resize (roller-driven); RICHBLOCK-SHAPE-001 root-caused live (tools = rich_block, visibility = section-level map) and the whole family made shape-aware; browser diag harness diag-longpress-hide.mjs 6/6.
  - **SIDEBAR-LINE-ECONOMY-001 (1.51.113, rule 40)** — the owner's packing technique as a gen rule (long+short adjacency, hide irrelevant, compressed forms).
  - **APPJS-REGEX-BACKSLASH-REPAIR (1.51.117)** — app.js CV-PLACEHOLDER-DROP-001 regex had lost its backslashes (dead in prod); restored + test-locked.

- **2026-07-04 reconcile — NIL-campaign legs shipped (1.51.103-116 + wk 1.14.126-131, parallel live session).** Discrete items now closed; rows 26/28/29/31 stay OPEN for their remaining gen/render/row-repair legs:
  - **META-revert writers killed** — META-DOWNGRADE-GUARD-001 (1.51.105, 277's GET mirror refuses a company→empty/"Unsolicited" downgrade) + META-DRIFT-GUARD-002 (1.51.108, the second unguarded cloud-adoption block — an appjs/appsrc mirror divergence — guarded in BOTH bundles). Locks: `meta-downgrade-guard.test.mjs`, `meta-drift-guard-both-blocks.test.mjs`. NIL meta survived reload + export in round-4. (Rows 29/31 primary revert closed; row-repair + 277 sequence-guard legs remain.)
  - **QA standalone page** — QA-SECTION-DURABLE-001 (1.51.107) + QA-STANDALONE-PAGE-001 + QA page-2 proper (1.51.110-112, wk 1.14.126): spec rule 24. Q&A is a real self-contained page 2 (own header, closure, alternate sign-off, name); the durable section is the source of truth (never auto-hidden). Verified in delivered CL "(9)/(10)". (Closes row 28 rule-24 leg.)
  - **Export enforcement belts (1.51.109)** — ROLE-CLASS-HIDE-001, BULLET-CAP-BELT-001, PLACEHOLDER-EXPORT-GATE-001, SIDEBAR-DEFAULT-32-001, ACCESS-MIDDLE-001; `export-belts-9704.test.mjs`. (Closes row 28 rules 16/17/18/34/36/38 in the export path.)
  - **PROFILE-ACCESS-SCRUB-001 (1.51.103/104/109) + kernel v10b seed scrub** — the banned "has not limited his career" / disability / filler sentences dropped from profile + accessibility in BOTH section shapes AND removed at the kernel SOURCE (v10b, 3 places — the actual seed). PATENT-WORD-001 (rule 21): "patented stray-light" in both pinned copies. (Closes row 28 rules 20/21/22.)
  - **TOOLS-HIDDEN-RESIDUE-001 (1.51.114/115/116, spec rules 42/42a/43)** — tools trimmed inside a compressed value surface as a per-app "Hidden - <category>" review row (never rendered in preview/PDF); eye-restore inserts tokens at the least-space position; long-press / right-click hide menu + hide/unhide analytics fed into the gen prompt. SIDEBAR-LINE-ECONOMY-001 (1.51.113, rule 40) turns the owner's packing technique into a gen rule. (Advances row 26 — the mechanism; exact gold-text acceptance is the open remainder.)
  - **CL-AI-NOTICE-FOOTER-001 (wk 1.14.131)** — CL AI notice moved to the section footer: every page, true bottom, engine-native (CV keeps its VML last-page frame). MERGED-TITLE-JOIN-001 (1.51.113, rule 41): role-merge joins with " & ". CL-RULE-BALANCE-002 worker leg (wk 1.14.132); preview leg still open (row 28-adjacent, live getComputedStyle hunt).
  - **CL-PROSE-LOSS-GUARD-002 regression test (e2d99b6, 2026-07-03)** — HWIC-vanishes-after-edit re-insertion path locked (`cl-prose-loss-guard-reinsert.test.mjs` D/E/F/G); the owner's #1 inline-edit persistence bug (fixed 1.51.7) now has a headless lock. See memory [[nightly-settled-headless-items]].

- Old row 13 (DATA-LOSS load-grace guard) — VERIFIED 2026-07-03: DATA-LOSS-LOAD-GRACE-001 code in BOTH bundles (`__hasReal` guard ×6 in app.js; the ID string is a src-only comment).
- Old row 14 (access-relay D1-WRITE-RETRY) — VERIFIED 2026-07-03: D1-WRITE-RETRY-001 in workers/access-relay/src/index.js (retry wrapper + 2 call sites); relay redeployed 2026-07-03 (JD-SCOPE Stage 2). Watch: `d1_write_failed` recurrence in telemetry.
- Old row 10 CORE (Kernel v2 + upload→kernel ingestion) — was stale "not started": SHIPPED 1.50.515-521 + relay kernel-v2 routes per docs/plan/KERNEL-V2-AND-INGESTION.md status log (D1 staging, tense, language, ingest engine, file extraction, import UI + conflict/gap modals, D1 persistence, entry button, reader bridge, auto-sync, structured apply, language step). Remainder tracked as NEW row 8.
- Old row 5 (photo bridge in-cell float-wrap) — SUPERSEDED by FIGURE-CONTACT-REF-001 (wk 1.14.120): the page-anchored medallion escapes the column/table entirely (worker comment marks the supersession); owner confirmed placement good 2026-07-03.
- Old row 29 (SIDEBAR-HEADLINE-PAGE2-ALIGN + HEADER-CONTACT-PHOTO-FIT) — batch 12/13: align round 2 = wk 1.14.122 spacer paragraph (owner PDF confirm rides new row 20a); contact fixed wk 1.14.120, owner confirmed placement, tracking tightened wk 1.14.123 (eyeball rides new row 20f).
- Old rows 25-28 (CL line-fill + signature spacing; CV orphans root cause; TA/SG results; core-comp table padding) — batches 12-13, PWA 1.51.71/72 + wk 1.14.121/122; sidebar runt mystery closed batch 15 (SIDEBAR-FONT-METRIC-001, 1.51.75, 8/8 proven in diag-orphan-preflight-sidebar.mjs).
- Old row 20 (gen-prompt example-text leftovers) — GEN-DEHARDCODE-003, 1.51.67; de-hardcode sweep COMPLETE.
- Old row 1 (candidate-header 3-col grid) — superseded by owner reference DOCX geometry (wk 1.14.119/120), deployed + owner-confirmed 2026-07-03.
- Owner round-2 batch 13: RESULTS-PIN-OWNER-EDIT-001 + SIDEBAR-TIGHTEN-001 (1.51.72) + ALIGN-002 (wk 1.14.122); batch 14: COMPRESSION-VS-LINE-WIDTH-001 general rule + TONE-DEFAULT-SCANDINAVIAN-002 (1.51.74); batch 16: RESULTS-PIN-NO-NUMBER-001 (1.51.76).
- Orphans v2 (EXPORT-METRIC-MEASURE-001 + EXPORT-PREFLIGHT-ORPHANS-001) — 1.51.57; sidebar half 1.51.71/75.
- Demo/template flicker loops — 1.51.56/1.51.59. CL base template struct — 1.51.58; converters tone-default + hydration family — 1.51.64.
- Gabriel gen-prompt de-hardcode rounds — 1.51.60/62/64/67 (+ workers 3.7.x). Demo-proxy gen-job/coherence/salary parity — workers 3.7.0, verified live.
- Preview pagination promotion gate (SIDEBAR-PROMOTE-MARGIN-001) — 1.51.63. app.js conflict-marker incident — broken 1.51.65, repaired 1.51.66.
- Demo-cap UX (DEMO-CAP-UX-001) — 1.51.68/69. PAN-IDRAET export half — 1.51.70.

- **UNSOL-GEN-DEFECTS-2026-07-29** (owner, from Terma app-generated CV+CL, Nordic Minimal) — generator/measure defects to fix (need render+live-kernel; capture for a render-capable session): (1) **CV renders 5 pages with no reason** — measurement over-splits + crams too many subsections onto page 1 instead of distributing across 2 (pagination/density_fit); (2) **Strategic Expertise table cells render 3 lines** — must be ONE line per row (Nordic Minimal ~55-char cell cap); SCE one-line-per-row not enforced; (3) **Profile renders 9 lines** — cap ≤7 (Nordic Minimal 200-280 chars); writing-engine profile compression not applied; (4) **strange gap before Profile** (spacing); (5) **CL missing SLOGAN** (slogan feature not rendering in generated CL — see [[cl-slogan-content-spec]]/[[slogan-readiness-plan]]); (6) **interests/hobbies missing from output** — add the 6-item interests list to the LIVE kernel (Rugby & inclusive sport / Tai-chi / Culture & Social / Hiking / Reading / Home supervision-cats; owner: "should already be covered by hobbies"). Also apply owner content defaults (memory gabriel-cv-output-defaults): merged role "Electro-Optics Engineer & Team Leader" (Engineer first, no R&D), never-name-the-ODM ("an ODM in Sweden"), accessibility no "(cochlear implant)", DEFAULT VISUAL = figure header-left + rounded boxes (navy-executive/header-left). PM-EN interim delivered as a hand-corrected docx; full batch (5 positions × EN+ES) owed via the app.

- **UNSOL-APP-DEFECTS-2026-07-29** (owner, Terma app-gen CV/CL — sibling of UNSOL-GEN-DEFECTS; APP-SIDE + render-dependent, need the running app/render to fix+verify): (a) **✏ edit → "apply to docs" throws a SCRIPT ERROR** (app.js edit-apply path) — **ROOT CAUSE FOUND + FIXED 1.51.3942 (GAP-APPLY-RICHBLOCK-001):** the gap-closure payload builder had no `rich_block` branch, so on this very app (D1 2751: CV 15/15 rich_block, CL 7/9) the model received 2 of 24 sections and was asked to patch the rest. Fixed + locked by a test that runs the REAL shipping builder. The exact SCRIPT ERROR text was never captured, so re-verify on the owner's next attempt and capture the console message if it recurs; (b) **a control gives NO RESPONSE** (Fit/Fuse? — owner "I press [f]…"); (c) **CL opening is EMPTY** (proxy/writing-engine greeting/opening not generated); (d) **brand-fit colours captured TOO BRIGHT / low-contrast AND don't match the real Terma site** — brand-fit sampler picks wrong/over-bright colours (ties [[brand-colors-contrast-accessibility]]); (e) **banner FIGURE + name/spec/contact are NOT inside the Word section header (header1.xml)** — they sit in the BODY, so they shift out of position; owner wants them INSIDE the Word header (ties the figure-header-left + rounded-boxes default); (f) **CL SLOGAN missing** — the worker already resolves `meta.slogan || meta.cl_slogan` (CL-SLOGAN-META-FIELD-001, L25617); still-empty means the APP isn't forwarding the LLM `cl_slogan` (or none generated) → APP/proxy fix, NOT the worker (subtitle fallback is wrong: slogan must be a sharp observation, not the Process·Products·People triad). All app-side/render items owed to a render+app session; content defaults in [[gabriel-cv-output-defaults]].

- **[CLOSED 2026-07-29, all layers — gen prompt OPENING-MANDATORY 1.51.4045 + sidecar fallback + load fixes 3962/3983; see SESSION_LOG_2026-07-29 §7]** **CL-OPENING-EMPTY-GEN-001** (owner 2026-07-29, "fix — proxy/gen-flow, owed") — the CL **opening** (rich_block) is persisted EMPTY at GENERATION (distinct from the LOAD bug fixed in UNSOL-CL-LOAD-HYDRATE-001 / CL-OPENING-SEED-985). Root cause: the opening skeleton item is a bracketed instruction (`[OPENING & APPLICATION CONTEXT …]`, app.src.js:4634); when the LLM's opening is rejected as placeholder-like (prose-guard bracket-LED detection) or the model omits the slot, it's cleared to "" and — with no prior real opening — stays blank. **Fix (proxy/writing-engine, workers/proxy CL gen):** guarantee the `opening` section is generated and VALIDATED non-empty (retry the slot if empty) and NEVER persist an empty/placeholder opening; if the model still returns nothing, fall back to a real JD-derived opener ("Following my interest in <role> at <company>, …") rather than "". **Interim client guard (PWA, lower-risk):** in `antcv-cl-prose-loss-guard-985.js`, if the opening is empty/placeholder AND no snapshot exists, build a fallback opener from `meta.role`+`meta.company` so an empty opening never renders/exports. Repro: generate a CL where the opening comes back empty (e.g. app 2810 before manual fill). Verified live via relay audit (2810 opening was "" pre-fix). Session log: SESSION_LOG_2026-07-29_CL_LOAD_2810_GUIDE.md.

- **[CLOSED 2026-08-20, same day — fixed + DEPLOYED (cv-proxy `f45a9605`, antcv-demo-proxy `c0db3c0e`); worker suite 42/42, repo 1914/1914. Root cause was NOT the filed one: stylesheets were read through `readCapped(200 KB)` and KOMBIT's theme.min.css is 3.9 MB of reset-greyscale first, so the CORRECT site scored "no usable colors" before the `.com` guess mattered. Three legs shipped — stream-scan the whole stylesheet (`tallyHexFromStream`, flat memory), `tldHints` ccTLD candidates after `.com` (gen-runner derives them from the JD language AND from the ccTLD of a posting host the recruiter guard drops), and `isNonEmployerPage()`. The live page also turned out to be a bot-CHALLENGE interstitial from Cloudflare's network, not the parking page a browser sees. Live: KOMBIT+dk hint → `www.kombit.dk` `#2e2e2e`/`#eb052f`; no hint → honest no-brand; Trackman/NKT unchanged. RESIDUAL (small, no row): an interstitial worded outside `BLOCK_PHRASES` still slips through, and `.com` is still tried before the ccTLD hints. See ACTIVE_BUGS 2026-08-20.]** ~~BRAND-CANONICAL-SITE-CCTLD-001~~ (found 2026-08-20 job-tracker nightly, Opus 5 — CI worker deploys still blocked by `CI-CF-TOKEN-EXPIRED-001`) — `workers/proxy/src/fetch-brand-colors.js` `buildCandidates()` guesses a company domain as **`<slug>.com` only**. For KOMBIT (`kombit.dk`) that resolves to `www.kombit.com`, an unrelated **parked "Coming Soon" page**, whose Tailwind-slate `#0f172a`/`#64748b` pass `isBrandWorthy` — so the loop breaks on it and returns `brandLike: true` with the WRONG company's palette presented as verified. Two legs: **(a)** no ccTLD candidates, so every non-`.com` employer (the whole Nordic pipeline) is exposed; **(b)** no parked/placeholder rejection, so a squatter page outranks a real miss. This is the wrong-brand twin of `BRAND-URL-RECRUITER-GUARD-001` — and that guard makes it WORSE, because it deliberately routes every ATS/recruiter-hosted posting (hr-manager.net, eightfold, workday…) through exactly this name-guess path. Verified live 2026-08-20: `POST /api/fetch-brand-colors {companyName:"KOMBIT"}` → `sampledHost: www.kombit.com`; and **passing an explicit correct `jdUrl: https://kombit.dk/` returned the same wrong host**, so a right answer supplied by the caller does not win. Proposed fix: derive ccTLD candidates from the row's country/JD language before `.com`; reject a sampled page whose text is a parking/placeholder stub; prefer the caller's `jdUrl` host when it validates. Interim mitigation applied tonight: `brand[udbudskonsulenter-i-udbu-6538]` hand-set from `kombit.dk`'s own `theme.min.css` (dominant UI `#2e2e2e`, KOMBIT brand red `#eb052f`), replacing a record previously sampled from the `hr-manager.net` ATS host.

- **[CLOSED 2026-08-20, same day — `STRUCTURAL_IDS = {"role_view", "who"}` generalises role_view's existing carve-out to the WHO block, so both drop levers spend their deficit elsewhere; label matching was impossible because WHO-LANG-001 localises those labels. Test `scripts/job-tracker/test_cl_fit_structural.py` 3/3, RED pre-fix, and pins that ordinary sections stay droppable.]** ~~CL-FIT-WHO-GOAL-DROP-001~~ (found 2026-08-20 job-tracker nightly, Opus 5 — `CL-V5-WHO-GOAL-001` makes the WHO I AM lead sentence and the **`My goal`** row MANDATORY and `Eligibility` the ONLY optional one. `cl_fit` LEVER A ("drop relevance-tail items", `cl_fit.py:169-185`) drops any item whose `b != "lead"` while keeping >=2 non-lead items per section — it has no knowledge of which WHO rows are mandatory. On app **3490** (KOMBIT, 1-page CL, 7 renders) it dropped **`How I operate` AND the mandatory `My goal`** and kept the optional `Eligibility`, i.e. it kept exactly the row the spec allows it to cut and cut the one it must not. The parser's `[who-gate]` warning did NOT fire — both rows were parsed correctly and lost downstream — so today nothing reports the violation. Fix: mark the WHO block's mandatory rows undroppable in `_body_items`/LEVER A (or give them a protected `b` key) and make the fit loop spend its deficit elsewhere.

- **[CLOSED 2026-08-20, same day — `pull` now runs the same 3-way merge `push` does, and BOTH sides gained `_merge_uk_dict` over all 15 uk-keyed dicts (a 409 push was dropping CLOUD-only entries just as pull dropped local-only ones). Union semantics, local wins a genuine conflict and it is reported, snapshot still records the cloud rev, `pull --force` restores the old behaviour. Test `scripts/job-tracker/test_sync_merge.py` 4/4, RED pre-fix.]** ~~JOBTRACKER-PULL-CLOBBERS-LOCAL-SUPPORT-001~~ (found 2026-08-20 job-tracker nightly, Opus 5 — the documented nightly step `job-tracker-sync.py pull --render` is **destructive**: `cmd_pull` does `_save(DOC, doc)` with the cloud doc, overwriting the local file wholesale. `push` has a careful row-level 3-way `_merge`; `pull` has none, and it merges nothing outside `rows` at all. Measured tonight before running it: the local doc held **34 support entries the cloud did not have** (`nvidia_siph`, `napatech`, `templafy`, `quantum_machines`, `satair_airbus`, …) plus 22 differing ones — a blind pull would have silently deleted all 34 blocks of role intel. Worked around by hand this run (backup `job_tracker_doc.json.bak-2026-08-20-nightly` on the Drive, then an **additive-only** local→cloud merge of `support`/`webintel`/`signals`/`notes`/`urls` — 35 keys, never overwriting a cloud value — then pull, rev 210, 72 rows, Proposed Inbox 32 leads). Fix: give `pull` the same 3-way merge `push` has, extended to the non-`rows` dicts, or make it refuse when `status` reports local edits unless `--force`.

- **[CLOSED 2026-08-20, same day — NOT A DEFECT. `discover-positions.py` documents `queue[uk]=False` on every proposed row as a HARD SAFETY INVARIANT: an auto-discovered lead must never generate without the owner arming it, so flipping the 13 rows would have violated it and nothing was flipped. The real problem was that the backlog was INVISIBLE — `gen-runner list` and the end of every `run` now print a `NOT QUEUED but ready` block (`unqueued_ready_rows`/`print_unqueued_summary`), which reports all 13 today. OWNER STILL DECIDES whether to arm any of them.]** ~~JT-DISCOVERY-SEEDS-UNQUEUED-001~~ (observed 2026-08-20 job-tracker nightly, Opus 5 — the doc holds **13 rows with a real JD (>200 chars) and no `artifacts[uk]`** (ranks 59-71: `quantum_machines`, `falcom_gn_group`, `radiometer_danaher_2`, `eaton`, `tetra_pak_processing`, `satair_airbus`, `radiometer_danaher_3`, `microsoft_2`, `3shape_2`, `ambu`, `everllence`, `venzo`, `elevateit_a_s`) that `gen-runner list` skips because `queue[uk]` is **`false`**, not absent. The eligibility gate treats absent-and-ungenerated as eligible but explicit-false as declined, so these never surface to a nightly. Either the discovery routine seeds `queue=false` (a seeding defect) or the owner declined them (correct behaviour, nothing owed) — the doc does not record which. Owner call: if these should generate, flip their `⏰` in the tracker island (or the seeder should leave `queue` absent); tonight's run did NOT touch them.

