{
  "name": "antcv-pwa-dist",
  "version": "1.00.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Static dist package: no build step needed')\"",
    "deploy": "wrangler pages deploy . --project-name=antcv"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
