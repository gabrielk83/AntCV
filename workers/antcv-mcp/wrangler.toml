# For more details on how to configure Wrangler, refer to:
# https://developers.cloudflare.com/workers/wrangler/configuration/

name = "antcv-mcp"
main = "src/index.ts"
compatibility_date = "2025-03-10"
compatibility_flags = ["nodejs_compat"]
upload_source_maps = true

[observability.logs]
enabled = true
invocation_logs = true

# KV namespace for session relay
# Create with: wrangler kv namespace create "SESSIONS"
# Then paste the id below
[[kv_namespaces]]
binding = "SESSIONS"
id = "2ef1dccebcf44af2ba5434a6ba0932c3"



# Note: Use secrets to store sensitive data.
# https://developers.cloudflare.com/workers/configuration/secrets/
#
# Commands to set secrets:
# npx wrangler secret put GITHUB_CLIENT_ID  --config wrangler.toml
# npx wrangler secret put GITHUB_CLIENT_SECRET  --config wrangler.toml
# npx wrangler secret put COOKIE_ENCRYPTION_KEY  --config wrangler.toml
# npx wrangler secret put CF_API_TOKEN --config wrangler.toml
# npx wrangler secret put CF_ACCOUNT_ID --config wrangler.toml

[[migrations]]
tag = "v1"
new_sqlite_classes = ["MyMCP"]

[[durable_objects.bindings]]
class_name = "MyMCP"
name = "MCP_OBJECT"

[[kv_namespaces]]
binding = "OAUTH_KV"
id = "f7e275da5e0b4601a8f23cedad9a2fad"
remote = true

[ai]
binding = "AI"

[dev]
port = 8788

# Smart Placement
# https://developers.cloudflare.com/workers/configuration/smart-placement/#smart-placement
# [placement]
# mode = "smart"

# Bindings
# Bindings allow your Worker to interact with resources on the Cloudflare Developer Platform, including
# databases, object storage, AI inference, real-time communication and more.
# https://developers.cloudflare.com/workers/runtime-apis/bindings/

# Environment Variables
# https://developers.cloudflare.com/workers/wrangler/configuration/#environment-variables
# Note: Use secrets to store sensitive data.
# https://developers.cloudflare.com/workers/configuration/secrets/
# [vars]
# MY_VARIABLE = "production_value"

# Static Assets
# https://developers.cloudflare.com/workers/static-assets/binding/
# [assets]
# directory = "./public/"
# binding = "ASSETS"

# Service Bindings (communicate between multiple Workers)
# https://developers.cloudflare.com/workers/wrangler/configuration/#service-bindings
# [[services]]
# binding = "MY_SERVICE"
# service = "my-service"
