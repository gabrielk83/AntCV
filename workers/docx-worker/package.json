{
  "name": "antcv-docx-worker",
  "version": "1.14.12",
  "description": "Cloudflare Worker that renders strict-Word-compliant .docx files from AntCV JSON payloads.",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev --env=\"\"",
    "deploy": "npx wrangler deploy --env=\"\"",
    "tail": "wrangler tail",
    "test": "node test/smoke.js"
  },
  "engines": {
    "node": ">=18"
  },
  "optionalDependencies": {
    "officecrypto-tool": "^1.4.0"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  }
}
